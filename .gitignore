# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
pnpm-workspace.yaml

node_modules
dist
dist-ssr
*.local

# Electron builder output
release/
out/
bin/

# Fetched at install time by scripts/fetch-vpkmerge.mjs
resources/vpkmerge/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# LLM scratch notes
suggestions.md
.suggestions.md.*
docs/RELEASE.md
AUDIT_REPORT.md
.claude/*
!.claude/settings.json
test-1click.html
README.txt
icon-32.png
/.pnpm-store
.codex-run/
reports/

# Local dev helper: register a prebuilt VPK as a named local mod
scripts/import-local-mod.mjs

# Project summary (LaTeX source + build artifacts)
docs/grimoire-project-summary.*

# Spare README screenshots, kept locally but not yet wired into the grid
docs/screenshots/browse2.png
docs/screenshots/locker-3d-ladygeist.png
docs/screenshots/locker-cards.png
docs/screenshots/locker-skins.png
docs/screenshots/mod-details.png

# vpkmerge / Rust build output
target/
# Cloth solver visual testbed (generated)
.cloth-testbed/

# Local scratch from manual testing (exported profiles, lookups, prompts).
# Root-anchored so real fixtures elsewhere are never caught.
/*.modprofile.json
/*.sharecode.txt
/gb-deadlock-lookup.tsv
/grimoire-import-prompt.md
# Marker written by `pnpm use-local-vpkmerge` so postinstall does not overwrite
# a locally built engine. Machine-specific; never committed.
resources/vpkmerge/.local-build
