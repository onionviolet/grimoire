name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout grimoire
        uses: actions/checkout@v6
        with:
          path: grimoire

      # pnpm-lock.yaml resolves @grimoire/social-types to
      # link:../grimoire-social/packages/social-types. Local dev works because
      # both repos sit side-by-side; CI has to mirror that or every social
      # import becomes an implicit-any and the build fails on the workspace dep.
      # scripts/check-sibling-repos.mjs (preinstall) enforces the layout.
      - name: Checkout grimoire-social (sibling for shared types)
        uses: actions/checkout@v6
        with:
          repository: Slush97/grimoire-social
          path: grimoire-social
          ref: main

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          # Match the local pnpm that generates pnpm-lock.yaml (v10). pnpm 9
          # mis-links the out-of-root @grimoire/social-types linked package
          # (../grimoire-social/packages/*), which makes tsc report it as an
          # unresolved module and every social import cascades to implicit-any.
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: grimoire/pnpm-lock.yaml

      # @grimoire/social-types declares zod as a peer dependency. Locally that
      # resolves via grimoire-social/node_modules/zod (each repo runs its own
      # pnpm install). In CI we have to mirror that — installing only in
      # grimoire leaves the workspace-shared package unable to resolve zod
      # when typecheck follows the symlink.
      - name: Install grimoire-social deps (for the shared workspace package)
        working-directory: grimoire-social
        run: pnpm install --frozen-lockfile

      - name: Install dependencies
        working-directory: grimoire
        run: pnpm install --frozen-lockfile

      - name: Verify app version
        working-directory: grimoire
        run: node scripts/verify-release-version.mjs

      - name: Lint
        working-directory: grimoire
        run: pnpm lint

      - name: Typecheck
        working-directory: grimoire
        run: pnpm exec tsc -b --noEmit

      # Vitest covers the FeModel parser, cloth fit math + solver stability,
      # Source 2 / NPR material reconstruction, the dynamic-scalar evaluator and
      # the profiles vpkIndex resolver. Nothing gated these until now, so a PR
      # could break every one of them and still go green.
      - name: Test
        working-directory: grimoire
        run: pnpm exec vitest run

      # Every referenced t()/Tx key must exist in the English catalog, and the
      # committed locale manifest (what the app fetches from `main` to populate
      # the language picker) must match the catalogs on disk.
      - name: i18n catalog + manifest
        working-directory: grimoire
        run: |
          pnpm i18n:check
          node scripts/gen-locale-manifest.mjs --check

      # The prod-build gate refuses to build without GRIMOIRE_SOCIAL_BASE_URL
      # set to an https URL. CI uses a placeholder to exercise the build
      # pipeline; the value is meaningless at runtime (CI doesn't run the app).
      - name: Build
        working-directory: grimoire
        env:
          GRIMOIRE_SOCIAL_BASE_URL: https://grimoire-social.ci.workers.dev
        run: pnpm exec electron-vite build
