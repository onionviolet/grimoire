name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      draft:
        description: 'Create as draft release'
        required: false
        default: true
        type: boolean

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: windows-latest
            platform: win
          # macos-latest must stay Apple Silicon: vpkmerge publishes a
          # darwin-arm64 asset only (scripts/fetch-vpkmerge.mjs), and the
          # CrossOver bottle Deadlock runs in is arm64 in practice. The build
          # step asserts the arch so an x64 runner fails loudly instead of
          # silently shipping without mod merging.
          - os: macos-latest
            platform: mac

    runs-on: ${{ matrix.os }}

    permissions:
      contents: read
      id-token: write
      attestations: write

    steps:
      - name: Checkout grimoire
        uses: actions/checkout@v6
        with:
          path: grimoire

      # pnpm-lock.yaml resolves @grimoire/social-types to
      # link:../grimoire-social/packages/social-types. Without this sibling
      # checkout the link dangles: pnpm install still exits 0, and the build
      # then fails on unresolved social imports.
      # scripts/check-sibling-repos.mjs (preinstall) enforces the layout.
      - name: Checkout grimoire-social (sibling for shared types)
        uses: actions/checkout@v6
        with:
          repository: Slush97/grimoire-social
          path: grimoire-social
          ref: main

      # Foundry's global-sound catalog and icon replacement path depend on
      # unreleased vpkmerge work from this fork.  Keep this SHA pinned: a
      # release must be reproducible and must not silently change merely
      # because the engine's main branch advanced.
      - name: Checkout forked vpkmerge
        uses: actions/checkout@v6
        if: matrix.platform == 'win'
        with:
          repository: onionviolet/vpkmerge
          ref: 798f3a7d28f3ef314d8f6ebf51ced0d9fe049445
          path: vpkmerge

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        if: matrix.platform == 'win'
        with:
          toolchain: stable-x86_64-pc-windows-msvc

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          # Match the local pnpm that generates pnpm-lock.yaml (v10); see ci.yml.
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: grimoire/pnpm-lock.yaml

      # @grimoire/social-types declares zod as a peer dependency; the symlinked
      # workspace package needs its own node_modules so the import resolves
      # from the Electron build.
      - name: Install grimoire-social deps (for the shared workspace package)
        working-directory: grimoire-social
        run: pnpm install --frozen-lockfile

      - name: Install dependencies
        working-directory: grimoire
        run: pnpm install --frozen-lockfile

      # The pinned fork sidecar is built from source for Windows only today;
      # mac and linux releases fall back to fetch-vpkmerge's asset path, the
      # same as upstream. Extending the pinned build to darwin-arm64 is a
      # deliberate follow-up, not something to improvise inside a release.
      - name: Build the forked vpkmerge sidecar
        working-directory: vpkmerge
        if: matrix.platform == 'win'
        run: cargo +stable-x86_64-pc-windows-msvc build --locked --release -p vpkmerge-cli

      - name: Bundle the forked vpkmerge sidecar
        working-directory: grimoire
        if: matrix.platform == 'win'
        run: pnpm use-local-vpkmerge

      # electron-updater throws in the main process when the packaged app
      # version is not SemVer. Reject bad or mismatched release tags before
      # spending time building artifacts that users cannot launch safely.
      - name: Verify release version
        working-directory: grimoire
        run: node scripts/verify-release-version.mjs

      # Refresh the ad/tracker filter lists so the shipped installer carries
      # current EasyList/EasyPrivacy/uBO rules. postinstall already fetched
      # them; --refresh makes the release build fail loudly rather than ship
      # stale or truncated lists.
      - name: Fetch bundled filter lists
        working-directory: grimoire
        run: node scripts/fetch-filter-lists.mjs --refresh

      - name: Clean previous builds
        working-directory: grimoire
        shell: bash
        run: |
          rm -rf dist release
          mkdir -p release

      - name: Build and package
        id: build
        working-directory: grimoire
        shell: bash
        env:
          # Baked into the renderer by electron.vite.config.ts. Production
          # builds refuse to start without this set to an https:// URL so
          # installers can't ship pointing at localhost:8787.
          GRIMOIRE_SOCIAL_BASE_URL: https://grimoire-social.slusheliott.workers.dev
          # Bake the fork marker into the main-process bundle. This keeps the
          # updater UI honest and ensures these artifacts identify their own
          # GitHub release channel rather than looking like stock builds.
          GRIMOIRE_FORK_BUILD: '1'
          # These secrets are intentionally optional: a fork can produce a
          # testable unsigned build before it has an Apple Developer account.
          # When configured, electron-builder signs and notarizes mac artifacts.
          CSC_LINK: ${{ secrets.MACOS_CERTIFICATE }}
          CSC_KEY_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          if [ "${{ matrix.platform }}" = "linux" ]; then
            pnpm exec electron-vite build && pnpm exec electron-builder --linux --publish never
          elif [ "${{ matrix.platform }}" = "mac" ]; then
            # See the matrix comment: arm64 only. postinstall skips the
            # vpkmerge fetch with a warning on unsupported platforms, so
            # without this the .dmg would build fine and just lose merging.
            ARCH="$(node -p 'process.arch')"
            if [ "$ARCH" != "arm64" ]; then
              echo "ERROR: mac runner is $ARCH; expected arm64 (no darwin-$ARCH vpkmerge asset)"
              exit 1
            fi
            pnpm exec electron-vite build && pnpm exec electron-builder --mac --arm64 --publish never
          else
            pnpm exec electron-vite build && pnpm exec electron-builder --win --publish never
          fi

      - name: List release directory
        if: always()
        working-directory: grimoire
        shell: bash
        run: |
          echo "=== Release directory contents ==="
          if [ -d "release" ]; then
            ls -la release/
            echo "=== File count ==="
            find release -type f | wc -l
          else
            echo "ERROR: release/ does not exist"
            exit 1
          fi

      - name: Generate SHA256 checksums
        working-directory: grimoire
        shell: bash
        run: |
          cd release
          # Hash user-facing artifacts only (skip auto-update metadata and blockmaps).
          # *.zip is the macOS update artifact; win/linux targets produce none.
          shopt -s nullglob
          files=(*.exe *.AppImage *.deb *.dmg *.zip)
          if [ ${#files[@]} -eq 0 ]; then
            echo "ERROR: no artifact files found to hash"
            exit 1
          fi
          # macOS runners have no sha256sum; shasum ships with the OS and emits
          # the same "<hash>  <name>" format.
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${files[@]}" > "SHA256SUMS-${{ matrix.platform }}.txt"
          else
            shasum -a 256 "${files[@]}" > "SHA256SUMS-${{ matrix.platform }}.txt"
          fi
          echo "=== SHA256SUMS-${{ matrix.platform }}.txt ==="
          cat "SHA256SUMS-${{ matrix.platform }}.txt"

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            grimoire/release/*.exe
            grimoire/release/*.AppImage
            grimoire/release/*.deb
            grimoire/release/*.dmg
            grimoire/release/*.zip

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.platform }}-build
          path: grimoire/release/*
          if-no-files-found: error
          retention-days: 1

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write

    steps:
      - name: Checkout release notes
        uses: actions/checkout@v6

      - name: Build release notes from changelog
        run: node scripts/release-notes.mjs

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: List downloaded artifacts
        run: ls -la artifacts/ 2>/dev/null || echo "No artifacts"

      - name: Combine per-platform checksums into SHA256SUMS
        shell: bash
        run: |
          cd artifacts
          shopt -s nullglob
          parts=(SHA256SUMS-*.txt)
          if [ ${#parts[@]} -eq 0 ]; then
            echo "ERROR: no per-platform SHA256SUMS files found"
            exit 1
          fi
          cat "${parts[@]}" > SHA256SUMS
          rm "${parts[@]}"
          echo "=== SHA256SUMS ==="
          cat SHA256SUMS

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: ${{ inputs.draft || false }}
          body_path: release-notes.md
          files: artifacts/*

  aur-publish:
    needs: release
    runs-on: ubuntu-latest
    # Disabled on the fork: the AUR package and its signing key belong to the
    # upstream release pipeline, and fork builds must not publish there. Linux
    # artifacts now build (the flatpak job repackages them), so this could be
    # re-pointed at a fork-owned AUR package if one ever exists.
    if: false

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download Linux artifacts
        uses: actions/download-artifact@v8
        with:
          name: linux-build
          path: artifacts

      - name: Compute version and .deb sha256
        id: meta
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          DEB="artifacts/grimoire_${VERSION}_amd64.deb"
          if [ ! -f "$DEB" ]; then
            echo "ERROR: $DEB not found in artifacts"
            ls -la artifacts/
            exit 1
          fi
          SHA="$(sha256sum "$DEB" | awk '{print $1}')"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION sha=$SHA"

      - name: Setup SSH for AUR
        env:
          AUR_KEY: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$AUR_KEY" > ~/.ssh/aur
          chmod 600 ~/.ssh/aur
          ssh-keyscan -t ed25519 aur.archlinux.org > ~/.ssh/known_hosts 2>/dev/null
          cat > ~/.ssh/config <<'EOF'
          Host aur.archlinux.org
            User aur
            IdentityFile ~/.ssh/aur
            IdentitiesOnly yes
          EOF
          chmod 600 ~/.ssh/config

      - name: Clone AUR repo
        run: git clone ssh://aur@aur.archlinux.org/grimoire-bin.git aur-repo

      - name: Sync PKGBUILD from project and bump version/sha
        shell: bash
        run: |
          cp aur/grimoire-bin/PKGBUILD aur-repo/PKGBUILD
          cd aur-repo
          sed -i "s/^pkgver=.*/pkgver=${{ steps.meta.outputs.version }}/" PKGBUILD
          sed -i "s/^pkgrel=.*/pkgrel=1/" PKGBUILD
          sed -i "s|^sha256sums=.*|sha256sums=('${{ steps.meta.outputs.sha }}')|" PKGBUILD
          echo "=== Updated PKGBUILD ==="
          cat PKGBUILD

      - name: Regenerate .SRCINFO via archlinux container
        shell: bash
        run: |
          docker run --rm -v "$PWD/aur-repo:/pkg:ro" archlinux:latest bash -c '
            {
              useradd -m builder
              cp /pkg/PKGBUILD /tmp/PKGBUILD
              chown builder:builder /tmp/PKGBUILD
            } >&2
            su builder -c "cd /tmp && makepkg --printsrcinfo"
          ' > aur-repo/.SRCINFO
          echo "=== .SRCINFO ==="
          cat aur-repo/.SRCINFO

      - name: Commit and push to AUR
        shell: bash
        run: |
          cd aur-repo
          git config user.name "esocidae"
          git config user.email "slusheliott@gmail.com"
          git add PKGBUILD .SRCINFO
          if git diff --cached --quiet; then
            echo "No changes to push; AUR already matches."
            exit 0
          fi
          git commit -m "Update to ${{ steps.meta.outputs.version }}"
          git push origin master

  apt-publish:
    needs: release
    runs-on: ubuntu-latest
    # Dormant until the R2 bucket + secrets exist and you set the repo variable
    # APT_PUBLISH_ENABLED to "true". Until then this is skipped, so releases
    # stay green while the apt repo isn't wired up. See docs/apt-repo.md.
    if: startsWith(github.ref, 'refs/tags/') && inputs.draft != true && vars.APT_PUBLISH_ENABLED == 'true'

    permissions:
      contents: read

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: auto
      R2_ENDPOINT: https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com
      BUCKET: grimoire-apt

    steps:
      - name: Download Linux artifacts
        uses: actions/download-artifact@v8
        with:
          name: linux-build
          path: artifacts

      - name: Install reprepro
        run: sudo apt-get update && sudo apt-get install -y reprepro

      # Import the dedicated (passphraseless) repo signing key into a throwaway
      # GNUPGHOME and stash its fingerprint for reprepro's SignWith.
      - name: Import signing key
        shell: bash
        run: |
          set -euo pipefail
          GNUPGHOME="$(mktemp -d)"; export GNUPGHOME
          echo "GNUPGHOME=$GNUPGHOME" >> "$GITHUB_ENV"
          echo "${{ secrets.APT_GPG_PRIVATE_KEY }}" | base64 -d | gpg --batch --import
          FPR="$(gpg --list-secret-keys --with-colons | awk -F: '/^fpr:/{print $10; exit}')"
          echo "APT_GPG_FPR=$FPR" >> "$GITHUB_ENV"

      # Rebuild a fresh, signed, latest-only repo from this release's .deb.
      # reprepro keeps a single version per package, so the repo always offers
      # exactly the current release; older versions remain on GitHub Releases.
      - name: Build signed repo
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          DEB="artifacts/grimoire_${VERSION}_amd64.deb"
          if [ ! -f "$DEB" ]; then
            echo "ERROR: $DEB not found in artifacts"; ls -la artifacts; exit 1
          fi
          mkdir -p repo/conf
          cat > repo/conf/distributions <<EOF
          Origin: Grimoire
          Label: Grimoire
          Codename: stable
          Suite: stable
          Architectures: amd64
          Components: main
          Description: Grimoire (Deadlock mod manager) APT repository
          SignWith: ${APT_GPG_FPR}
          EOF
          reprepro -b repo includedeb stable "$DEB"
          gpg --export "$APT_GPG_FPR" > repo/grimoire.gpg

      # Pool .debs are version-stamped and immutable, so cache them hard. The
      # dists/ index reuses stable paths every release, so it must be no-cache
      # or apt update sees a stale package list behind Cloudflare's CDN. Upload
      # the pool before the index so the index never points at a missing .deb.
      - name: Publish to R2
        shell: bash
        run: |
          set -euo pipefail
          aws s3 sync repo/pool/ "s3://${BUCKET}/pool/" --delete \
            --endpoint-url "$R2_ENDPOINT" \
            --cache-control "public, max-age=31536000, immutable"
          aws s3 sync repo/dists/ "s3://${BUCKET}/dists/" --delete \
            --endpoint-url "$R2_ENDPOINT" \
            --cache-control "no-cache"
          aws s3 cp repo/grimoire.gpg "s3://${BUCKET}/grimoire.gpg" \
            --endpoint-url "$R2_ENDPOINT" \
            --cache-control "no-cache"

  # The flatpak repackages the released .deb (same binaries users already run)
  # rather than building from source, so it can only be assembled after the
  # release exists. This job patches the manifest to this version, builds the
  # bundle, attaches it to the release, and commits the bump back to main so
  # the repo matches what shipped. That last part used to be a manual chore
  # (see "chore(flatpak): repackage the v1.27.0 deb").
  flatpak-publish:
    needs: release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') && inputs.draft != true

    permissions:
      contents: write

    # Matches runtime-version/base-version in the manifest. The image ships
    # flatpak + flatpak-builder and pre-adds the flathub remote; --privileged
    # is required for the bubblewrap sandbox the builder runs builds in.
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:freedesktop-24.08
      options: --privileged

    steps:
      # Check out main rather than the tag: a tag checkout is detached and the
      # manifest bump at the end of this job needs a branch to push to.
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main

      # Only needed to hash the .deb. flatpak-builder itself fetches the .deb
      # from the release URL pinned in the manifest, so the bundle is built
      # from exactly the bytes users download.
      - name: Download Linux artifacts
        uses: actions/download-artifact@v8
        with:
          name: linux-build
          path: artifacts

      - name: Point the manifest at this release
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          DEB="artifacts/grimoire_${VERSION}_amd64.deb"
          if [ ! -f "$DEB" ]; then
            echo "ERROR: $DEB not found in artifacts"; ls -la artifacts; exit 1
          fi
          SHA="$(sha256sum "$DEB" | awk '{print $1}')"
          DATE="$(date -u +%Y-%m-%d)"
          MANIFEST=flatpak/com.grimoiremods.Grimoire.yml
          METAINFO=flatpak/com.grimoiremods.Grimoire.metainfo.xml

          # Three version-stamped spots in the manifest: the ar command that
          # unpacks the .deb, and the source url/sha256 pair. The 8-space
          # anchors keep these off the sibling `path:` sources below them.
          # github.server_url/github.repository keep the pin on whichever
          # repo runs this job, so a fork release repackages its own .deb.
          sed -i "s|^      - ar x grimoire_.*_amd64\.deb$|      - ar x grimoire_${VERSION}_amd64.deb|" "$MANIFEST"
          sed -i "s|^        url: .*$|        url: ${{ github.server_url }}/${{ github.repository }}/releases/download/v${VERSION}/grimoire_${VERSION}_amd64.deb|" "$MANIFEST"
          sed -i "s|^        sha256: .*$|        sha256: ${SHA}|" "$MANIFEST"

          # Newest release first. Skipped if the entry already exists so a
          # re-run of this job stays idempotent.
          if grep -q "version=\"${VERSION}\"" "$METAINFO"; then
            echo "metainfo already lists ${VERSION}"
          else
            sed -i "s|^  <releases>$|  <releases>\n    <release version=\"${VERSION}\" date=\"${DATE}\"/>|" "$METAINFO"
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "=== $MANIFEST (sources) ==="
          grep -A4 '^    sources:' "$MANIFEST"
          echo "=== $METAINFO (releases) ==="
          grep -A3 '<releases>' "$METAINFO"

      - name: Build bundle
        uses: flatpak/flatpak-github-actions/flatpak-builder@v6
        with:
          manifest-path: flatpak/com.grimoiremods.Grimoire.yml
          bundle: Grimoire-${{ steps.meta.outputs.version }}.flatpak
          # The .deb source is version-stamped, so a build dir cached from the
          # previous release would never be reused anyway.
          cache: false

      - name: Attach bundle to the release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: Grimoire-${{ steps.meta.outputs.version }}.flatpak

      # Runs after the bundle is attached: if the push is rejected the release
      # is already complete and only the repo-side bump needs redoing.
      - name: Commit the manifest bump
        shell: bash
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config user.name "esocidae"
          git config user.email "slusheliott@gmail.com"
          git add flatpak/com.grimoiremods.Grimoire.yml flatpak/com.grimoiremods.Grimoire.metainfo.xml
          if git diff --cached --quiet; then
            echo "No changes to push; manifest already matches."
            exit 0
          fi
          git commit -m "chore(flatpak): repackage the v${{ steps.meta.outputs.version }} deb"
          git push origin main
