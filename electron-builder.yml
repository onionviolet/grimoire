appId: com.grimoire.modmanager
productName: Grimoire
# GameBanana 1-Click Mod Installer protocol registration
protocols:
  - name: Grimoire 1-Click Installer
    schemes:
      - grimoire
# GitHub auto-update configuration
publish:
  provider: github
  owner: onionviolet
  repo: grimoire
directories:
  output: release
  buildResources: resources
files:
  - dist/**/*
  - "!node_modules/**/*"
  # Include required runtime dependencies
  - node_modules/adm-zip/**/*
  - node_modules/better-sqlite3/**/*
  - node_modules/bindings/**/*
  - node_modules/file-uri-to-path/**/*
  - node_modules/node-7z/**/*
  - node_modules/node-unrar-js/**/*
  - node_modules/7zip-bin/**/*
  - node_modules/ffmpeg-static/**/*
  - node_modules/cross-spawn/**/*
  - node_modules/shebang-command/**/*
  - node_modules/shebang-regex/**/*
  - node_modules/which/**/*
  - node_modules/isexe/**/*
  - node_modules/path-key/**/*
  # Ghostery resolves this package dynamically at runtime to register its
  # cosmetic-filter preload, so Rollup cannot discover and bundle it.
  - node_modules/@ghostery/adblocker-electron-preload/**/*
asarUnpack:
  - node_modules/better-sqlite3/**/*
  - node_modules/node-unrar-js/**/*
  - node_modules/7zip-bin/**/*
  - node_modules/ffmpeg-static/**/*
# vpkmerge CLI binary (fetched via scripts/fetch-vpkmerge.mjs) ships outside
# the asar so the OS can execute it directly. Resolved at runtime via
# process.resourcesPath.
extraResources:
  # ChatLane is the YAML <-> VPK conversion engine for the Chat Wheel page.
  - from: resources/chatlane
    to: chatlane
    filter:
      - "**/*"
  - from: resources/vpkmerge
    to: vpkmerge
    filter:
      - "**/*"
      - "!*.bak"
  # Ad/tracker filter lists fetched at build time by
  # scripts/fetch-filter-lists.mjs; the blocker reads them from
  # process.resourcesPath/filters so the packaged app needs no runtime fetch.
  - from: resources/filters
    to: filters
    filter:
      - "**/*"
linux:
  target:
    - AppImage
    - deb
  category: Game
  icon: resources/icons
  maintainer: esoc
win:
  target:
    - nsis
    - portable
  icon: resources/icon.ico
# macOS runs Deadlock through a CrossOver bottle (see docs/macos.md), so the
# audience is Apple Silicon only in practice. The bundled vpkmerge sidecar
# currently supports Apple Silicon too; do not advertise an Intel artifact
# until it adds darwin-x64 support.
#
# There is no Developer ID cert. identity is pinned to null so electron-builder
# never auto-discovers the local Apple Development cert (which is not valid for
# distribution and would fail on every other machine). Skipping electron-builder
# signing leaves the bundle with an INVALID signature, which macOS reports as
# "damaged", so scripts/adhoc-sign-mac.mjs re-signs ad-hoc afterwards. Users
# still get the "unidentified developer" prompt on first launch.
mac:
  target:
    - dmg
    - zip
  category: public.app-category.games
  icon: resources/icon.png
  identity: null
afterPack: scripts/adhoc-sign-mac.mjs
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  menuCategory: true
  shortcutName: Grimoire
  uninstallDisplayName: Grimoire Mod Manager
  artifactName: "${productName}-Setup-${version}.${ext}"
portable:
  artifactName: "${productName}-Portable-${version}.${ext}"
