---
phase: 08-release-engineering
plan: 01
status: complete
---

# Plan 08-01: Ship v1.27.1

**Goal:** Publish the fork's v1.27.1 GitHub Release end to end.

## Tasks

1. Write the CHANGELOG entry for 1.27.1 (fork release; absorbed upstream v1.27 features; fork fixes; no bare upstream PR numbers)
2. Verify `scripts/verify-release-version.mjs` passes and `scripts/release-notes.mjs` extracts the section
3. Create annotated tag `v1.27.1` at the release commit
4. Push `main` and `v1.27.1` to `origin` (onionviolet/grimoire only)
5. Monitor the `release.yml` run: build, SHA256SUMS, attestations, Release creation
6. Confirm the GitHub Release is published with changelog notes; report the URL

## Acceptance Criteria

- `verify-release-version.mjs` passes with tag == 1.27.1
- Release workflow completes; GitHub Release exists with notes from the changelog
- Release URL reported; STATE.md final state recorded
