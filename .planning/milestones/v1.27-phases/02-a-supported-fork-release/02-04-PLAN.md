---
phase: 02-a-supported-fork-release
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - electron/main/services/heroPoseModels.ts
  - src/components/foundry/GlobalSoundBrowse.tsx
  - src/lib/assetClaims.ts
  - src/lib/assetClaims.test.ts
  - src/lib/useAssetClaims.ts
  - src/lib/globalSoundSections.ts
  - src/lib/heroPortraitIdentity.ts
  - src/lib/soundInventory.ts
  - src/locales/en/translation.json
  - src/locales/manifest.json
  - src/pages/Foundry.tsx
  - docs/merge-plan-upstream-2026-08.md
autonomous: false
requirements: [REQ-upstream-merge-aug-2026]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The five commits on structural-refactor-7 are reachable from main, so nothing is lost when the branch is deleted in plan 02-05"
    - "The merge does not begin until the structural-refactor-7 worktree is confirmed inactive and the working tree is reconciled, so the merge never fights an agent still working there (D-04)"
    - "The two files that exist independently on both sides are resolved into one file each by a recorded human decision, not by an automatic or arbitrary pick (D-04)"
    - "pnpm typecheck, pnpm lint, and pnpm test are all green on the merged tree, so a failure after this point is attributable to the merge and to nothing else (D-04)"
    - "The branch inventory used to drive this work is measured live, not read from the merge-plan document's numbers"
    - "The uncommitted edit to the temporary merge-plan document is committed or deliberately discarded before any branch work starts, because a clean tree is that plan's own stated precondition (D-04)"
  artifacts:
    - path: "src/lib/assetClaims.ts"
      provides: "One path-ownership module, not two competing definitions"
    - path: "src/lib/useAssetClaims.ts"
      provides: "The hook the branch adds, arriving clean with no conflict"
    - path: "docs/merge-plan-upstream-2026-08.md"
      provides: "Reconciled and committed, ready for retirement in plan 02-05"
  key_links:
    - from: "src/lib/assetClaims.ts"
      to: "src/lib/useAssetClaims.ts"
      via: "the hook consumes the resolved ownership module; if the add/add resolution changes the module's exports, the hook must be updated in the same commit"
      pattern: "assetClaims"
    - from: "src/lib/assetClaims.ts"
      to: "src/lib/assetClaims.test.ts"
      via: "the test file is the second half of the same add/add collision and must be resolved to match whichever design wins"
      pattern: "assetClaims"
  prohibitions:
    - statement: "MUST NOT resolve the assetClaims add/add collision by taking one side automatically, by preferring the newer timestamp, or by concatenating both; the design choice belongs to the repository owner"
      category: values
      status: resolved
      verification: judgment
    - statement: "MUST NOT begin the merge while the structural-refactor-7 worktree may still be driven by an agent, because a merge landing under a working agent destroys work neither side can attribute"
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Fold `structural-refactor-7`'s five unmerged commits into `main`, and prove the result is green before anything gets deleted.

Purpose: this is the only branch in the repository holding work that is not reachable from `main`. Per D-04 it gets merged, but only after confirming its worktree is inactive, and the full gate runs afterwards because the instruction was to combine and then make sure it is not an issue later. Deletion is a separate plan (02-05) behind its own one-way door.

Output: a merged `main`, ten resolved conflicts including one design collision settled by a human, and a green gate.

## Why this plan runs after the source-edit plans

`src/locales/en/translation.json` and `src/locales/manifest.json` are on both this merge's conflict list and the earlier plans' file lists. Running the merge last means the conflict on those two files is resolved once, against their final state, rather than twice. It also means a red gate immediately after this merge is attributable to the merge alone, because no other plan is in flight.

## What the numbers were at planning time, and why you must re-measure

Measured live on 2026-08-07 against `main` at `be6bc68`: `structural-refactor-7` is 5 ahead and 96 behind, and `git merge-tree --write-tree main structural-refactor-7` reproduces exactly ten conflicts, two of them add/add on `src/lib/assetClaims.ts` and `src/lib/assetClaims.test.ts`. The branch also adds `src/lib/useAssetClaims.ts`, which does not exist on `main` and arrives without a conflict.

`docs/merge-plan-upstream-2026-08.md` states different behind-counts because it was measured on 2026-08-05, before Phase 1 landed. Only the ahead-count matters for correctness and it has not moved. Per Pitfall 3 in RESEARCH.md, re-measure live and do not treat a changed behind-count as the document's stop-and-ask signal.

Three earlier plans in this phase will also have landed on `main` before this runs, so the conflict set may be slightly larger than ten. Re-run the trial merge and work from what it reports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-a-supported-fork-release/02-RESEARCH.md
@.planning/phases/02-a-supported-fork-release/02-PATTERNS.md
@docs/merge-plan-upstream-2026-08.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the worktree is inactive and reconcile the working tree</name>
  <precondition>The worktree at `.claude/worktrees/agent-a4ad3a26969f16ebb` still exists and is checked out on `structural-refactor-7`.</precondition>
  <read_first>
    - docs/merge-plan-upstream-2026-08.md (section 6 rule 1 and section 8 lines 434 to 449; the stop-and-ask gates this task honors rather than re-derives, plus the Phase 0 clean-tree precondition around line 101)
    - .planning/phases/02-a-supported-fork-release/02-RESEARCH.md (Requirement 3, the live branch inventory and the worktree list)
  </read_first>
  <files>docs/merge-plan-upstream-2026-08.md</files>
  <what-built>
    Before presenting this checkpoint, gather the evidence with read-only commands and put the actual output in front of the user. Do not summarize from the research document; run these now:
    - `git worktree list`
    - `git -C .claude/worktrees/agent-a4ad3a26969f16ebb status --short`
    - `git -C .claude/worktrees/agent-a4ad3a26969f16ebb log -1 --format='%h %cI %s'`
    - `git status --short` in the primary worktree
    - `git rev-list --left-right --count main...structural-refactor-7`
    - `git log main..structural-refactor-7 --oneline`
    - `git merge-tree --write-tree main structural-refactor-7 | grep -i conflict`
    Present the live ahead-count, the live conflict file list, the timestamp of the worktree's last commit, and every uncommitted path in both worktrees.
  </what-built>
  <how-to-verify>
    Two questions, both blocking.

    1. Is the `structural-refactor-7` worktree finished? Look at the last-commit timestamp and at whether that worktree has uncommitted changes. The merge-plan document's own rule is to stop and ask before this phase precisely because merging under a live agent fights that agent. Answer "inactive" or "still working".

    2. What happens to each uncommitted path listed above? The merge-plan document's Phase 0 precondition is a clean tree. For each path say commit, stash, or discard. Note that `docs/merge-plan-upstream-2026-08.md` is among them: its uncommitted diff adds a verification-build step and a stop-and-ask rule about never putting a real social URL in a verification build, which is worth keeping. Paths unrelated to this phase (main-process services, renderer libs, an untracked test file) are someone else's in-progress work and should not be swept into this phase's commits.

    After you answer, the executor applies your instruction path by path, commits or stashes accordingly, and confirms `git status --short` is empty before Task 2 runs.
  </how-to-verify>
  <acceptance_criteria>
    - The live output of all seven commands above appears in the checkpoint presentation
    - After the user's instruction is applied, `git status --short` in the primary worktree produces no output
    - `git stash list` and the commit log together account for every path that was dirty before this task
    - `git rev-list --left-right --count main...structural-refactor-7` shows a right-hand count of 5
    - No file outside the user's stated instruction was committed, stashed, or discarded
  </acceptance_criteria>
  <resume-signal>Reply with "inactive" or "still working", then one disposition (commit, stash, or discard) per uncommitted path.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the assetClaims design collision</name>
  <reversibility rating="costly">Both surfaces that read path ownership are rewritten against whichever module shape wins, so reversing the choice later means touching every consumer plus the hook the branch adds.</reversibility>
  <read_first>
    - src/lib/assetClaims.ts (the version currently on main)
    - src/lib/assetClaims.test.ts (the version currently on main)
    - docs/merge-plan-upstream-2026-08.md (the section flagging this collision as requiring a human call rather than an automatic pick)
  </read_first>
  <files>src/lib/assetClaims.ts, src/lib/assetClaims.test.ts</files>
  <decision>
    Which design wins for `src/lib/assetClaims.ts` and its test: the version on `main`, the version on `structural-refactor-7`, or a stated combination of the two.
  </decision>
  <context>
    Both files were created independently on the two sides, so git reports an add/add conflict and has no common ancestor to merge from. There is no correct automatic answer: git would either pick a side or interleave both, and either outcome is a design decision made by a tool.

    Before presenting the options, gather the evidence:
    - `git show main:src/lib/assetClaims.ts` and `git show structural-refactor-7:src/lib/assetClaims.ts`
    - `git show main:src/lib/assetClaims.test.ts` and `git show structural-refactor-7:src/lib/assetClaims.test.ts`
    - `git show structural-refactor-7:src/lib/useAssetClaims.ts`, the hook the branch adds on top of its version
    - `git grep -n "assetClaims" main -- src electron` and the same against `structural-refactor-7`, to show which call sites each side has
    Summarize for each side: the exported surface, what it treats as the ownership key, and how many call sites depend on it. The branch commit `eb2ff8c refactor(claims): derive path ownership once, for both surfaces` states the branch's intent in its own words; quote it.

    The governing invariant either answer must preserve: exact normalized VPK entry paths are the ownership key. Labels, hero names, and mod metadata are never a substitute.
  </context>
  <options>
    <option id="keep-main">
      <name>Keep main's version</name>
      <pros>No consumer on main changes. The branch's other four commits still land.</pros>
      <cons>The branch's hook `useAssetClaims.ts` and the call sites in its other commits may not compile against it, so this option can cascade into the rest of the merge.</cons>
    </option>
    <option id="keep-branch">
      <name>Keep the branch's version</name>
      <pros>Matches the intent the branch commit states: derive path ownership once, for both surfaces. Its hook and its call sites arrive consistent.</pros>
      <cons>Any consumer added on main since the branch diverged must be rewritten against the new exported surface in this same commit.</cons>
    </option>
    <option id="combine">
      <name>A stated combination</name>
      <pros>Keeps a capability only one side has.</pros>
      <cons>Produces a module neither side reviewed. Only pick this if you name exactly which exports come from which side.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Both versions of both files were shown to the user before the question was asked
    - The chosen option id is recorded in the plan summary along with the user's stated reason
    - After Task 3, `src/lib/assetClaims.ts` and `src/lib/assetClaims.test.ts` each exist exactly once with no conflict marker
    - `grep -rc "<<<<<<<" src/lib/assetClaims.ts src/lib/assetClaims.test.ts` returns 0 for both files
  </acceptance_criteria>
  <resume-signal>Select: keep-main, keep-branch, or combine plus which exports come from which side.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge, resolve every conflict, and run the full gate</name>
  <reversibility rating="reversible">The merge commit can be undone with a reset while `structural-refactor-7` still exists; the irreversible step is branch deletion, which lives behind the checkpoint in plan 02-05.</reversibility>
  <read_first>
    - docs/merge-plan-upstream-2026-08.md (the Phase B mechanics and the resolution notes for each conflicting file)
    - .planning/phases/02-a-supported-fork-release/02-RESEARCH.md (Requirement 3, the reproduced conflict list)
    - src/locales/en/translation.json (one of the conflicting files, and one that plans 02-01 and 02-03 also edited in this phase)
    - CLAUDE.md (the i18n and encoding gates, which run over the merged catalog)
    - scripts/check-upstream-refs.mjs (the backlink rule the merge commit message must not break)
  </read_first>
  <files>electron/main/services/heroPoseModels.ts, src/components/foundry/GlobalSoundBrowse.tsx, src/lib/assetClaims.ts, src/lib/assetClaims.test.ts, src/lib/useAssetClaims.ts, src/lib/globalSoundSections.ts, src/lib/heroPortraitIdentity.ts, src/lib/soundInventory.ts, src/locales/en/translation.json, src/locales/manifest.json, src/pages/Foundry.tsx</files>
  <action>
    On `main`, run `git merge structural-refactor-7`. Do not use `--squash` and do not rebase: the five commits keep their identity so `git branch -d` can later prove the branch is merged.

    Resolve every reported conflict. Work from the live conflict list produced in Task 1, not from a remembered list. Known shape at planning time:
    - `src/lib/assetClaims.ts` and `src/lib/assetClaims.test.ts` are add/add. Resolve them exactly as the user decided in Task 2, and update `src/lib/useAssetClaims.ts` and every call site in the same commit if the chosen surface differs from what they expect.
    - `src/locales/en/translation.json` and `src/locales/manifest.json` are content conflicts. Take both sides' keys: the branch's sound-vocabulary rework and this phase's own key changes from plans 02-01 and 02-03 are additive to each other. After resolving the catalog, regenerate the manifest with `pnpm i18n:manifest` rather than hand-merging it, since it is generated output.
    - `electron/main/services/heroPoseModels.ts`, `src/components/foundry/GlobalSoundBrowse.tsx`, `src/lib/globalSoundSections.ts`, `src/lib/heroPortraitIdentity.ts`, `src/lib/soundInventory.ts`, and `src/pages/Foundry.tsx` are content conflicts. Resolve by intent: the branch's four refactor commits fold duplicate hero-name tables into one, derive path ownership once, and give the sound vocabulary a single set of terms. Preserve that consolidation rather than reinstating a duplicate that `main` happens to still carry, and preserve any behavior `main` gained after the branch diverged.

    Never leave a conflict marker in a file. Never resolve a conflict by deleting a feature that only one side has without saying so in the merge commit message.

    Write a merge commit message that describes what landed and why, and that contains no bare issue reference of three or more digits and no github.com link to the upstream repository, because `pnpm refs:check` rejects those and GitHub would post a backlink in a repository this fork does not own.

    Then run the entire gate. Every one of these must pass before this task is done: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm i18n:check`, `node scripts/gen-locale-manifest.mjs --check`, `pnpm encoding:check`, `pnpm refs:check`, and `pnpm engine-pin:check`. If any fails, fix it in this same task: a red gate here is attributable to this merge, which is the whole reason this plan runs alone in its wave.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm i18n:check &amp;&amp; node scripts/gen-locale-manifest.mjs --check &amp;&amp; pnpm encoding:check &amp;&amp; pnpm refs:check &amp;&amp; pnpm engine-pin:check</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --left-right --count main...structural-refactor-7` shows a right-hand count of 0
    - `git branch --no-merged main` produces no output
    - `git grep -c "<<<<<<<" -- src electron docs scripts` finds no match in any tracked file
    - `git log -1 --format=%P main | wc -w` outputs 2, proving a real merge commit rather than a squash or a rebase
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `pnpm test` exits 0
    - `pnpm i18n:check` exits 0 and `node scripts/gen-locale-manifest.mjs --check` exits 0
    - `pnpm encoding:check` exits 0
    - `pnpm refs:check` exits 0
    - `pnpm engine-pin:check` exits 0
    - `test -f src/lib/useAssetClaims.ts` succeeds, proving the branch's new file arrived
    - `pnpm exec vitest run src/lib/assetClaims.test.ts` exits 0
  </acceptance_criteria>
  <done>The five commits are reachable from `main`, no conflict marker survives anywhere, and the entire repository gate is green on the merged tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unmerged branch to main | Five commits nobody has reviewed in the context of current `main` enter the shipped tree. |
| concurrent worktree agent to repository state | Another agent may be mid-work in `.claude/worktrees/agent-a4ad3a26969f16ebb` on the branch being merged. |
| commit message to upstream repository | A commit message containing a bare issue number or an upstream link makes GitHub post a permanent backlink in a repository this fork does not own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-A | Denial of service (of work) | `.claude/worktrees/agent-a4ad3a26969f16ebb` | high | mitigate | Task 1 is a blocking checkpoint that gathers live worktree evidence and requires an explicit "inactive" before any merge command runs. This is the merge-plan document's own rule, honored rather than re-derived. |
| T-02-04-B | Tampering | resolved conflict files | high | mitigate | A conflict resolved by silently dropping one side's feature is indistinguishable from a bug later. Every conflict is resolved by stated intent, the add/add pair by a recorded human decision, and no conflict marker may survive. The full gate runs on the result. |
| T-02-04-C | Repudiation | merge commit message | medium | mitigate | `pnpm refs:check` is in the task's gate. The message must contain no bare three-digit issue reference and no upstream github.com link. The five inbound commit messages were checked at planning time and contain none. |
| T-02-04-D | Denial of service (of work) | uncommitted working-tree changes | medium | mitigate | Task 1 enumerates every dirty path live and applies a per-path disposition the user states, so unrelated in-progress work is neither swept into this phase's commits nor destroyed by a checkout. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | This plan runs no package-manager install task. `pnpm-lock.yaml` is not on the conflict list; if the live trial merge reports it as conflicting, stop and treat that as a dependency change requiring its own legitimacy review before proceeding. |
</threat_model>

<verification>
- `git branch --no-merged main` produces no output
- `git log -1 --format=%P main` names two parents
- No conflict marker anywhere in the tracked tree
- `pnpm typecheck && pnpm lint && pnpm test && pnpm i18n:check && node scripts/gen-locale-manifest.mjs --check && pnpm encoding:check && pnpm refs:check && pnpm engine-pin:check` all green
- The chosen `assetClaims` option id and the user's reason are recorded in the summary
</verification>

<success_criteria>
No branch holds unmerged work, and the merged tree passes every gate this repository runs, so the deletions in plan 02-05 discard nothing and hide nothing.
</success_criteria>

<output>
Create `.planning/phases/02-a-supported-fork-release/02-04-SUMMARY.md` when done
</output>
