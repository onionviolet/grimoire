---
phase: 08-release-engineering
plan: 01
subsystem: release
tags: [release, changelog, tag, github-release]

requires: []
provides:
  - CHANGELOG entry for 1.27.1 (fork release; absorbed upstream v1.27; fork fixes; no bare upstream PR references)
  - Annotated tag v1.27.1 pushed to origin (onionviolet/grimoire only)
  - Published GitHub Release with installer, blockmap, auto-update metadata, SHA256SUMS, and provenance attestations
affects: [REQ-release-v1.27.1]

actuals:
  tokens: 0
  tasks: 6
  commits: 1

one_liner: v1.27.1 shipped end to end: changelog, tag, push, and a published GitHub Release with installer, checksums, and attestations.
---

# Plan 08-01 Summary: Ship v1.27.1

## Delivered

- **CHANGELOG.md** entry `## [1.27.1] - 2026-08-10` describing the fork release:
  DeadlockForge loopback bridge, GameBanana mirror routing + failover,
  download-servers diagnostics card, crosshair rasterization, VPK `treeSize`
  clamp, and the fork's fixes (live-store bulk undo, blocker-line aria,
  portrait catalog failed state). No bare upstream PR numbers.
- **Version verification:** `scripts/verify-release-version.mjs` passes
  (`Release version verified: 1.27.1`); `scripts/release-notes.mjs 1.27.1`
  extracts the changelog section.
- **Tag:** annotated `v1.27.1` at commit `071d11c`, pushed to
  `onionviolet/grimoire` (the local `v1.27` archive tag and upstream's local
  `v1.27.0` tag were deliberately not pushed).
- **Release:** `release.yml` run `31426792542` completed; the GitHub Release
  `v1.27.1` is published (draft=false) at
  https://github.com/onionviolet/grimoire/releases/tag/v1.27.1 with
  `Grimoire-Setup-1.27.1.exe`, `Grimoire-Portable-1.27.1.exe`, blockmap,
  `latest.yml`, `SHA256SUMS`, and builder-debug.yml; provenance attestations
  were generated by `actions/attest-build-provenance`.

## Gate Results

- `scripts/verify-release-version.mjs` - pass
- Push gates (`.husky/pre-push`): i18n:check, manifest --check, encoding:check,
  refs:check, engine-pin:check - all pass
- Release workflow (windows-latest): build + package, checksums, attestation,
  release creation - all pass

## Notes / Carried Debt

- The CI test job on `main` is red on two pre-existing Linux-CI issues, neither
  a regression from this run and neither affecting the Windows release:
  `scripts/check-encoding.test.ts` (identical failure on the 08-05 and 08-07
  CI runs; platform-sensitive fixture decoding) and the symlink-sweep case in
  `electron/main/services/browserDownloadCapture.test.ts` (first CI exposure;
  Linux-only, skipped on Windows). Owner: fork maintainers; recommended fix is
  a follow-up that makes the encoding test platform-independent and hardens the
  Linux symlink gate.
