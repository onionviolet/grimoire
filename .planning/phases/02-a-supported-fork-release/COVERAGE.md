No external API integration: this phase edits static support links, a fetch-script header comment, a page-level route gate, documentation, and git branch state. The deterministic detector returned `detected: false` over the phase scope. The one external service in view, the `grimoire-social` Worker, is deliberately left untouched by locked decision D-01, which keeps every existing call site and the baked base URL exactly as they are; no capability of that service is added, removed, or newly consumed.
