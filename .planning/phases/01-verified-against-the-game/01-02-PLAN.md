---
phase: 01-verified-against-the-game
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/foundry/AlternativesGallery.test.tsx
  - src/components/foundry/MySoundChanges.test.tsx
autonomous: true
requirements:
  - REQ-renderer-test-harness

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The alternatives-gallery lane is driven at interaction depth: the audition control transitions through busy and playing states, calls `foundryAuditionSourceClip` with the clicked member's mod id and entry path, and surfaces the failure toast when the clip resolves null (D-04)."
    - "The sound trim/gain badge lane renders `SoundChangeDetails` against a recorded `SoundTuningState` and asserts the visible badge text, not merely that some badge element exists (D-04)."
    - "The sound trim/gain badge test asserts the rendered trim and gain strings are the values `soundTuningBadges` produced, interpolated into the existing catalog strings, with no rounding, truncation, or unit conversion applied inside the component."
    - "Both tests follow the harness shape proven in 01-01: per-file `// @vitest-environment jsdom` pragma, a side-effect `../../i18n` import, raw `react-dom/client` + `act`, and a per-file `window.electronAPI` stub carrying only the methods that lane reaches (D-01, D-03)."
  prohibitions:
    - statement: "MUST NOT let a render test report coverage it does not have. A lane whose media or IPC dependency is unstubbed silently exercises a fallback branch, so a test that only asserts the component rendered without throwing certifies nothing."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/components/foundry/AlternativesGallery.test.tsx"
      provides: "alternatives gallery render + audition interaction coverage"
      min_lines: 50
    - path: "src/components/foundry/MySoundChanges.test.tsx"
      provides: "sound trim/gain badge render coverage"
      min_lines: 50
  key_links:
    - from: "src/components/foundry/AlternativesGallery.test.tsx"
      to: "src/components/foundry/AlternativesGallery.tsx"
      via: "renders the default export AlternativePreview under createRoot"
      pattern: "from './AlternativesGallery'"
    - from: "src/components/foundry/MySoundChanges.test.tsx"
      to: "src/components/foundry/soundTuning.ts"
      via: "asserts rendered badge text equals soundTuningBadges output"
      pattern: "soundTuningBadges|describeSoundTuning"
---

<objective>
Render and drive two more of the six Foundry lanes `REQ-renderer-test-harness` names: the alternatives gallery and the sound trim/gain badges.

Purpose: both lanes shipped with pure-model coverage only. `alternativePreview.ts` and `soundTuning.ts` are tested; the components that display them are not. This plan closes that for both, reusing the harness proven in 01-01 without re-deciding any of its architecture.

Output: `src/components/foundry/AlternativesGallery.test.tsx`, `src/components/foundry/MySoundChanges.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-verified-against-the-game/01-CONTEXT.md
@.planning/phases/01-verified-against-the-game/01-PATTERNS.md
@.planning/phases/01-verified-against-the-game/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and files created by phase 01 (do not flag these as drift; none existed before this phase):
`src/components/foundry/ChangePools.test.tsx`, `src/components/foundry/AssetSourcesPanel.test.tsx`, `src/components/foundry/AlternativesGallery.test.tsx`, `src/components/foundry/MySoundChanges.test.tsx`, `src/components/foundry/SoundImportEditor.test.tsx`, `src/components/foundry/PortraitEditor.test.tsx`, `electron/main/services/chatWheel.roundtrip.test.ts`, `docs/ingame-verification-record.md`, `scripts/check-verification-record.mjs` (exports `parseVerificationRecord`, `checkVerificationRecord`; CLI flag `--strict`), `scripts/check-verification-record.test.ts`, the `jsdom` devDependency at `24.1.3`, the `engineDefault` field on `HUD_CONVARS` / `ADVANCED_GAMEINFO_CONVARS` / both `ConfigKeyDefinition` declarations / `PerformanceConvarState`, and the `settings.gameConvars.valueStateHint.engineDefault` i18n key.

<tasks>

<task type="auto">
  <name>Task 1: Alternatives gallery renders and auditions the clicked member</name>
  <precondition>`src/components/foundry/ChangePools.test.tsx` from plan 01-01 exists and `pnpm exec vitest run src/components/foundry/ChangePools.test.tsx` exits 0, so the harness shape this test copies is proven.</precondition>
  <read_first>
    - `src/components/foundry/ChangePools.test.tsx` (the proven harness shape: pragma, `../../i18n` side-effect import, `IS_REACT_ACT_ENVIRONMENT`, root lifecycle, electronAPI stub style)
    - `src/components/foundry/AlternativesGallery.tsx` (whole file: the default export `AlternativePreview`, `KindIcon`, `PreviewImage`, `SourceThumbnail` and its `foundrySourceThumbnail` effect, and `AuditionButton` with its `playing`/`busy` state machine and `showToast` error path)
    - `src/components/foundry/alternativePreview.ts` and `alternativePreview.test.ts` (`alternativePreviewFor`, `rememberPreviewUrl`, and the existing inline-factory style for `FoundryPoolMember` fixtures)
    - `src/components/foundry/poolView.ts` (the `FoundryPoolMember` shape, including `sourceFileName`, `kind`, `entries`, `enabled`, `inShuffle`, `matched`)
    - `src/stores/toastStore.ts` (`showToast` signature, to decide whether to spy on it or assert the rendered toast)
    - `src/lib/api.ts` around the `foundryAuditionSourceClip` and `foundrySourceThumbnail` wrappers
  </read_first>
  <files>src/components/foundry/AlternativesGallery.test.tsx</files>
  <action>
Create `src/components/foundry/AlternativesGallery.test.tsx` following the 01-01 harness shape exactly: first-line `// @vitest-environment jsdom` pragma, a side-effect import of `../../i18n`, `IS_REACT_ACT_ENVIRONMENT`, `createRoot` plus `act`, and the `beforeEach`/`afterEach` host-and-root lifecycle ending in `vi.restoreAllMocks()`.

Stub `window.electronAPI.foundry` with exactly two methods: `sourceThumbnail` and `auditionSourceClip`. `SourceThumbnail` calls `foundrySourceThumbnail` from an effect on mount for image-kind members, so that method must be present even in the audio cases.

Build `FoundryPoolMember` fixtures with local inline factory functions with overridable fields. Cover both member kinds the component branches on: one that renders through `PreviewImage`/`SourceThumbnail`, and one sound member that renders `AuditionButton`.

Interaction depth (D-04), three cases:

1. Audition happy path. Click the audition control and assert, in order, that the control is disabled while the `auditionSourceClip` promise is pending, that `auditionSourceClip` was called with the exact `(modId, entryPath)` the clicked member represents, and that the control's `aria-label` swaps from the play label to the stop label once the promise resolves. The label swap is driven by `playing` state, which the component sets synchronously before awaiting `audio.play()`.
2. Clip unavailable. Have `auditionSourceClip` resolve `null` and assert the component does not enter the playing state and reports the failure through the toast path. Either spy on `showToast` or render the toast surface and assert its text; pick one and be consistent.
3. Stop. With the component in the playing state, click again and assert the element's `pause` was called and the label returns to the play form.

Under jsdom `HTMLMediaElement.prototype.play` is a no-op returning `undefined` rather than a promise, and `onended` never fires on its own. Do not write an assertion that waits for playback to end; if the ended transition needs covering, invoke the element's `onended` handler explicitly from the test. Spy on `HTMLMediaElement.prototype.play` and `pause` so case 3 has something real to assert against.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/foundry/AlternativesGallery.test.tsx</automated>
    <automated>grep -q "auditionSourceClip" src/components/foundry/AlternativesGallery.test.tsx</automated>
    <automated>grep -q "sourceThumbnail" src/components/foundry/AlternativesGallery.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/components/foundry/AlternativesGallery.test.tsx` exits 0 with at least 3 passing tests
    - The test asserts `auditionSourceClip` was called with the exact mod id and entry path of the clicked member
    - The test covers the resolves-null case and asserts the component does not enter the playing state
    - The test spies on `HTMLMediaElement.prototype.play` and `HTMLMediaElement.prototype.pause` and asserts against those spies
    - Line 1 of the file is `// @vitest-environment jsdom`
    - The file imports `../../i18n` for its initialisation side effect
  </acceptance_criteria>
  <done>The alternatives gallery renders both member kinds, auditions the exact clicked member, reports an unavailable clip, and stops on a second click.</done>
</task>

<task type="auto">
  <name>Task 2: Sound trim/gain badges render the recorded tuning verbatim</name>
  <read_first>
    - `src/components/foundry/MySoundChanges.tsx` (whole file: the exported `SoundChangeDetails` and its props, the internal `SoundTuningBadges` it renders at the `tuning` call site, `tuningSummary`, and the `useConfirm` call in `SoundChangeDetails`)
    - `src/components/foundry/soundTuning.ts` and `soundTuning.test.ts` (`describeSoundTuning`, `soundTuningBadges`, `formatTrimWindow`, `formatGain`, the `SoundTuningState` shape, and the existing fixture style)
    - `src/components/common/confirmContext.ts` (`useConfirm` throws outside a `ConfirmContext.Provider`; the test must supply one)
    - `src/locales/en/translation.json` at the `soundTools.badge.*` keys (`trim`, `gain`, `loopOn`, `loopOff`, `untouched`, `legacy`, `title`) so assertions match the real interpolated strings
    - `src/types/mod.ts` at the `Mod` and `soundSwap` shapes, and `src/components/foundry/changeList.test.ts` for the existing `Mod` inline-factory style
    - `src/lib/api.ts` around `foundrySoundAnnotationKey`, `foundryInspectAssetSources`, `foundryCheckAudioPaths`, `foundrySwapSound`
  </read_first>
  <files>src/components/foundry/MySoundChanges.test.tsx</files>
  <action>
Create `src/components/foundry/MySoundChanges.test.tsx` covering the sound trim/gain badge lane through the exported `SoundChangeDetails` (the badges component itself is module-private and stays that way; do not export it just to test it).

Harness: same shape as 01-01. Additionally wrap the render in a `ConfirmContext.Provider` supplying a `vi.fn()` that resolves `false`, because `SoundChangeDetails` calls `useConfirm`, which throws without a provider.

Stub `window.electronAPI.foundry` with the methods `SoundChangeDetails` reaches: `inspectAssetSources`, `checkAudioPaths`, and `swapSound`. `foundrySoundAnnotationKey` is a pure string join in `src/lib/api.ts` with no IPC, so it needs no stub. Read the component before finalising the stub object: at least one of these is reached from an expand-driven effect rather than a direct click.

Build a `Mod` with a recorded `soundSwap.reforge` using an inline factory. Drive `describeSoundTuning` over that mod to get the `SoundTuningState` the component will derive, and assert against that same derived value rather than a hand-typed string.

Interaction depth (D-04), three cases:

1. Tuned change. A mod whose recorded state yields trim and gain badges. Assert the rendered badge text equals the catalog string interpolated with the exact `value` field `soundTuningBadges` returned for that badge kind. This is the point of the test: the component must display what `soundTuning.ts` computed, with no second rounding, no unit conversion, and no re-derivation. A component that reformatted `2.35s` into `2s` would pass a looser assertion and be wrong.
2. Untouched change. A mod whose recorded state yields the untouched badge, asserting the untouched string renders and no trim or gain badge does.
3. No badges. A state for which `soundTuningBadges` returns an empty array, asserting the badge container is absent rather than rendered empty.

Also assert the expanded detail surface shows the per-clip winner text that only appears after the `inspectAssetSources` stub resolves, so the test covers the effect path and not only static props.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/foundry/MySoundChanges.test.tsx</automated>
    <automated>grep -Eq "soundTuningBadges|describeSoundTuning" src/components/foundry/MySoundChanges.test.tsx</automated>
    <automated>grep -q "ConfirmContext" src/components/foundry/MySoundChanges.test.tsx</automated>
    <automated>pnpm exec tsc -b</automated>
    <automated>pnpm exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/components/foundry/MySoundChanges.test.tsx` exits 0 with at least 3 passing tests
    - The test derives its expected badge values from `soundTuningBadges` / `describeSoundTuning` output rather than hand-typed literals
    - The test renders `SoundChangeDetails` inside a `ConfirmContext.Provider`
    - The test covers the tuned, untouched, and no-badge states
    - The test asserts on text that appears only after the `inspectAssetSources` stub resolves
    - `pnpm exec tsc -b` exits 0 and `pnpm exec vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>The sound trim/gain badge lane renders the recorded tuning as computed, across the tuned, untouched, and empty states, and its expanded detail resolves real winner text through the IPC stub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> `window.electronAPI` stub | The test stands in for the preload `contextBridge`; nothing reaches the real main process or the filesystem |
| test process -> jsdom media element | `HTMLMediaElement` is a jsdom stub; no audio device or network fetch is involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information disclosure | `window.electronAPI` test stubs | low | accept | Plain in-process objects holding fabricated fixture data. No credential, no path from the developer's real install, and `*.test.tsx` is outside the electron-vite renderer entry so nothing ships. |
| T-01-05 | Tampering | audition URL handling in `AlternativesGallery` | low | accept | The clip URL is produced by the stub, assigned to a jsdom `HTMLMediaElement` whose `play()` is a no-op, and never fetched. The real audition path's input validation is unchanged by this plan. |
| T-01-SC | Tampering | npm/pnpm installs | high | mitigate | This plan installs nothing. The single phase dependency (`jsdom`) is gated by the blocking human checkpoint in 01-01. |
</threat_model>

<verification>
- `pnpm exec vitest run` (full suite) green
- `pnpm exec tsc -b` green
- `pnpm lint` green
- `pnpm encoding:check` green
- `git diff --exit-code vitest.config.ts package.json` exits 0 (this plan adds no dependency and no config change)
</verification>

<success_criteria>
- Four of the six lanes named by `REQ-renderer-test-harness` now have interaction-depth render tests
- No lane test asserts only that a component mounted
- No new dependency, no `vitest.config.ts` change, no shared-file edit beyond the two new test files
</success_criteria>

<output>
Create `.planning/phases/01-verified-against-the-game/01-02-SUMMARY.md` when done
</output>
