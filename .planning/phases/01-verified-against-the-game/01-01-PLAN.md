---
phase: 01-verified-against-the-game
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/foundry/ChangePools.test.tsx
  - src/components/foundry/AssetSourcesPanel.test.tsx
autonomous: false
requirements:
  - REQ-renderer-test-harness

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`package.json` declares `jsdom` at exactly `24.1.3` in `devDependencies`, so the render tests no longer depend on a hoisted transitive copy (D-02)."
    - "A Foundry lane component renders under jsdom through `react-dom/client` + `act` with no testing-library, a per-file `// @vitest-environment jsdom` pragma, and an unmodified `vitest.config.ts` (D-01, D-03)."
    - "The pool-cards lane test drives the shipped behaviour: the shuffle control flips `aria-pressed` and the mount-time `foundryInspectAssetSources` call resolves into visible winner text, rather than asserting that the component mounted (D-04)."
    - "The audition-preview lane test drives `AssetSourcesPanel`'s inspect-then-audition path and asserts against the stub's recorded calls (D-04)."
    - "Every `window.electronAPI.foundry` method a lane reaches, including the ones called from a mount-time effect, is present on that test's stub object; an unstubbed method surfaces as a failing assertion, not as an unhandled rejection."
  prohibitions:
    - statement: "MUST NOT let a render test report coverage it does not have. A lane whose canvas, media, IPC, or i18n dependency is unstubbed silently exercises a null-guarded fallback branch, so a test that only asserts the component rendered without throwing certifies nothing."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "package.json"
      provides: "explicit pinned jsdom devDependency"
      contains: "\"jsdom\": \"24.1.3\""
    - path: "src/components/foundry/ChangePools.test.tsx"
      provides: "pool-cards lane render + interaction coverage"
      min_lines: 60
    - path: "src/components/foundry/AssetSourcesPanel.test.tsx"
      provides: "audition-preview lane render + interaction coverage"
      min_lines: 60
  key_links:
    - from: "src/components/foundry/ChangePools.test.tsx"
      to: "src/components/foundry/ChangePools.tsx"
      via: "renders the default export FoundryPoolList under createRoot"
      pattern: "from './ChangePools'"
    - from: "src/components/foundry/ChangePools.test.tsx"
      to: "src/lib/api.ts"
      via: "window.electronAPI.foundry stub standing in for the preload contextBridge"
      pattern: "electronAPI"
    - from: "src/components/foundry/AssetSourcesPanel.test.tsx"
      to: "src/components/foundry/AssetSourcesPanel.tsx"
      via: "renders the default export inside a MemoryRouter"
      pattern: "from './AssetSourcesPanel'"
---

<objective>
Prove the render-test architecture end to end on one Foundry lane, then expand to a second, and pin the dependency the whole harness rests on.

Purpose: `REQ-renderer-test-harness` names six shipped Foundry lanes that no check has ever rendered. Before writing six test files, one lane must be driven all the way through the stack the harness needs: an explicitly pinned `jsdom`, a per-file environment pragma, an initialised i18next catalog, a `window.electronAPI` stand-in for the preload bridge, and a real DOM interaction assertion. If any of those five is wrong, it is wrong in all six files. This plan finds that out after one commit.

Output: `jsdom@24.1.3` in `devDependencies`, `src/components/foundry/ChangePools.test.tsx` (pool cards), `src/components/foundry/AssetSourcesPanel.test.tsx` (audition preview).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-against-the-game/01-CONTEXT.md
@.planning/phases/01-verified-against-the-game/01-RESEARCH.md
@.planning/phases/01-verified-against-the-game/01-PATTERNS.md
@src/components/common/HeroSelect.test.tsx
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase produces

New symbols and files created by phase 01 (none of these exist in the tree before this phase; do not flag them as drift):

| Artifact | Kind | Introduced by |
|---|---|---|
| `src/components/foundry/ChangePools.test.tsx` | new file | 01-01 |
| `src/components/foundry/AssetSourcesPanel.test.tsx` | new file | 01-01 |
| `jsdom` at `24.1.3` in `devDependencies` | new dependency | 01-01 |
| `src/components/foundry/AlternativesGallery.test.tsx` | new file | 01-02 |
| `src/components/foundry/MySoundChanges.test.tsx` | new file | 01-02 |
| `src/components/foundry/SoundImportEditor.test.tsx` | new file | 01-03 |
| `src/components/foundry/PortraitEditor.test.tsx` | new file | 01-03 |
| `electron/main/services/chatWheel.roundtrip.test.ts` | new file | 01-04 |
| `engineDefault` field on every `HUD_CONVARS` / `ADVANCED_GAMEINFO_CONVARS` entry | new field | 01-05 |
| `ConfigKeyDefinition.engineDefault` (both declarations) | new field | 01-05 |
| `PerformanceConvarState.engineDefault` | new field | 01-05 |
| `settings.gameConvars.valueStateHint.engineDefault` i18n key | new key | 01-05 |
| `docs/ingame-verification-record.md` | new file | 01-06 |
| `scripts/check-verification-record.mjs` (`parseVerificationRecord`, `checkVerificationRecord`, `--strict` flag) | new file + exports + CLI flag | 01-06 |
| `scripts/check-verification-record.test.ts` | new file | 01-06 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the jsdom package before installing it</name>
  <what-built>Nothing yet. This gate runs before any install. The phase research ran the package legitimacy audit and returned a `SUS` verdict for `jsdom`, triggered by the `too-new` heuristic on its most recent release (`30.0.1`, published 8 days before the audit). The mitigation already baked into this plan is that the version being installed is `24.1.3`, not the release that triggered the flag: `24.1.3` is the copy already hoisted into `node_modules` and already passing under `src/components/common/HeroSelect.test.tsx`.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/jsdom
    2. Confirm the repository field points at `github.com/jsdom/jsdom`, the weekly download count is in the tens of millions, and the package is a long-lived project rather than a recent publish.
    3. Confirm version `24.1.3` is listed in the version history.
    4. Confirm no other package is being added by this phase.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the pinned install, or name the concern to stop the phase.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "a Foundry lane renders and responds" - pool cards only</name>
  <read_first>
    - `src/components/common/HeroSelect.test.tsx` (the only working jsdom render test in the tree; copy its pragma, `IS_REACT_ACT_ENVIRONMENT` assignment, `createRoot`/`act` lifecycle, and `beforeEach`/`afterEach` root handling)
    - `src/components/foundry/ChangePools.tsx` (whole file; the default export `FoundryPoolList`, the internal `PoolCard` with its mount-time `inspect()` effect at the `entriesKey` dependency, and the exported `PoolMemberCell`)
    - `src/components/foundry/poolView.ts` (the `FoundryPoolMember` / `FoundryPoolCard` / `FoundryPoolWinners` shapes the test must build by hand, and `resolvePoolWinners`)
    - `src/components/foundry/poolView.test.ts` (the existing inline-factory convention for building `Mod` and pool fixtures; reuse the same factory style rather than adding a fixtures directory)
    - `src/lib/api.ts` (the `foundryInspectAssetSources` wrapper, to see the exact `window.electronAPI.foundry` method name and return shape the stub must satisfy)
    - `src/i18n.ts` (initialises i18next synchronously with `initAsync: false`, so a side-effect import makes `useTranslation` resolve real English strings under jsdom)
    - `package.json` (current `devDependencies` block)
    - `vitest.config.ts` (confirms `src/**/*.test.tsx` is already in `include`, so no config edit is needed)
  </read_first>
  <files>package.json, pnpm-lock.yaml, src/components/foundry/ChangePools.test.tsx</files>
  <action>
Two moves, in this order.

First, add the dependency: run `pnpm add -D jsdom@24.1.3` (exact version, no caret). Per D-02 this replaces reliance on the hoisted transitive copy. Per the research pitfall, an unpinned install resolves the registry's current `30.0.1`, a major bump nobody has reviewed; the `package.json` entry must read the literal `24.1.3`, not a range. Do not add the native `canvas` package, and do not add any testing-library package: D-01 fixes the harness shape as raw `react-dom/client` + `act`.

Second, create `src/components/foundry/ChangePools.test.tsx` covering the pool-cards lane, threading every layer the harness needs:

- Environment: a first-line `// @vitest-environment jsdom` pragma per D-03. Do not touch `vitest.config.ts`.
- i18n: a side-effect import of `../../i18n` before the component import, so `useTranslation` inside `PoolCard` resolves against the real English catalog instead of returning keys. `ChangePools.tsx` calls `useTranslation` in three places (`FoundryPoolList`, `PoolCard`, `PoolMemberCell`).
- IPC: assign a plain object to `window.electronAPI` in `beforeEach` carrying only `foundry.inspectAssetSources`, a `vi.fn()` resolving a `FoundryAssetSourcesInspection` with populated `sources` and `winners` so `resolvePoolWinners` has something real to resolve. `PoolCard` calls this from a mount-time `useEffect`, not from a click, so an incomplete stub surfaces as an unhandled rejection rather than a clean failure. Do not module-mock `../../lib/api`; the project convention is manual objects, not module-level mocking of project code.
- Fixture: build the `FoundryPoolCard` and its `FoundryPoolMember` entries with local inline factory functions with overridable fields, matching `poolView.test.ts`. Give the pool at least two members contending for the same entry path so the card is a real pool rather than a `single` one.
- Interaction depth (D-04): render `FoundryPoolList`, then (a) assert the winner text that only appears once the inspection promise has resolved and `resolvePoolWinners` has run, and (b) click the pool-level shuffle button and assert both that `onToggleShuffleKey` was called once per key returned by `poolShuffleToggle` and that the button's `aria-pressed` reflects the pool's `allIncluded` state. Wrap renders and events in `act()`; await the resolved inspection inside `act()` so React flushes the state update.
- Lifecycle: `beforeEach` creates the host div and root, `afterEach` unmounts inside `act()`, removes the host, and calls `vi.restoreAllMocks()`.

If `ChangePools.tsx` reaches a `window.electronAPI` method beyond `foundry.inspectAssetSources`, add it to the stub rather than removing the assertion that reaches it.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/foundry/ChangePools.test.tsx</automated>
    <automated>node -e "const d=require('./package.json').devDependencies; if(d.jsdom!=='24.1.3'){console.error('jsdom pin is '+d.jsdom);process.exit(1)} console.log('jsdom pinned '+d.jsdom)"</automated>
    <automated>grep -q "@vitest-environment jsdom" src/components/foundry/ChangePools.test.tsx</automated>
    <automated>grep -q "aria-pressed" src/components/foundry/ChangePools.test.tsx</automated>
    <automated>git diff --exit-code vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies contains the literal `"jsdom": "24.1.3"` (exact, no caret or tilde)
    - `pnpm exec vitest run src/components/foundry/ChangePools.test.tsx` exits 0 with at least 2 passing tests
    - `src/components/foundry/ChangePools.test.tsx` line 1 is `// @vitest-environment jsdom`
    - `src/components/foundry/ChangePools.test.tsx` contains `IS_REACT_ACT_ENVIRONMENT`, `createRoot`, and `restoreAllMocks`
    - `src/components/foundry/ChangePools.test.tsx` imports `../../i18n` for its initialisation side effect
    - `src/components/foundry/ChangePools.test.tsx` asserts on `aria-pressed` after a click, and asserts on text produced only after the inspection promise resolved
    - `git diff --exit-code vitest.config.ts` exits 0 (the config is untouched, per D-03)
    - No testing-library package and no `canvas` package appear in `package.json`
  </acceptance_criteria>
  <done>The pool-cards lane renders under jsdom, its mount-time IPC call resolves through the stub into visible winner text, and its shuffle control responds to a real click. The dependency the whole harness rests on is pinned to the version that behaviour was proven against.</done>
</task>

<task type="auto">
  <name>Task 3: Audition-preview lane renders and auditions</name>
  <read_first>
    - `src/components/foundry/ChangePools.test.tsx` (the harness shape just proven in Task 2; this test follows it)
    - `src/components/foundry/AssetSourcesPanel.tsx` (whole file; the default export's props, the `inspect()` callback and its `onRefreshInspection` short-circuit, the `audition()` path around `foundryAuditionSourceClip`, the `TOKEN_SEPARATOR` play-state token, and the `useNavigate` and `useAppStore` calls)
    - `src/components/foundry/sourceGating.ts` and `src/components/foundry/assetSourceInspection.test.ts` (the `FoundryAssetSourcesInspection` fixture shape and the gating the panel renders from)
    - `src/lib/api.ts` (`foundryInspectAssetSources`, `foundryAuditionSourceClip` wrapper signatures)
    - `src/stores/appStore.ts` (only the `toggleMod` action the panel subscribes to, to decide whether the real store is safe to leave in place)
  </read_first>
  <files>src/components/foundry/AssetSourcesPanel.test.tsx</files>
  <action>
Create `src/components/foundry/AssetSourcesPanel.test.tsx` for the audition-preview lane, following the harness shape proven in Task 2 (jsdom pragma, `../../i18n` side-effect import, `IS_REACT_ACT_ENVIRONMENT`, `createRoot`/`act`, root lifecycle).

Three dependencies this lane has that the pool-cards lane did not:

- Router: `AssetSourcesPanel` calls `useNavigate` from `react-router-dom`, which throws outside a router. Wrap the render in a `MemoryRouter`.
- Store: it subscribes to `useAppStore((state) => state.toggleMod)`. Leave the real Zustand store in place and assert only on what the panel renders and on the audition stub; do not assert on mod-store side effects here.
- Media: `audition()` assigns to an `HTMLAudioElement` and awaits `play()`. Under jsdom `play()` is a no-op that returns `undefined` rather than a rejected promise, and `onended` never fires by itself. Assert the state transitions the component drives synchronously (the button's disabled/busy state during the pending `foundryAuditionSourceClip` call, and the play/stop label swap once it resolves). If the test needs the ended transition, invoke the element's `onended` handler explicitly rather than waiting for jsdom to fire it.

Stub `window.electronAPI.foundry` with `inspectAssetSources` and `auditionSourceClip` only. `auditionSourceClip` resolves a fake URL string for the happy path; add a second case where it resolves `null` and assert the panel does not enter the playing state.

Interaction depth (D-04): render the panel over a `paths` array with a populated `inspection` prop, assert the listed sources and the resolved winner text, then click the audition control and assert `auditionSourceClip` was called with the exact `(modId, entryPath)` pair the clicked row represents. That call-argument assertion is the point: it is what proves the panel auditions the source the user clicked rather than an arbitrary one, which is the parity claim the in-game sweep row IG-07 will later check against the engine.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/foundry/AssetSourcesPanel.test.tsx</automated>
    <automated>grep -q "MemoryRouter" src/components/foundry/AssetSourcesPanel.test.tsx</automated>
    <automated>grep -q "auditionSourceClip" src/components/foundry/AssetSourcesPanel.test.tsx</automated>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/components/foundry/AssetSourcesPanel.test.tsx` exits 0 with at least 2 passing tests
    - The test renders `AssetSourcesPanel` inside a `MemoryRouter`
    - The test asserts `auditionSourceClip` was called with the exact mod id and entry path of the clicked row
    - The test covers the `auditionSourceClip` resolves-null case and asserts the panel does not enter the playing state
    - `pnpm exec tsc -b` exits 0
    - `pnpm exec vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>The audition-preview lane renders, lists its inspected sources, and auditions the exact clicked source, with both the resolved and the null-clip outcomes covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> developer machine | A new devDependency is resolved and its install scripts run locally |
| test process -> `window.electronAPI` stub | The test stands in for the preload `contextBridge`; nothing crosses to the real main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `pnpm add -D jsdom` | high | mitigate | Package legitimacy audit returned `SUS` (`too-new` on release `30.0.1`). Task 1 is a blocking human checkpoint against npmjs.com before any install, and Task 2 pins the exact already-resolved `24.1.3` rather than the flagged release. Never auto-approvable. |
| T-01-01 | Tampering | `package.json` / `pnpm-lock.yaml` | medium | mitigate | Exact pin (no range) asserted by an automated check, so a later `pnpm install` cannot silently resolve a different major. |
| T-01-02 | Information disclosure | `window.electronAPI` test stub | low | accept | The stub is a plain object created inside the test process. It reaches no real IPC channel, holds no credentials, and is never bundled: `*.test.tsx` is outside the electron-vite renderer entry. |
| T-01-03 | Elevation of privilege | jsdom test environment | low | accept | jsdom is a devDependency only. It is never loaded by the packaged app, and the renderer keeps `contextIsolation` on and `nodeIntegration` off unchanged by this plan. |
</threat_model>

<verification>
- `pnpm exec vitest run` (full suite) green, including the pre-existing `src/components/common/HeroSelect.test.tsx`
- `pnpm exec tsc -b` green
- `pnpm lint` green
- `pnpm encoding:check` green
- `git diff --exit-code vitest.config.ts` exits 0
- `package.json` shows exactly one new devDependency line
</verification>

<success_criteria>
- `jsdom` is an explicit, exactly-pinned devDependency at `24.1.3`
- Two of the six Foundry lanes named by `REQ-renderer-test-harness` have interaction-depth render tests that pass
- The harness shape (pragma, i18n init, electronAPI stub, act lifecycle) is proven and reusable by plans 01-02 and 01-03 without further architectural decisions
</success_criteria>

<flagged_assumptions>
- RESEARCH.md assumption A3: `AssetSourcesPanel` subscribes to the real `appStore`. This plan leaves the real store in place and asserts only on rendered output and stub calls. If the store turns out to require isolation (for example it performs IPC on subscribe), surface it rather than deleting the assertion.
- RESEARCH.md did not surface that all six lanes call `useTranslation`, nor that `AssetSourcesPanel` calls `useNavigate`. Both are handled explicitly above (side-effect `../../i18n` import, `MemoryRouter`). Expect the same two needs in plans 01-02 and 01-03.
</flagged_assumptions>

<output>
Create `.planning/phases/01-verified-against-the-game/01-01-SUMMARY.md` when done
</output>
