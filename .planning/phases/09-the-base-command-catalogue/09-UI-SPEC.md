---
phase: 9
slug: the-base-command-catalogue
status: approved
reviewed_at: 2026-08-11
shadcn_initialized: false
preset: none
created: 2026-08-11
---

# Phase 9: UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (plain Tailwind 4, no shadcn CLI, no components.json in the repo) |
| Preset | not applicable |
| Component library | none (hand-rolled primitives in `src/components/common/`; no Radix, base-ui, or shadcn) |
| Icon library | lucide-react (already used by `ChatWheel.tsx` and `ui.tsx`) |
| Font | Radiance (app default, `--font-radiance`); ui-monospace stack for raw YAML key text; Reaver untouched for existing display headings |

**Sources:** no `components.json` found (glob); tokens and fonts from `src/index.css` `@theme` (lines 83-131); icon usage from `ChatWheel.tsx:3` and `ui.tsx:2`; house rules from `docs/ui-conventions.md`.

---

## Spacing Scale

Declared values (multiples of 4 on the house 8-point scale; every non-multiple-of-4 value used in this phase is declared as an exception below):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps inside three-state options, Tag inner padding |
| sm | 8px | Gap between controls in a row, chip gap, summary margin |
| md | 16px | Section inner padding (`p-3`/`p-4`), gap between toolbar and list |
| lg | 24px | Gap between the catalogue section and its neighbours on the page |
| xl | 32px | Not used in this phase (page-level layout gaps stay on the page) |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions (closed set, all existing conventions, no new values): `gap-1.5`/`py-1.5` (6px) for dense control rows, matching the current ChatWheel form rows (`ChatWheel.tsx:331-341`); `text-[11px]` for the search summary line, per the `SearchInput`/`ResultSummary` precedent (`SearchInput.tsx:94`); `px-2.5` (10px) for the category chips and three-state options, matching the existing SegmentedControl chip padding (`ui.tsx:562`); and `mt-0.5` (2px) for the broken-caveat icon optical alignment, matching the existing warning icon offset (`ChatWheel.tsx:218`).

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (`text-sm`) | 400 | 1.5 | Command display names, help text, legend |
| Label | 12px (`text-xs`) | 600 | 1.4 | Column headers, chips, section summary, status Tags |
| Heading | 14px (`text-sm`) | 600 | 1.3 | Catalogue section heading inside `<summary>`, "Other commands" heading (matches the "Menus and commands" h2 at `ChatWheel.tsx:320`) |
| Display | 24px (`text-2xl`) | 700 | 1.2 | Page h1, pre-existing, unchanged (`ChatWheel.tsx:204`) |

Declared sizes: 12, 14, 24 (text-xl/20px exists elsewhere in the app but is not used in this phase). Declared weights: 400 and 600 only; the page h1 at 700 is pre-existing and untouched.

Command display names render in Radiance body text, never in a display or mono face: they are the game's voice-command strings, not data identifiers the user types.

---

## Color

Tokens come from `src/index.css` `@theme` (lines 83-114). No raw hex or raw Tailwind palette colors in new `className` values.

| Role | Token / Value | Usage |
|------|---------------|-------|
| Dominant (60%) | `bg-bg-primary` #0f0f0f | App background, the catalogue section surface (`bg-bg-primary` card, matching the Advanced YAML details at `ChatWheel.tsx:381`) |
| Secondary (30%) | `bg-bg-secondary` #1a1a1a, `bg-bg-tertiary` #242424 | Cards and panels (`bg-bg-secondary`); inputs, inactive chips, raised surfaces (`bg-bg-tertiary`) |
| Accent (10%) | `accent` #f97316 | Active filter chip, active three-state option, focus-visible rings, search focus border. See reserved list below. |
| Destructive | `state-danger` #f87171 | No destructive actions in this phase; token reserved for the existing page error banner only |

Accent reserved for: the active (pressed) category chip, the active (pressed) three-state option, `focus-visible:ring-accent` rings, and the SearchInput focus border. Not for: command names, row text, status Tags, or section chrome. Status Tags use the state tokens: `state-success` (bindable by default), `state-info` (hidden), `state-warning` (broken / game-state dependent), `text-text-secondary` (neutral).

---

## Copywriting Contract

Every visible string is an i18n key in the `chatWheel.*` namespace, with an English fallback passed to `t()`/`Tx` as the existing page does. No em-dashes anywhere. Raw command display strings are data (D-02) and never pass through `t()`.

| Element | Copy |
|---------|------|
| Primary CTA | None new. Saving flows through the existing `chatWheel.saveAndInstall` ("Save & install") button; the catalogue adds no save control of its own |
| Section heading | `chatWheel.catalog.title` = "Base command catalogue" |
| Legend | `chatWheel.catalog.legend` = "Each command has a game default. Inherit keeps it, On forces it enabled in the saved YAML, Off forces it disabled." |
| Empty state heading | `chatWheel.catalog.emptyTitle` = "No commands match your search" |
| Empty state body | `chatWheel.catalog.emptyBody` = "Try a different search or category. Toggles change what ChatLane offers, not what the game guarantees." |
| Empty state action | `chatWheel.catalog.emptyAction` = "Clear search and filters" |
| Error state | None new. Invalid YAML continues to surface on the existing validation status line (`ChatWheel.tsx:388-394`); catalogue toggles write only boolean values, so they cannot produce invalid YAML |
| Destructive confirmation | None. No destructive actions in this phase; toggling a command off is fully reversible through the same control |

The broken-category caveat is honest about capability, not outcome (D-03):

`chatWheel.catalog.brokenCaveat` = "Broken commands are not bindable by default and only matter in the game states that use them, such as post-game all-chat. Toggling them here changes what ChatLane offers, not what the game guarantees."

---

## UI Considerations

Applicable state considerations resolved: 6 covered, 1 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | catalogue list after search/filter | ✅ covered | A search or category filter with no matches renders the documented empty state with a visible "Clear search and filters" action |
| zero-one-many | "Other commands in this file" group | ✅ covered | The group renders only when at least one YAML override key is absent from the catalogue; zero unknown keys renders nothing |
| loading | catalogue section | ✅ covered | The catalogue rows are static typed data with no async load; while `starterLoading`, four `Skeleton` rows replace the list and all controls stay disabled until the parsed YAML model is available |
| long-text | command display names | 🧪 backstop | Command names truncate with an ellipsis and a `title` tooltip carrying the full string; lifts as `{ statement: 'Long command names such as "Good Game (Post Game) - All Chat" truncate without wrapping or horizontal overflow', verification: backstop }` |
| overflow | responsive row layout | ✅ covered | The row grid collapses to a single column below the `sm` breakpoint; no horizontal scrolling is introduced |
| error | catalogue toggle surface | ✅ covered | No new error surface; invalid YAML continues to surface on the existing validation status line, and toggles write only boolean values |

---

## Component Inventory

| Component | Decision | Notes |
|-----------|----------|-------|
| `BaseCommandCatalog.tsx` (new, `src/components/chatwheel/`) | build | The collapsible section body: search, filter chips, legend, rows, broken caveat, "Other commands" group |
| `TriStateControl.tsx` (new, `src/components/chatwheel/`) | build | The inherit/on/off control, one per map per row. New UI: no three-state primitive exists |
| `SearchInput` (`src/components/common/SearchInput.tsx`) | reuse | Search field with visible scope, live result summary, labelled clear, Escape to clear |
| `Tag` (`src/components/common/ui.tsx`) | reuse | Game-default status chip per row |
| `Button` (`src/components/common/ui.tsx`) | reuse | Empty-state clear action, size sm variant secondary |
| `Skeleton` (`src/components/common/Skeleton.tsx`) | reuse | Loading placeholder rows |
| `EmptyState` (`src/components/common/PageComponents.tsx`) | do not use | Page-scale (`h-full`, 64px icon, `text-xl` title); the section builds an inline empty-state box instead (Loading and Empty States below). The page's existing import serves its own top-level empty state, not this section |
| `Tx` / `useTranslation` | reuse | All visible strings |
| `SegmentedControl` + `useSegmentedTabs` | do not use | `role="tab"` promises a panel (`ui.tsx:487-493`); category filters and per-row selectors are not tab switches. RESEARCH risk 8 |
| `Toggle` | do not use | Two-state switch only; cannot represent inherit (D-04) |
| `CheckboxMark` | do not use | Its indeterminate glyph is a static visual, not an interactive third state |

---

## Section Layout

**Placement:** inside the page's main `<section>` (the `space-y-5 rounded border border-border bg-bg-secondary p-4` section, `ChatWheel.tsx:297`), as the next sibling after the `xl:grid-cols-[minmax(0,1fr)_18rem]` grid (line 306) and before the `<datalist>` + Advanced YAML details (lines 377-387). It renders beneath "Menus and commands" (D-06) and above Advanced YAML, at full section width.

**Wrapper:** native `<details>` matching the Advanced YAML precedent (`ChatWheel.tsx:381-387`):

```
<details open={catalogOpen} onToggle={...} className="rounded border border-border bg-bg-primary p-3">
  <summary className="cursor-pointer text-sm font-semibold text-text-primary">
    <Tx k="chatWheel.catalog.title" fallback="Base command catalogue" />
  </summary>
```

`catalogOpen` is page-local React state, default `true` for new drafts, updated in `onToggle`, and retained for the edit session (D-06). It is not persisted to disk; the Advanced YAML details stays uncontrolled as it is today.

**Body (`mt-3 space-y-3`):**

**Visual anchor:** the search toolbar is the section's primary entry point. It renders first in the body, is the first focusable control once the section opens, and everything else (legend, columns, rows, caveat) is subordinate to it.

1. **Toolbar row:** `flex flex-wrap items-start gap-3`. `SearchInput` (`flex-1 min-w-[200px]`) on the left, category filter chips in a `flex flex-wrap gap-1.5` on the right.
2. **Legend line:** `<p className="text-xs text-text-secondary">` with `chatWheel.catalog.legend`.
3. **Column header row** (hidden below `sm`): `hidden sm:grid grid-cols-[minmax(0,1fr)_9rem_11rem_11rem] gap-2 px-1 pb-1 text-xs font-semibold text-text-secondary`, four labels: Command / Game default / Chat Wheel / Ping wheel (`chatWheel.catalog.columnCommand`, `columnDefault`, `columnChatWheel`, `columnPingWheel`).
4. **Row list.**
5. **Broken caveat** footnote when the Broken category is in the current view (All or Broken filter active) and at least one broken row survives the current search.
6. **"Other commands in this file" group.**

**Category filter chips:** four plain buttons, not a tablist:

```
<button type="button" aria-pressed={active} className={chipClasses}>
```

Chip classes match the SegmentedControl active/inactive language without tab semantics: `inline-flex items-center gap-1 rounded-md border px-2.5 py-1 text-xs font-medium transition-colors cursor-pointer focus:outline-none focus-visible:ring-2 focus-visible:ring-accent`, active `border-accent/70 bg-accent/15 text-text-primary`, inactive `border-border bg-bg-tertiary text-text-secondary hover:border-accent/40 hover:text-text-primary`. The group carries `role="group"` with `aria-label={t('chatWheel.catalog.filterLabel', 'Filter by category')}`.

Chips: All, Default, Hidden, Broken (`chatWheel.catalog.filterAll`/`filterDefault`/`filterHidden`/`filterBroken`), each showing its static catalogue total: All 53, Default 33, Hidden 12, Broken 8. Counts never change with search; the live narrowed count is the SearchInput summary. Single-select behaviour: picking one category hides the others; All shows everything.

**Search contract:** `SearchInput` with `placeholder` = "Search commands", `scope` = "Search all {{count}} commands by name." (`chatWheel.catalog.searchScope`), `summary` while narrowed = "Showing {{count}} of {{total}} commands." (`chatWheel.catalog.searchSummary`), `clearLabel` = "Clear search". Matching is case-insensitive substring against the raw display string (the YAML key, D-01). Escape clears while focused (built in).

**Row anatomy** (`role="list"` with `aria-label`; each row `role="listitem"`):

```
<div className="grid items-center gap-2 border-b border-border/50 py-2 sm:grid-cols-[minmax(0,1fr)_9rem_11rem_11rem]">
```

- Cell 1: command display name, raw English (D-02), `<span className="truncate text-sm text-text-primary" title={id}>`, never translated.
- Cell 2: game-default `Tag`. Tone and label by category + Chat Wheel default:
  - `default` and bindable: tone `success`, "Bindable by default"
  - `default` and not bindable: tone `neutral`, "Not bindable by default"
  - `hidden`: tone `info`, "Hidden by default" (title: `chatWheel.catalog.statusHiddenTooltip` = "Available in game but not bindable on the stock Chat Wheel; ChatLane unlocks it.")
  - `broken`: tone `warning`, "Game-state dependent" (title: `chatWheel.catalog.statusBrokenTooltip` = "Only usable in the game states that use it, such as post-game all-chat.")
- Cell 2 addendum (ping default honesty): when `pingWheelBindable !== bindable`, render a second small Tag after the first, tone `neutral`, from `chatWheel.catalog.statusPingBindable` ("On ping wheel by default") or `chatWheel.catalog.statusPingNotBindable` ("Not on ping wheel by default") as the value dictates. Without it the Ping wheel Inherit state has no visible default for the 15 commands where the two flags diverge (the 3 isMenu default commands, bindable true / ping false, plus all 12 hidden commands, bindable false / ping true; no broken entry diverges, Missing is true/true and the rest false/false).
- Cell 3: `TriStateControl` for the Chat Wheel / bind map, `aria-label` = `t('chatWheel.catalog.ariaChatWheel', '{{command}}: Chat Wheel', { command: id })`. Writes `override_bindable`.
- Cell 4: `TriStateControl` for the Ping wheel map, `aria-label` = `t('chatWheel.catalog.ariaPingWheel', '{{command}}: Ping wheel', { command: id })`. Writes `override_ping_wheel_bindable`.

The whole row is not a tab stop; each control is independently focusable.

---

## Three-State Control Contract (new UI)

`TriStateControl` props: `value: 'inherit' | 'on' | 'off'`, `onChange(state)`, `ariaLabel`, `disabled?`. (A `showInherit?` prop was considered and dropped: the unknown-key group uses its own two-state button pair, so no caller ever passes false.)

- Render `<div role="group" aria-label={ariaLabel} className="inline-flex gap-1">` with three buttons `type="button" aria-pressed={active}`, styled with the same chip classes as the filter chips (active/inactive above), plus `disabled:cursor-default disabled:opacity-50`.
- Button icons (lucide, `h-3.5 w-3.5`): inherit = `Minus`, on = `Check`, off = `X`. Button text from `chatWheel.catalog.stateInherit` ("Inherit"), `stateOn` ("On"), `stateOff` ("Off").
- State derivation (D-04): the control value is `inherit` when the YAML map has no key for this command; `on` when the map has `true`; `off` when the map has `false`. What inherit means for this specific command (the catalogue default) is shown by the game-default Tag beside it and explained by the legend.
- Explicit `false` is a legal, representable state (D-04): `config.gd` validates bool values, and ChatLane's loader accepts `false`.
- Do not use `role="tab"` or `role="tablist"`: `role="tab"` promises a panel and this is a per-row selector, not a tab switch (`ui.tsx:487-493`). Do not use `Toggle` (binary) or `CheckboxMark` (static indeterminate glyph only).
- Controlled component: the page owns `model`, and the controls derive from it. Toggling calls `onToggle(commandId, map, state)`; the page routes it through `applyModel` -> `updateChatWheelYaml`, so Advanced YAML stays the single source of truth and manual edits flow back through `parseChatWheelYaml` (D-06).

---

## "Other commands in this file" Group

- Renders only when at least one YAML override key is absent from the catalogue (zero unknown keys renders nothing).
- Heading: `<h3 className="text-sm font-semibold text-text-primary">` `chatWheel.catalog.otherCommands` = "Other commands in this file", with help `chatWheel.catalog.otherCommandsHelp` = "These override keys are not in the base catalogue. They are kept byte-for-byte and stay editable."
- Row: raw key in `font-mono text-xs text-text-primary` (mono signals it is a raw YAML key) plus two boolean on/off toggles, one per map, each a two-option `aria-pressed` button pair.
- A key present in a map shows its current boolean (`true`/`false`). A key absent from a map shows a muted "Not set" state (`chatWheel.catalog.otherNotSet`); toggling it on writes `true`.
- Unknown commands have no catalogue default, so they get no inherit option; the three-state control is not used here (D-06: editable booleans).
- Toggling never deletes a key: switching off writes explicit `false`. Deleting an entry entirely is only possible in Advanced YAML, which honors D-05 byte preservation.

---

## Broken Category Caveat

When the Broken category is in the current view (All or Broken filter active) and at least one broken row survives the current search, render beneath the list:

```
<div className="flex items-start gap-2 rounded border border-state-warning/20 bg-state-warning/5 px-3 py-2 text-xs text-text-secondary">
  <AlertTriangle className="mt-0.5 h-4 w-4 flex-shrink-0 text-state-warning" />
  {t('chatWheel.catalog.brokenCaveat')}
</div>
```

Copy is capability-honest (D-03): it states what ChatLane can offer and the game states involved, and explicitly does not guarantee an in-match outcome.

---

## Loading and Empty States

- **Loading:** while `starterLoading`, render four `Skeleton` rows in place of the list and the "Other commands" group, and pass `disabled` to every control. The catalogue rows themselves are static typed data and need no load state.
- **Empty (search or filter yields no rows):** an EmptyState-style box built inline: `SearchX` icon (`h-8 w-8 text-text-secondary`), `text-sm font-semibold` title, `text-xs text-text-secondary` body, and a `Button` (variant secondary, size sm) labelled "Clear search and filters" that resets both the query and the category filter. Do NOT reuse the shared `EmptyState` component here: it is page-scale (`h-full`, 64px icon, `text-xl` title, `PageComponents.tsx:119-121`) and `text-xl` is outside this phase's declared type scale. The empty action is the next useful step (the `SearchInput` contract requires the zero-result state to offer one, `SearchInput.tsx:21-23`).
- **Empty "Other commands" group:** render nothing (see zero-one-many row above).
- **Converter unavailable:** editing the catalogue stays enabled. Toggling writes YAML only and needs no converter; only Save & install is gated by `converterAvailable` as today.

---

## Keyboard Accessibility Floor (D-06)

- Every interactive element is a native control: `<summary>`, the search `<input>`, plain `<button>` chips and three-state options. No custom key handling beyond the roving-tabindex arrow movement inside a `TriStateControl` group (below); no key handler replaces a native activation. No drag-only interaction. This floor is what Phase 10 drag-and-drop must preserve.
- Every row control has an accessible name via `aria-label` combining the command id and the map name. Column headers are presentational text labels, not `th`; the list is `role="list"` with `role="listitem"` rows, so no grid/row semantics are introduced.
- Focus rings everywhere: `focus:outline-none focus-visible:ring-2 focus-visible:ring-accent`, with `ring-offset-2 ring-offset-bg-primary` on solid surfaces.
- Filter focus management: pressing a category chip keeps focus on the chip; no focus steal on result changes. The `SearchInput` live region announces "Showing X of Y". When a filter empties the list, the empty state's "Clear search and filters" button is the immediate next tab stop.
- The three-state options are a single tab stop per control group: roving `tabIndex` among the three buttons, arrow keys move focus only and never change the value. This deliberately differs from `SegmentedControl` (whose move() handler selects on arrow, ui.tsx around lines 550-561): these are `aria-pressed` toggles whose activation writes saved YAML, so a focus move must not mutate the document. Enter/Space activate natively.
- Rows are not focus targets; children are. `tabIndex` is never added to a row container.

---

## i18n Keys (new, all under `chatWheel.*`, English fallbacks)

| Key | Fallback |
|-----|----------|
| `chatWheel.catalog.title` | "Base command catalogue" |
| `chatWheel.catalog.legend` | "Each command has a game default. Inherit keeps it, On forces it enabled in the saved YAML, Off forces it disabled." |
| `chatWheel.catalog.searchPlaceholder` | "Search commands" |
| `chatWheel.catalog.searchScope` | "Search all {{count}} commands by name." |
| `chatWheel.catalog.searchSummary` | "Showing {{count}} of {{total}} commands" |
| `chatWheel.catalog.searchClear` | "Clear search" |
| `chatWheel.catalog.filterLabel` | "Filter by category" |
| `chatWheel.catalog.filterAll` | "All" |
| `chatWheel.catalog.filterDefault` | "Default" |
| `chatWheel.catalog.filterHidden` | "Hidden" |
| `chatWheel.catalog.filterBroken` | "Broken" |
| `chatWheel.catalog.brokenCaveat` | "Broken commands are not bindable by default and only matter in the game states that use them, such as post-game all-chat. Toggling them here changes what ChatLane offers, not what the game guarantees." |
| `chatWheel.catalog.statusBindable` | "Bindable by default" |
| `chatWheel.catalog.statusNotBindable` | "Not bindable by default" |
| `chatWheel.catalog.statusPingBindable` | "On ping wheel by default" |
| `chatWheel.catalog.statusPingNotBindable` | "Not on ping wheel by default" |
| `chatWheel.catalog.statusHidden` | "Hidden by default" |
| `chatWheel.catalog.statusHiddenTooltip` | "Available in game but not bindable on the stock Chat Wheel; ChatLane unlocks it." |
| `chatWheel.catalog.statusBroken` | "Game-state dependent" |
| `chatWheel.catalog.statusBrokenTooltip` | "Only usable in the game states that use it, such as post-game all-chat." |
| `chatWheel.catalog.stateInherit` | "Inherit" |
| `chatWheel.catalog.stateOn` | "On" |
| `chatWheel.catalog.stateOff` | "Off" |
| `chatWheel.catalog.columnCommand` | "Command" |
| `chatWheel.catalog.columnDefault` | "Game default" |
| `chatWheel.catalog.columnChatWheel` | "Chat Wheel" |
| `chatWheel.catalog.columnPingWheel` | "Ping wheel" |
| `chatWheel.catalog.listLabel` | "Base commands" |
| `chatWheel.catalog.emptyTitle` | "No commands match your search" |
| `chatWheel.catalog.emptyBody` | "Try a different search or category. Toggles change what ChatLane offers, not what the game guarantees." |
| `chatWheel.catalog.emptyAction` | "Clear search and filters" |
| `chatWheel.catalog.otherCommands` | "Other commands in this file" |
| `chatWheel.catalog.otherCommandsHelp` | "These override keys are not in the base catalogue. They are kept byte-for-byte and stay editable." |
| `chatWheel.catalog.otherNotSet` | "Not set" |
| `chatWheel.catalog.ariaChatWheel` | "{{command}}: Chat Wheel" |
| `chatWheel.catalog.ariaPingWheel` | "{{command}}: Ping wheel" |

Reused existing keys: `chatWheel.saveAndInstall`. The unknown-key group's On/Off pairs reuse `ariaChatWheel`/`ariaPingWheel` with the raw key as `{{command}}`; no dedicated keys exist for them. `statusNotBindable` is unreachable with the pinned commit's data (every `default` entry is bindable) and exists so a future `VC_LIST` change degrades honestly; `check-i18n.mjs` lists unused keys as a warning only. Raw catalogue display strings are data (D-02) and never enter `translation.json`; `pnpm i18n:check` and `pnpm i18n:manifest` are run after any catalogue change.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required (shadcn not initialized) |
| third-party registries | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
