---
phase: 09-the-base-command-catalogue
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/chatwheel/TriStateControl.tsx
  - src/components/chatwheel/BaseCommandCatalog.tsx
  - src/components/chatwheel/BaseCommandCatalog.test.tsx
  - src/pages/ChatWheel.tsx
  - src/pages/ChatWheel.test.tsx
  - src/locales/en/translation.json
  - src/locales/manifest.json
  - docs/chat-wheel.md
autonomous: true
requirements:
  - REQ-cw-override-editing
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "A user can search the 53 catalogued commands case-insensitively, filter by All/Default/Hidden/Broken chips carrying static totals 53/33/12/8, and see each command's game-default status as a toned Tag, all without touching YAML."
    - "Every command whose ping-wheel default differs from its Chat Wheel default carries a second neutral Tag naming the ping default, so the Ping wheel control's Inherit state always has a visible meaning (D-04)."
    - "Each catalogue row carries two three-state controls (Chat Wheel, Ping wheel); their value derives from the parsed YAML maps via overrideStateFor, and changing one routes through applyOverride and updateChatWheelYaml so Advanced YAML instantly reflects it."
    - "YAML override keys absent from the catalogue render in an 'Other commands in this file' group with the raw key in mono text and editable two-state booleans plus a 'Not set' state; the group renders nothing when there are no unknown keys."
    - "A search or filter with no matches renders the empty state with a working 'Clear search and filters' action; while starterLoading, four Skeleton rows replace the list and all controls are disabled."
    - "Manual Advanced YAML edits flow back into the controls through parseChatWheelYaml, and every visible string added is a chatWheel.catalog.* i18n key with an English fallback; raw command ids never pass through t()."
    - "Category filter chips and three-state options are plain aria-pressed buttons, never role=tab; every interactive element is a native control reachable by keyboard."
  artifacts:
    - src/components/chatwheel/BaseCommandCatalog.test.tsx
  key_links:
    - "TriStateControl and the chips reuse the SegmentedControl visual language but not its tablist semantics (09-RESEARCH risk 8, UI-SPEC Component Inventory 'do not use' rows)."
    - "The page never stores override state outside the model: the section is a pure projection of parseChatWheelYaml(yaml), which is what keeps Advanced YAML the single source of truth (D-06)."
  prohibitions:
    - statement: "No role=tab or role=tablist anywhere in the new components; a filter is not a tab switch (ui.tsx's own warning, RESEARCH risk 8)."
      source: 09-UI-SPEC.md
      verification: tested
    - statement: "Raw command display strings are data (D-02) and must not enter translation.json or pass through t()."
      source: 09-CONTEXT.md
      verification: tested
    - statement: "No em-dashes in any UI string, comment, or doc line. Verify with a grep for U+2014 across src/components/chatwheel/ and the translation.json diff returning nothing (check-encoding.mjs hunts cp1252 mojibake, not literal em-dashes, so it is not the gate for this)."
      source: CLAUDE.md
      verification: tested
---

<objective>
Give the catalogue its form surface: a collapsible "Base command catalogue" section on the Chat Wheel page with search, category filters, per-row game-default status, two three-state override controls per row, and an honest group for unknown YAML entries.

Purpose: this is the user-facing half of REQ-cw-override-editing. Everything it renders is a projection of the plan 09-01 model; everything it writes goes back through the byte-preserving update path.

Output: `TriStateControl.tsx`, `BaseCommandCatalog.tsx`, their tests, the ChatWheel.tsx integration, and the `chatWheel.catalog.*` i18n keys with a regenerated locale manifest.
</objective>

<execution_context>
Run binaries directly from `./node_modules/.bin/`, never through `pnpm run` (local pnpm is v11 while CI pins v10 via pnpm/action-setup in `.github/workflows/ci.yml`; pnpm 11 changes pre-command behavior against this lockfile): `./node_modules/.bin/vitest run <files>`, `./node_modules/.bin/tsc -b`, `./node_modules/.bin/eslint .`, `node scripts/check-i18n.mjs`, `node scripts/gen-locale-manifest.mjs` (regenerate; `--check` to verify), `node scripts/check-encoding.mjs`.
</execution_context>

<context>
@.planning/phases/09-the-base-command-catalogue/09-UI-SPEC.md (the binding visual/interaction contract: layout, classes, copy table, a11y floor)
@.planning/phases/09-the-base-command-catalogue/09-CONTEXT.md (D-01 through D-07)
@src/pages/ChatWheel.tsx (the host page; section placement after the xl:grid closing div, before the datalist)
@src/lib/chatWheelModel.ts (as extended by plan 09-01: overrideStateFor, applyOverride, OverrideState)
@src/lib/chatWheelCommands.ts (as created by plan 09-01)
@src/components/common/SearchInput.tsx, src/components/common/ui.tsx (Tag, Button), src/components/common/Skeleton.tsx, src/components/common/PageComponents.tsx (EmptyState)
@src/pages/ChatWheel.test.tsx (the jsdom page-test harness pattern)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TriStateControl and BaseCommandCatalog components</name>

  <files>
    src/components/chatwheel/TriStateControl.tsx,
    src/components/chatwheel/BaseCommandCatalog.tsx,
    src/components/chatwheel/BaseCommandCatalog.test.tsx,
    src/locales/en/translation.json,
    src/locales/manifest.json
  </files>

  <read_first>
    .planning/phases/09-the-base-command-catalogue/09-UI-SPEC.md (Section Layout, Three-State Control Contract, Other commands group, Broken Category Caveat, Loading and Empty States, Keyboard Accessibility Floor, and the full i18n key table),
    src/components/common/SearchInput.tsx (props contract: value, onChange, placeholder, scope, clearLabel, label, summary),
    src/components/common/ui.tsx (Tag tones and Button variants; note the SegmentedControl warning comment about role=tab),
    src/components/common/Skeleton.tsx and src/components/common/PageComponents.tsx (read EmptyState only to see why it is NOT reused here: h-full root, 64px icon, text-xl title, all page-scale),
    src/components/chatwheel/RadialWheelPreview.tsx (house style for chatwheel components),
    src/pages/ChatWheel.test.tsx (jsdom + createRoot + act + flush pattern for component tests)
  </read_first>

  <behavior>
    - TriStateControl renders a role=group of three aria-pressed buttons (Inherit/On/Off with Minus/Check/X lucide icons), exactly one pressed, styled with the chip classes from the UI spec; the group is one tab stop with roving tabIndex and ArrowLeft/ArrowRight moving focus among the three; disabled disables all three.
    - BaseCommandCatalog renders one row per catalogue command: truncated name with title tooltip, a game-default Tag (success 'Bindable by default' for bindable default entries, neutral 'Not bindable by default' for non-bindable default entries, info 'Hidden by default' for hidden, warning 'Game-state dependent' for broken, with the UI-spec tooltips on hidden and broken), and two TriStateControls whose aria-labels interpolate the command id. The 'Not bindable by default' branch is unreachable with the pinned commit's data (every default entry is bindable) and exists so a future VC_LIST change degrades honestly rather than mislabelling.
    - Ping default honesty (UI-spec Cell 2 addendum): when a command's pingWheelBindable differs from bindable, a second neutral Tag ('On ping wheel by default' / 'Not on ping wheel by default') renders after the first, so the Ping wheel control's Inherit state always has a visible default. This fires for exactly 15 commands: the 3 isMenu default commands (bindable true, ping false) plus all 12 hidden commands (bindable false, ping true). No broken entry diverges (Missing is true/true, the rest false/false).
    - Search narrows rows case-insensitively by substring on the raw id; the SearchInput summary announces 'Showing X of Y commands' (no terminal period, matching the house ResultSummary shape).
    - The four chips filter by category, show static totals (All 53, Default 33, Hidden 12, Broken 8) from CHAT_WHEEL_COMMAND_COUNTS, are single-select, and are aria-pressed buttons inside a role=group labelled 'Filter by category'; no role=tab appears anywhere in the rendered output.
    - The broken caveat box renders when the active filter is All or Broken and at least one broken row is visible, and not otherwise.
    - Unknown keys from either override map render in the 'Other commands in this file' group: mono raw key, one two-state control per map showing true/false/'Not set'; setting a value writes an explicit boolean and never deletes a key. Zero unknown keys renders no group at all.
    - With no matching rows, an inline empty-state box (SearchX icon, text-sm title, text-xs body per the UI spec; NOT the shared page-scale EmptyState component, whose h-full and text-xl title break the section's type scale) renders with a 'Clear search and filters' Button that resets query and category.
    - With loading true, four Skeleton rows render instead of the list and every control is disabled.
  </behavior>

  <action>
Create `src/components/chatwheel/TriStateControl.tsx` exactly per the UI spec's Three-State Control Contract: props `{ value: OverrideState; onChange: (state: OverrideState) => void; ariaLabel: string; disabled?: boolean }` (the spec no longer declares showInherit; the unknown-keys group uses its own two-state buttons instead). Implement roving tabIndex inside the group: the pressed option carries tabIndex 0, the others -1, ArrowLeft/ArrowRight (and ArrowUp/ArrowDown) move DOM focus between siblings without changing the value; Enter/Space activate natively as buttons. Focus-only arrows are a deliberate departure from SegmentedControl (which selects on arrow): these are aria-pressed toggles whose activation writes saved YAML, so a focus move must not mutate the document. Reuse the exact chip class strings from the UI spec for active/inactive states.

Create `src/components/chatwheel/BaseCommandCatalog.tsx` with props `{ model: ChatWheelModel; loading: boolean; disabled: boolean; onSetOverride: (id: string, map: 'bindable' | 'pingWheel', state: OverrideState) => void }`. `loading` and `disabled` are separate on purpose: skeletons replace the list only while the starter loads (`loading={starterLoading}`), while `disabled` also covers `busy !== null`; deriving skeletons from `disabled` would blank the whole list during a save. Internal state: `query` and `category: 'all' | ChatWheelCommandCategory`. Compose per the UI spec's Section Layout: toolbar (SearchInput + chips), legend line, sm+ column header row, `role="list"` rows, conditional broken caveat, then the unknown group. Derive row control values with `overrideStateFor(model.overrideBindable, cmd.id)` and the ping variant. Compute unknown keys as the ordered union of keys from both maps that `findChatWheelCommand` does not know, preserving map insertion order (bindable first, then ping-only keys). Unknown group rows: raw key in `font-mono text-xs`, and per map a pair of aria-pressed On/Off buttons plus a muted 'Not set' text when the key is absent from that map; pressing On/Off calls `onSetOverride(key, map, 'on' | 'off')`. All copy through the i18n keys in the UI-spec table with English fallbacks; command ids and unknown keys render raw, never through t().

Add every `chatWheel.catalog.*` key from the UI-spec i18n table to `src/locales/en/translation.json` (keep the file's existing key style; nest under the existing `chatWheel` object). No em-dashes. Then regenerate the manifest: `node scripts/gen-locale-manifest.mjs`.

Create `src/components/chatwheel/BaseCommandCatalog.test.tsx` (`// @vitest-environment jsdom`, the ChatWheel.test.tsx render pattern: createRoot + act + a flush() macrotask helper). Start the file with `import '../../i18n';`, the pattern every component test uses (e.g. src/components/foundry/ChangePools.test.tsx line 3); do NOT mock react-i18next, no test in the repo does, and the real catalog resolves the chatWheel.catalog.* keys. There is no testing-library in this repo: drive the DOM with `element.dispatchEvent(new MouseEvent('click', { bubbles: true }))` inside `act()`. Cover: 53 rows render for an empty model; search narrows and the summary updates; each chip filters to its category count; tri-state derivation (a model with one true, one false, one absent key shows on/off/inherit); toggling calls onSetOverride with the right triple; the ping-default second Tag renders exactly for commands where pingWheelBindable differs from bindable; unknown-key group appears only when a map holds an unknown key and its 'Not set' state renders for the map missing it; empty state renders on a no-match search and its button clears both query and filter; loading renders skeletons and disables controls; a TriStateControl keyboard case (the pressed button has tabIndex 0, the other two -1; dispatching ArrowRight moves document.activeElement to the next button and does NOT call onChange); and `document.querySelector('[role="tab"]')` is null. Note: `role="list"` on a div is a first use in this codebase; that is intentional, not an oversight.
  </action>

  <verify>
    <automated>./node_modules/.bin/vitest run src/components/chatwheel/BaseCommandCatalog.test.tsx && ./node_modules/.bin/tsc -b && node scripts/check-i18n.mjs && node scripts/gen-locale-manifest.mjs --check</automated>
  </verify>

  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/components/chatwheel/BaseCommandCatalog.test.tsx` exits 0 covering every behavior line above.
    - `grep -rn 'role="tab"' src/components/chatwheel/` returns nothing.
    - `grep -n 'SegmentedControl\|useSegmentedTabs\|<Toggle' src/components/chatwheel/BaseCommandCatalog.tsx src/components/chatwheel/TriStateControl.tsx` returns nothing.
    - Every key in the UI-spec i18n table exists in translation.json; `node scripts/check-i18n.mjs` exits 0; `node scripts/gen-locale-manifest.mjs --check` exits 0.
    - No raw command id string appears in translation.json.
    - `./node_modules/.bin/tsc -b` exits 0.
  </acceptance_criteria>

  <done>
    The catalogue section exists as tested components: searchable, filterable, honestly labelled, three-state editable, with unknown entries surfaced rather than hidden, and a keyboard floor with no fake tab semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the section into the Chat Wheel page</name>

  <files>
    src/pages/ChatWheel.tsx,
    src/pages/ChatWheel.test.tsx
  </files>

  <read_first>
    src/pages/ChatWheel.tsx (the section at line 297, the xl:grid closing at line 375, the datalist at 377, the Advanced YAML details at 381, applyModel at line 59),
    .planning/phases/09-the-base-command-catalogue/09-UI-SPEC.md (Section Layout: placement and the details wrapper snippet),
    src/pages/ChatWheel.test.tsx (existing mocks and flush pattern)
  </read_first>

  <behavior>
    - The catalogue renders inside a native details element titled 'Base command catalogue', placed after the editor/preview grid and before the icon datalist, defaulting open, its open state held in page state for the session (toggling it, editing elsewhere, and re-rendering keeps the user's choice). Deliberate simplification of D-06: the section defaults open for every load, not only new drafts; session retention of the user's choice is what D-06 actually protects and it is honored.
    - Changing a TriStateControl updates the Advanced YAML textarea text through updateChatWheelYaml (observable: the yaml state gains or loses exactly the expected override line), and the control state follows manual textarea edits through parseChatWheelYaml.
    - While starterLoading, the section shows the loading skeletons and disabled controls.
    - The page compiles with no unused imports and every new visible string is an i18n key.
  </behavior>

  <action>
In `src/pages/ChatWheel.tsx`: add `const [catalogOpen, setCatalogOpen] = useState(true);` and an `onSetOverride` callback `(id, map, state) => applyModel(applyOverride(model, map, id, state))`. Render, between the xl:grid's closing div and the datalist, the details wrapper from the UI spec (`open={catalogOpen}`, `onToggle` reading `event.currentTarget.open` into state, `className="rounded border border-border bg-bg-primary p-3"`, summary with `Tx k="chatWheel.catalog.title"`), containing `<BaseCommandCatalog model={model} loading={starterLoading} disabled={starterLoading || busy !== null} onSetOverride={...} />` inside a `mt-3` body. Import `applyOverride` from the model lib and the component. React 19 details behaviour (verified empirically): a controlled `open` fires no toggle loop and DOM/state stay in sync, but React emits one `toggle` event on mount with `open=true` before any interaction, so the handler must be idempotent (`if (next !== catalogOpen) setCatalogOpen(next)`) and no test may assert the handler was not called before interaction. In jsdom, drive the section by dispatching a bubbling `click` on the `summary` inside `act()`; jsdom does flip `details.open` on summary activation.

Extend `src/pages/ChatWheel.test.tsx`. Two harness facts the tests depend on:

1. The current `apiMock.getChatWheelStarter` returns `'name: My Chat Wheel\ncustom_menus:\n'` with no override maps at all. Change it to return the real starter shape (`name:`, `override_bindable: {}`, `override_ping_wheel_bindable: {}`, `custom_menus:`), keeping the existing tests passing, so the round-trip test exercises the inline-{}-to-block path that actually ships in resources/chatlane/starter.yml rather than the append-at-end path.
2. The page renders a disabled "Chat Wheel is off" state unless `settings?.experimentalChatWheel` is true, and the harness `beforeEach` resets `appStoreMock.settings` to undefined. Every new test must set `appStoreMock.settings = { experimentalChatWheel: true }` before `renderPage()` or the catalogue never mounts and the failure is opaque.

Then: assert the summary text renders and the section is open by default; simulate an override change by dispatching a bubbling MouseEvent click (no testing-library exists in this repo) on an On button inside the catalogue for a known command inside `act()`, then `await flush()`, and assert the textarea value now contains that command's `: true` line under `override_bindable:`; then simulate a manual textarea edit setting the same key to false and assert the pressed state of that row's Off button. Keep the existing tests' assertions untouched and passing.

Finally, update `docs/chat-wheel.md`: the override maps are no longer Advanced-YAML-only, so describe the Base command catalogue section (search, category filters, per-command three-state overrides, the "Other commands in this file" group) in the page-behavior section, leaving the "never edits gameinfo.gi" rule and everything else untouched. While there, note that the bundled CLI's fork repo link is not publicly readable (it 404s), matching the provenance note in chatWheelCommands.ts.
  </action>

  <verify>
    <automated>./node_modules/.bin/vitest run src/pages/ChatWheel.test.tsx src/components/chatwheel/BaseCommandCatalog.test.tsx src/lib/chatWheelModel.test.ts && ./node_modules/.bin/tsc -b && ./node_modules/.bin/eslint src/pages/ChatWheel.tsx src/components/chatwheel && node scripts/check-i18n.mjs && node scripts/gen-locale-manifest.mjs --check && node scripts/check-encoding.mjs</automated>
  </verify>

  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/pages/ChatWheel.test.tsx` exits 0 with the round-trip assertions (control to textarea, textarea to control) present.
    - The details section sits between the grid and the datalist in the JSX; the Advanced YAML details is untouched.
    - `./node_modules/.bin/tsc -b`, the scoped eslint run, and `node scripts/check-i18n.mjs` exit 0.
    - `node scripts/check-encoding.mjs` exits 0.
  </acceptance_criteria>

  <done>
    The Chat Wheel page carries the full catalogue surface: form controls write YAML, YAML writes form controls, and Advanced YAML remains the single source of truth.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML-derived strings to DOM | Unknown override keys from user YAML render in the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure / XSS | unknown YAML keys rendered in the Other commands group | low | accept | Keys render as React text nodes (auto-escaped), never as HTML, hrefs, selectors, or paths. |
| T-09-05 | Tampering | catalogue toggles writing YAML | low | mitigate | Toggles write only boolean tokens through applyOverride and updateChatWheelYaml; they cannot construct arbitrary YAML. The existing converter validation still runs on the result before any install. |
| T-09-SC | Tampering | npm installs | none | accept | Zero new dependencies. |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/components/chatwheel/BaseCommandCatalog.test.tsx src/pages/ChatWheel.test.tsx` passes.
- `./node_modules/.bin/tsc -b`, `./node_modules/.bin/eslint .`, `node scripts/check-i18n.mjs`, `node scripts/gen-locale-manifest.mjs --check`, `node scripts/check-encoding.mjs` all pass.
- Full `./node_modules/.bin/vitest run` stays at or below the documented pre-existing baseline of 9 failing files / 26 failing tests (browserContentFilter x2, browserDownloadCapture, foundryNonStandard, foundryTextureReplace, modinfoFormat, vpkIdentity, heroStageMode, uiPrefs; recorded in docs/upstream-absorption-1.28.md).
</verification>

<success_criteria>
A user can search and filter all 53 base commands, read each one's honest game-default status, set either override to inherit/on/off without touching YAML, see and edit unknown entries instead of losing them, and every phase 9 roadmap success criterion for the UI half is observably true in the running page.
</success_criteria>

## Artifacts this plan produces

**New files**
- `src/components/chatwheel/TriStateControl.tsx`
- `src/components/chatwheel/BaseCommandCatalog.tsx`
- `src/components/chatwheel/BaseCommandCatalog.test.tsx`

**Changed files**
- `src/pages/ChatWheel.tsx` (catalogue section + onSetOverride wiring)
- `src/pages/ChatWheel.test.tsx` (starter mock updated to the real shape; new round-trip tests)
- `src/locales/en/translation.json` (the chatWheel.catalog.* keys), `src/locales/manifest.json` (regenerated)
- `docs/chat-wheel.md` (documents the catalogue section; the override maps are no longer Advanced-YAML-only)

<output>
Create `.planning/phases/09-the-base-command-catalogue/09-03-SUMMARY.md` when done
</output>
