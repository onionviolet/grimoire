---
phase: 05-one-inventory-one-journey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/lockerMode.ts
  - src/lib/lockerMode.test.ts
  - src/lib/globalInventory.ts
  - src/lib/globalInventory.test.ts
  - src/pages/Locker.tsx
  - src/locales/en/translation.json
  - src/locales/manifest.json
autonomous: true
requirements: [REQ-global-inventory-coherence, REQ-sound-locker-surface, REQ-ui-consequence-and-vocabulary]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Opening `/locker/global` with no query lands on `All content`, which lists both visual categories and global sound categories in one rail from one merged projection (per D-01, D-05)"
    - "`?mode=looks` and `?mode=sounds` still select exactly the section they select today, so every shipped deep link keeps its meaning (per D-02, D-05)"
    - "`/locker/sounds`, `/locker/sounds/global` and `/locker/sounds/hero/<name>` still rewrite to the same canonical destinations they rewrite to today, unaffected by the widened default (per D-02, REQ-sound-locker-surface)"
    - "The third section widens what Global shows, not what Global owns: hero-specific installed sounds stay on each Locker hero's own page, global sounds stay under Global, and Foundry stays the authoring context, so this is one inventory model with contextual views rather than one giant route (per D-03)"
    - "A rail category whose count is zero renders no row at all, in both the visual and the sound vocabulary (per D-04)"
    - "When a narrower section has zero non-empty categories, the rail shows a three-part reset state naming what is missing, why the view is empty, and a control that returns to `All content` (per D-04, D-18)"
    - "The header total keeps reading `countGlobalInventoryMods`, so the number beside `Global` is the same unique-mod count in all three sections and never a sum of the section rows (per D-04)"
    - "The `All content` rail renders the section-membership caveat once above the list, never per row, so its counts are never presented as values that should sum to the header (per D-04)"
    - "Selecting a sound-category row while `All content` is active renders the sound shelf body, and selecting a visual-type row renders the visual card grid; `activeRailKey` always matches a rendered row (per D-05)"
    - "The retag context menu keeps listing every visual category unconditionally, because it is a retag target list rather than an inventory rail (per D-04)"
    - "The runtime concept `which installed source the game reads at this exact path right now` is named `Active source` at every key that carries it, across Locker, Foundry, the sound shelf, the pool view and the portrait family surfaces (per D-17)"
    - "Build-time collision resolution keeps its own separate wording, because deciding which of two edits wins an entry in one build is a different fact from which installed mod the game reads at that path (per D-17)"
    - "Every i18n key this phase's later plans consume exists in `src/locales/en/translation.json` after this plan, so no later plan needs to touch the catalog (per D-17)"
    - "With no visual or sound mods installed at all, all three segments stay selectable and `All content` renders the drill-in's existing whole-inventory empty state rather than disabling the selector"
    - "The selector itself has no distinct error state: a failure to load mods stays the existing whole-app mod-load error path, unchanged"
    - "Exactly three segments render, always; the selector is never a variable-length list"
    - "Global has no partial-hero concept, so the only partial state on this surface is the category-level one; the selector has no partial rendering of its own"
    - "The three segment labels sit inside the existing rail flex row and introduce no overflow surface the two-segment version did not already tolerate"
    - "A rail row's presence is decided once per render pass from the same `groups` and `globalSoundEntries` computation the header total already reads, so no row flickers in and out while counts resolve"
    - "A category whose count is above zero renders with exactly the row treatment it has today"
    - "Hiding rows renders a strict subset of today's rows, so no new overflow surface appears, and the reset state fits the rail's existing width"
    - "Zero non-empty categories in a narrower section renders the reset state; one or many render as today without empty rows interleaved"
    - "The reset state's copy wraps within the rail's existing width like every other rail label"
    - "Hide-empty is a fixed always-on rule with no user-facing preference control, and `lockerHideEmpty` (the hero-grid preference) is left untouched"
    - statement: "The three-segment selector renders immediately and lets the merged list populate under it, matching the existing two-tab behaviour, rather than showing a transient state before `globalGroups` and `globalSoundEntries` first resolve"
      verification: backstop
    - statement: "`All content` is the longest of the three English labels and still fits the rail's existing width in the longest shipped locale"
      verification: backstop
    - statement: "A sound entry that failed per-mod classification keeps its category visible, because unclassified content is still content"
      verification: backstop
  prohibitions:
    - requirement_id: REQ-global-inventory-coherence
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT present section row counts as values that add up to the header total; the header stays the Set-deduplicated unique-mod count and the merged list carries the membership caveat"
    - requirement_id: REQ-sound-locker-surface
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT break or narrow any legacy `/locker/sounds*` rewrite while widening the default section, and MUST NOT reintroduce a second top-level route as a home for global inventory"
    - requirement_id: REQ-global-inventory-coherence
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT pull hero-specific installed sounds into the Global drill-in as part of widening it; per D-03 the hero page stays the management context for a hero's own sounds and Global stays scoped to global content"
  artifacts:
    - path: "src/lib/lockerMode.ts"
      provides: "The widened three-value LockerMode and its route defaulting"
      contains: "'all'"
    - path: "src/lib/globalInventory.ts"
      provides: "globalInventoryRailRows and firstGlobalRailRowKey, the pure merged and hide-empty rail projection"
      exports: ["globalInventoryRailRows", "firstGlobalRailRowKey", "GlobalRailRow", "GlobalRailRowInput"]
    - path: "src/pages/Locker.tsx"
      provides: "The three-segment Global selector, the merged rail, the reset state and the split rail/pane vocabulary booleans"
      contains: "globalInventoryRailRows"
    - path: "src/locales/en/translation.json"
      provides: "The Active source vocabulary and every new key this phase consumes"
      contains: "Active source"
  key_links:
    - from: "src/pages/Locker.tsx"
      to: "src/lib/globalInventory.ts"
      via: "LockerGlobalView builds its rail rows through globalInventoryRailRows instead of the inline two-branch map"
      pattern: "globalInventoryRailRows"
    - from: "src/pages/Locker.tsx"
      to: "src/lib/lockerMode.ts"
      via: "GLOBAL_SECTION_TABS and the onSelectSection navigate call both range over the widened LockerMode"
      pattern: "GLOBAL_SECTION_TABS"
    - from: "src/lib/lockerMode.ts"
      to: "src/pages/Locker.tsx"
      via: "resolveLockerRoute's legacy branch still hands `/locker/sounds/global` to the explicit `?mode=sounds` rewrite"
      pattern: "mode=sounds"
---

<objective>
Give Global one inventory with three views. Widen `LockerMode` to `all | looks | sounds`, add the merged `All content` rail projection, hide empty categories, and land the phase's entire English catalog change in one place so no later plan has to reopen it.

Purpose: closes REQ-global-inventory-coherence's two genuinely unbuilt halves (the tri-state selector and the hide-empty rail), answers REQ-sound-locker-surface by proving the legacy URL contract survives rather than by building a second home, and executes D-17's "fix the catalog before the call sites" instruction literally.
Output: a three-value `LockerMode`, a pure `globalInventoryRailRows` projection beside `countGlobalInventoryMods`, a three-segment accessible tablist, a three-part reset state, and a catalog that already holds every key plans 02 through 05 will read.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-one-inventory-one-journey/05-CONTEXT.md
@.planning/phases/05-one-inventory-one-journey/05-RESEARCH.md
@.planning/phases/05-one-inventory-one-journey/05-PATTERNS.md
@.planning/phases/05-one-inventory-one-journey/05-UI-SPEC.md
@CLAUDE.md
@docs/ui-conventions.md

<interfaces>
Contracts the executor needs. Do not go looking for these in the codebase.

From `src/lib/lockerMode.ts` (existing, lines 9-14 and 58-81):

```typescript
export type LockerMode = 'looks' | 'sounds';

export function lockerModeFromSearch(search: string): LockerMode | null {
  const mode = new URLSearchParams(search).get('mode');
  return mode === 'looks' || mode === 'sounds' ? mode : null;
}

export function resolveLockerRoute(pathname: string, search: string): LockerRoute {
  const legacy = legacySoundTarget(pathname);
  if (legacy) { /* ... unchanged ... */ }
  if (/^\/locker\/global\/?$/.test(pathname)) {
    return { drillIn: 'global', section: lockerModeFromSearch(search) ?? 'looks' };
  }
  /* ... hero and grid branches unchanged ... */
}
```

From `src/lib/globalInventory.ts` (existing, the shape to mirror):

```typescript
export function countGlobalInventoryMods(
  groups: GlobalModGroups,
  soundEntries: readonly SoundInventoryEntry[]
): number;

export function countGlobalInventoryCategories(
  groups: GlobalModGroups,
  soundEntries: readonly SoundInventoryEntry[]
): number;
```

From `src/pages/Locker.tsx` (existing), the four state values the third section has to be reconciled against:

```typescript
const isSounds = section === 'sounds';                                    // :1716
const [selectedType, setSelectedType] = useState<GlobalModType | null>(null);          // :1722
const [selectedSoundCategory, setSelectedSoundCategory] = useState<SoundCategory | null>(null); // :1785
const activeRailKey: string = isSounds ? `sound:${activeSoundCategory}` : activeType;  // :1796
```

From `src/pages/Locker.tsx` (existing, line 1511), the only `onSelectSection` call site:

```typescript
onSelectSection={(next) =>
  navigate(next === 'sounds' ? '/locker/global?mode=sounds' : '/locker/global')
}
```

From `src/pages/Locker.tsx` (existing, line 467-470), the legacy rewrite this plan must leave working:

```typescript
if (route.drillIn !== 'legacy') return;
const target = route.legacy;
if (target.kind === 'global') {
  navigate('/locker/global?mode=sounds', { replace: true });
```
</interfaces>
</context>

<planner_decisions>
Recorded so the executor does not re-litigate them.

- **The third member is named `'all'`.** `lockerModeFromSearch` accepts it, so `?mode=all` is a valid explicit deep link, and `resolveLockerRoute`'s default for a bare `/locker/global` becomes `'all'` per D-05. `onSelectSection` writes the bare path for `'all'` and keeps the two existing query forms, so the canonical URL for the default view has no query at all.
- **Hide-empty is a fixed rule, no preference control.** This settles the UI-SPEC's E2 `partial` row, which it marked `unresolved` (fixed always-on rule versus a user toggle mirroring `lockerHideEmpty`). D-04 says "hidden by default", which permits but does not require a toggle, and CONTEXT.md's Discretion section does not name a toggle as open. A fixed rule is the smaller surface. Recorded as an explicit planner assumption, not a silent build.
- **Vocabulary changes values at existing key names.** This settles the second UI-SPEC `unresolved` row. No key is renamed and no call site moves, so `pnpm i18n:check` stays green. The consequence, stated rather than hidden: the shipped translated locales keep their keys and will show a stale translation of the old English meaning until Weblate re-translates. That is accepted, and it is why the change is made at the catalog rather than at 30 call sites.
- **Which keys the `Active source` consolidation covers.** Only the runtime concept: which installed source the game reads at this exact entry path right now. Build-time collision resolution keeps its own wording, because "which of two edits in this build wins this entry" is a different fact. Excluded on that ground and left exactly as they are: `foundry.buildTray.collisionWinner`, `foundry.buildTray.confirmCollisions_one`, `foundry.buildTray.confirmCollisions_other`, `mergeMods.review.winner`, `mergeMods.review.unreadable_one`, `mergeMods.review.unreadable_other`, `conflicts.reorder.makeWinner`. Also excluded: `soundLocker.row.openWinner` (value "open" is a verb, not a name for the state) and `portrait.family.winnerRow` / `portrait.family.moreWinners_one` / `portrait.family.moreWinners_other` (their key names carry the old word but their values already say "replaced").
- **Verified citation correction, carried into this plan.** The UI-SPEC and RESEARCH.md both cite the two lead consolidation targets as `locker.cards.winner` and `locker.cards.isWinner`. Parsing the catalog shows those key paths do not exist: the keys holding "Current winner: {{name}}" and "winner" are `foundry.sources.winner` and `foundry.sources.isWinner`, rendered by `AssetSourcesPanel.tsx:210,294` and `SoundBrowse.tsx:957`. `locker.cards` has no `winner` key; it has `locker.cards.ownership`. The line numbers 527 and 538 in both documents are correct, the namespaces are not.
- **Second verified citation correction.** The UI-SPEC directs new retry actions to reuse `locker.pose.retry` "verbatim". That key's value is "Retry preview", which is wrong copy for a catalog retry. `common.actions.retry` = "Retry" is the generic one, and the sibling portrait surface already uses it (`PortraitBrowse.tsx:302`). Later plans reuse `common.actions.retry`; no new retry key is added.
- **No orphan keys.** Every key added by task 2 has a named consumer in plan 02, 03 or 04. The singular "Portrait family" term D-17 asks for is delivered inside `portrait.family.failedTitle`, a string that is actually rendered, rather than as a standalone label key nothing reads. RESEARCH.md's Pitfall 2 is the reason: `pnpm i18n:check` cannot see an unread key, so an orphan would be invisible to every gate.
</planner_decisions>

<artifacts_this_phase_produces>
Symbols this plan creates.

- Union member: `'all'` on `LockerMode` (`src/lib/lockerMode.ts`)
- Types: `GlobalRailRowInput`, `GlobalRailRow` (`src/lib/globalInventory.ts`)
- Functions: `globalInventoryRailRows`, `firstGlobalRailRowKey` (`src/lib/globalInventory.ts`)
- i18n keys (new): `locker.global.sectionAll`, `locker.global.sectionMembershipHint`, `locker.global.filteredZeroTitle`, `locker.global.filteredZeroBody`, `locker.global.filteredZeroReset`, `locker.global.unnamedPakDerived`, `locker.global.unnamedPakDerivedMore`, `locker.global.unnamedPakUnknown`, `locker.cards.shuffleScopeThisHero`, `foundry.myChanges.shuffleScopeAllForged`, `portrait.family.unresolvedTag`, `portrait.family.unresolvedHint`, `portrait.family.failedTitle`, `portrait.family.failedBody`, `installed.actions.bulkBusy`, `installed.actions.bulkUndoing`, `common.bulkUndo.message_one`, `common.bulkUndo.message_other`, `common.bulkUndo.action`, `common.bulkUndo.restored_one`, `common.bulkUndo.restored_other`, `common.bulkUndo.partial`
- i18n keys (value changed, names unchanged): `foundry.sources.winner`, `foundry.sources.isWinner`, `foundry.myChanges.overriddenBy`, `foundry.myChanges.openWinner`, `foundry.myChanges.resolving`, `foundry.myChanges.noWriteSet`, `foundry.sound.existingSources`, `foundry.sound.existingSourcesHint`, `soundLocker.row.overriddenBy`, `poolView.winner`, `poolView.winsPathsHint`, `poolView.foreignWinner`, `portrait.family.sourcesTitle`, `locker.cards.ownership`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End to end "select All content" through route, projection, tablist and pane, one path only</name>
  <reversibility rating="reversible">Per D-05 the default section for a bare `/locker/global` changes from Visuals to All content; reverting is a one-line change to `resolveLockerRoute`'s fallback and the tab array, and no shipped URL changes meaning either way.</reversibility>
  <read_first>
    - src/lib/lockerMode.ts
    - src/lib/lockerMode.test.ts
    - src/lib/globalInventory.ts
    - src/lib/globalInventory.test.ts
    - src/lib/globalSoundSections.ts
    - src/lib/soundInventory.ts
    - src/pages/Locker.tsx
    - src/locales/en/translation.json
    - .planning/phases/05-one-inventory-one-journey/05-RESEARCH.md
    - .planning/phases/05-one-inventory-one-journey/05-PATTERNS.md
    - .planning/phases/05-one-inventory-one-journey/05-UI-SPEC.md
    - docs/ui-conventions.md
  </read_first>
  <files>src/lib/lockerMode.ts, src/lib/lockerMode.test.ts, src/lib/globalInventory.ts, src/lib/globalInventory.test.ts, src/pages/Locker.tsx, src/locales/en/translation.json, src/locales/manifest.json</files>
  <action>
Wire exactly ONE new path, `All content`, from the URL through the pure projection to a rendered rail row and a rendered body pane. Do not touch the retag context menu near `Locker.tsx:2407`; it is a retag target list and is correctly unconditional, which is a different call site from the rail.

Route layer. In `src/lib/lockerMode.ts` widen `LockerMode` to `'all' | 'looks' | 'sounds'`. Extend `lockerModeFromSearch` to accept `all` as a third recognised value, still returning null for anything else. Change `resolveLockerRoute`'s global branch fallback from `'looks'` to `'all'` per D-05. Update the type's head comment so it states that the query parameter now selects one of three sections and that a bare path means all content. Leave `legacySoundTarget`, the `LegacySoundTarget` union, `HERO_SECTIONS` and every other branch of `resolveLockerRoute` untouched: the legacy rewrite at `Locker.tsx:467-470` navigates to an explicit `?mode=sounds` and must keep landing on the Sounds section, not on the new default.

Pure projection. In `src/lib/globalInventory.ts` add, beside the two existing counters and following their shape, `export interface GlobalRailRowInput { key: string; label: string; count: number }`, `export interface GlobalRailRow extends GlobalRailRowInput { kind: 'visual' | 'sound' }`, and `export function globalInventoryRailRows(section: LockerMode, visualRows: readonly GlobalRailRowInput[], soundRows: readonly GlobalRailRowInput[]): GlobalRailRow[]`. It returns the visual rows tagged `'visual'` for `looks`, the sound rows tagged `'sound'` for `sounds`, and the visual rows followed by the sound rows for `all`, in every case keeping only rows whose count is greater than zero, per D-04. Add `export function firstGlobalRailRowKey(rows: readonly GlobalRailRow[]): string | null` returning the first row's key or null. Both functions are pure and take already-localized labels, because the labels come from `t()` and `GLOBAL_MOD_TYPE_LABELS` in the component and this module must stay renderer-framework free. Write a head comment explaining that hiding a zero-count row reverses a deliberate as-shipped decision, and name D-04 as the decision that reversed it.

Component state. In `src/pages/Locker.tsx`, inside `LockerGlobalView`, replace the single `isSounds` boolean with two derived values so the third section cannot silently break the four pieces of selection state (this is the failure RESEARCH.md's Pitfall 5 names). Add `const [selectedAllKey, setSelectedAllKey] = useState<string | null>(null)` recording the last row clicked while `all` is showing. Build `railRows` with `useMemo` from `globalInventoryRailRows(section, visualRowInputs, soundRowInputs)`, where `visualRowInputs` maps `GLOBAL_VISUAL_MOD_TYPE_ORDER` to `{ key: type, label: GLOBAL_MOD_TYPE_LABELS[type], count: groups[type].length }` and `soundRowInputs` maps `soundCounts` to `{ key: 'sound:' + id, label: globalSoundSectionLabel(t, id), count }`. Derive `activeRailKey` as: for `sounds`, the existing `'sound:' + activeSoundCategory`; for `looks`, the existing `activeType`; for `all`, `selectedAllKey` when it still matches a row in `railRows`, otherwise `firstGlobalRailRowKey(railRows)`, otherwise `activeType`. Derive `paneIsSounds` as `section === 'sounds' || (section === 'all' && activeRailKey.startsWith('sound:'))` and use it everywhere the old `isSounds` decided which body renders, which highlight key is measured, and which heading is drawn. Keep `activeType`, `activeMods`, `activeSoundCategory` and `shownSoundEntries` exactly as they are derived today; the merged view reuses them rather than adding a parallel derivation.

Row selection. Each rendered rail row's `onSelect` keeps calling the existing `setSelectedType` or `setSelectedSoundCategory` for its own vocabulary, and additionally calls `setSelectedAllKey(row.key)`. That is what makes a click in the merged view move both the highlight and the body pane through the state the two existing sections already use, with no third source of truth for what is selected. The retag menu's `isCurrent` comparison keeps reading `activeType`, which is still a `GlobalModType`, so the menu is correct in all three sections without a change; state that in a short comment beside it.

Tablist. Widen `GLOBAL_SECTION_TABS` to `['all', 'looks', 'sounds']`. The roving arrow, Home and End handler at lines 1806-1819 is already length-generic and needs no change. In the render loop at lines 1921-1950, replace the two-arm label ternary with a three-arm one reading `locker.global.sectionAll` for `all`, `locker.mode.sounds` for `sounds` and `locker.global.visuals` otherwise. Every other attribute on the button, including the shared `aria-controls` panel id and the `tabIndex` roving rule, stays exactly as it is. At the one `onSelectSection` call site, `Locker.tsx:1511`, write `/locker/global?mode=sounds` for sounds, `/locker/global?mode=looks` for looks, and the bare `/locker/global` for all, so the default view's canonical URL carries no query.

Rail render and reset state. Replace the inline two-branch array built at lines 1970-1982 with `railRows.map(...)`, keeping the row button markup, the ref registration into `tabRefs`, the sliding indicator and the count span exactly as they are. Delete the `isEmpty` opacity branch, which can no longer fire now that zero-count rows do not render, and replace the block comment above it with one that states the current rule and cites D-04. When `railRows` is empty and `section` is not `all`, render a reset block in the rail instead of the rows: a heading from `locker.global.filteredZeroTitle` interpolating the active section's own label, a body from `locker.global.filteredZeroBody`, and a button reading `locker.global.filteredZeroReset` that calls `onSelectSection('all')`. Use the existing rail typography and a `p-4` container. When `railRows` is empty and `section` is `all`, render nothing extra: the whole inventory is genuinely empty and the right pane's existing empty state is the correct and only message.

Membership caveat. When `section` is `all`, render `locker.global.sectionMembershipHint` once directly above the rail rows as an `text-[11px] text-text-secondary` line, never per row, per D-04. Leave the header total at lines 1905-1914 completely untouched: it already reads `countGlobalInventoryMods` and is the one denominator.

i18n for this task only. Add five keys under `locker.global` in `src/locales/en/translation.json`, ASCII only, straight apostrophes, no em-dashes: `sectionAll` "All content", `sectionMembershipHint` "Counted once per section. A mod can appear in more than one.", `filteredZeroTitle` "No {{filter}} content installed", `filteredZeroBody` "Empty categories are hidden, and nothing in this section has content.", `filteredZeroReset` "Show all content". Then run `pnpm i18n:manifest`.

Tests. Extend `src/lib/lockerMode.test.ts`: `lockerModeFromSearch` returns the third value for its own query form and still returns null for an unknown one; a bare `/locker/global` and a trailing-slash `/locker/global/` both resolve to the new default section; `?mode=looks` and `?mode=sounds` still resolve to exactly what they resolve to today; and every existing legacy case, including `/locker/sounds`, `/locker/sounds/global`, `/locker/sounds/hero/Grey%20Talon` and `/locker/sounds?hero=Seven`, still returns the same object it returns today. Extend `src/lib/globalInventory.test.ts` with cases for `globalInventoryRailRows`: each of the three sections returns the right vocabulary and the right tag; a zero-count row is dropped in all three; the merged order is visual rows first then sound rows; a section whose rows are all zero returns an empty array; and `firstGlobalRailRowKey` returns null for an empty array.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/lockerMode.test.ts src/lib/globalInventory.test.ts && pnpm typecheck && pnpm lint && pnpm i18n:check && pnpm encoding:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0, `pnpm lint` exits 0, `pnpm i18n:check` exits 0, `pnpm encoding:check` exits 0
    - `pnpm exec vitest run src/lib/lockerMode.test.ts src/lib/globalInventory.test.ts` exits 0
    - `src/lib/lockerMode.ts` declares a three-member `LockerMode` union, verified by `node -e` printing the matched type declaration line
    - `src/lib/globalInventory.ts` exports both `globalInventoryRailRows` and `firstGlobalRailRowKey`
    - `src/pages/Locker.tsx` imports `globalInventoryRailRows` from `../lib/globalInventory`, and the `<nav>` rail block maps over the rows that function returns rather than over a section-branching ternary
    - The retag menu is provably untouched: `git diff src/pages/Locker.tsx` shows no changed line inside the `role="menu"` block, and its category list still enumerates every visual type with no count filter
    - `src/pages/Locker.tsx` contains `GLOBAL_SECTION_TABS` with three entries and its tablist render has a three-arm label expression
    - `src/locales/en/translation.json` contains `"sectionAll"` and `"filteredZeroReset"`, and a second `pnpm i18n:manifest` leaves `src/locales/manifest.json` with no diff
    - `src/lib/lockerMode.test.ts` contains an assertion that `/locker/sounds/global` still resolves to its existing legacy target object
    - Behaviour: opening `/locker/global` with no query shows three segments with the first selected, a rail listing both visual and sound categories, and no row whose count reads 0
  </acceptance_criteria>
  <done>`All content` is selectable end to end from URL through pure projection to a rendered rail row and body pane, empty categories no longer render, every legacy sound URL still resolves as before, and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Land the whole phase's catalog change: Active source vocabulary plus every key plans 02 to 05 consume</name>
  <read_first>
    - src/locales/en/translation.json
    - src/components/foundry/AssetSourcesPanel.tsx
    - src/components/foundry/ChangePools.tsx
    - src/components/foundry/MyChanges.tsx
    - src/components/foundry/MySoundChanges.tsx
    - src/components/foundry/SoundBrowse.tsx
    - src/components/locker/SoundEntryRow.tsx
    - src/components/locker/HeroCardPicker.tsx
    - .planning/phases/05-one-inventory-one-journey/05-UI-SPEC.md
    - CLAUDE.md
  </read_first>
  <files>src/locales/en/translation.json, src/locales/manifest.json</files>
  <action>
Apply D-17's vocabulary consolidation as value edits at existing key names, then add every remaining key the later plans read. This is a catalog-only task: no `.ts` or `.tsx` file changes, because a value edit at an unchanged key name needs no call-site change. Use ASCII only, straight apostrophes, three-dot ellipsis, and no em-dashes anywhere.

Active source consolidation. Change the value at each of these fourteen existing keys, leaving every key name and every call site exactly as it is:

- `foundry.sources.winner`: to "Active source: {{name}}"
- `foundry.sources.isWinner`: to "Active source"
- `foundry.myChanges.overriddenBy`: to "Active source: {{winner}}"
- `foundry.myChanges.openWinner`: to "Open the active source"
- `foundry.myChanges.resolving`: to "Resolving the active source..."
- `foundry.myChanges.noWriteSet`: replace the phrase naming a live winner with one naming the active source, keeping the rest of the sentence and the re-forge instruction intact
- `foundry.sound.existingSources`: to "Sources and active source"
- `foundry.sound.existingSourcesHint`: to "Show installed sources and the active source for these exact sound paths"
- `soundLocker.row.overriddenBy`: to "Active source: {{winner}}"
- `poolView.winner`: to "Active source"
- `poolView.winsPathsHint`: to "Active source for: {{paths}}"
- `poolView.foreignWinner`: to "A mod outside this pool is the active source for {{n}} of its paths, so shuffling will not change them."
- `portrait.family.sourcesTitle`: to "Sources and active source"
- `locker.cards.ownership`: to "Show installed sources and the active source"

Every interpolation placeholder already present in a value stays present and keeps its exact name, because the call sites are unchanged. Do not touch the seven build-time collision keys or the three excluded keys named in this plan's `planner_decisions`.

New keys the later plans consume. Add these and nothing else, each as a sibling inside the namespace named:

- Under `locker.cards`: `shuffleScopeThisHero` "This hero"
- Under `foundry.myChanges`: `shuffleScopeAllForged` "All forged portraits"
- Under `portrait.family`: `unresolvedTag` "Unresolved"; `unresolvedHint` "Grimoire could not match these to a known hero. Their raw names are shown exactly as they appear in your game files."; `failedTitle` "Portrait family could not be read"; `failedBody` "Grimoire could not read {{hero}}'s portrait family from your installed game files, so it cannot show what a replacement would change. Retry, or check the game path in Settings."
- Under `locker.global`: `unnamedPakDerived` "Writes {{entryList}}"; `unnamedPakDerivedMore` "Writes {{entryList}} and {{count}} more"; `unnamedPakUnknown` "Unknown content"
- Under `installed.actions`: `bulkBusy` "Finish the current update before starting another."; `bulkUndoing` "Restoring the previous state. This will finish in a moment."
- Under `common`, as a new `bulkUndo` object: `message_one` "{{count}} mod updated."; `message_other` "{{count}} mods updated."; `action` "Undo"; `restored_one` "{{count}} mod restored."; `restored_other` "{{count}} mods restored."; `partial` "{{done}} of {{total}} mods updated. {{failed}} could not be changed."

`portrait.family.failedTitle` is where D-17's singular "Portrait family" term lands. It is deliberately delivered inside a string that gets rendered rather than as a standalone label key, because a key nothing reads is invisible to `pnpm i18n:check` and would rot unnoticed.

Regenerate the manifest with `pnpm i18n:manifest`, then confirm a second run leaves no diff. Do not add, remove or rename any key in `src/locales/unwired-en.json`, and do not edit any non-English catalog: the translated locales keep their keys and go semantically stale until Weblate re-translates, which is the accepted and stated consequence of a value-only pass.
  </action>
  <verify>
    <automated>pnpm i18n:check && pnpm encoding:check && node scripts/gen-locale-manifest.mjs --check && pnpm exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm i18n:check` exits 0 and `pnpm encoding:check` exits 0
    - `node scripts/gen-locale-manifest.mjs --check` exits 0
    - `pnpm exec vitest run` exits 0 with no previously passing test now failing
    - A `node -e` script that parses `src/locales/en/translation.json` prints `Active source: {{name}}` for `foundry.sources.winner` and `Active source` for `foundry.sources.isWinner`
    - The same script prints a non-undefined value for every one of these paths: `locker.cards.shuffleScopeThisHero`, `foundry.myChanges.shuffleScopeAllForged`, `portrait.family.unresolvedTag`, `portrait.family.unresolvedHint`, `portrait.family.failedTitle`, `portrait.family.failedBody`, `locker.global.unnamedPakDerived`, `locker.global.unnamedPakDerivedMore`, `locker.global.unnamedPakUnknown`, `installed.actions.bulkBusy`, `installed.actions.bulkUndoing`, `common.bulkUndo.message_one`, `common.bulkUndo.message_other`, `common.bulkUndo.action`, `common.bulkUndo.restored_one`, `common.bulkUndo.restored_other`, `common.bulkUndo.partial`
    - The same script prints the unchanged original value for `mergeMods.review.winner` and for `conflicts.reorder.makeWinner`, proving the build-time collision family was left alone
    - `git diff --name-only` for this task lists only `src/locales/en/translation.json` and `src/locales/manifest.json`
  </acceptance_criteria>
  <done>The runtime active-source concept has one name at every key that carries it, the build-time collision family is untouched and provably so, every key plans 02 to 05 read exists, the manifest is regenerated, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL to renderer | `?mode=` is user-editable and reaches `lockerModeFromSearch`, which decides which section renders |
| mod metadata to rendered copy | GameBanana-controlled mod names and category labels interpolate into rail labels and the reset state's filter name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `lockerModeFromSearch` (`src/lib/lockerMode.ts`) | low | mitigate | Keep the allow-list shape: the function returns one of exactly three literals or null, so an arbitrary `?mode=` value cannot reach the render as a section. Covered by the extended `lockerMode.test.ts` case for an unknown value. |
| T-05-02 | Information Disclosure | i18n interpolation of mod and section names into new copy | low | mitigate | Every new string interpolates through normal JSX children, which React escapes. `escapeValue: false` is set at `src/i18n.ts:190` precisely because React already escapes, so this plan introduces no `dangerouslySetInnerHTML` anywhere near an interpolated value. |
| T-05-03 | Denial of Service | merged rail projection over both axes | low | accept | `globalInventoryRailRows` is O(visual types + sound categories), both fixed-size constant arrays, and reuses counts the header total already computes. No new pass over the mod list. |
| T-05-04 | Repudiation | value-only i18n edits leaving translated locales semantically stale | low | accept | Accepted and stated: keys are unchanged, so `pnpm i18n:check` cannot flag it and no automated gate is being defeated. The staleness window is explicit in this plan and in the summary, and closes when Weblate re-translates. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no external package (05-RESEARCH.md Package Legitimacy Audit: not applicable). No package-manager install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm typecheck` green: widening `LockerMode` makes the compiler enumerate every site that branches over it, which is the authoritative checklist for this plan
- `pnpm lint` green
- `pnpm exec vitest run` green
- `pnpm i18n:check` green, `pnpm encoding:check` green, and `node scripts/gen-locale-manifest.mjs --check` green
- A second `pnpm i18n:manifest` leaves `src/locales/manifest.json` unchanged
</verification>

<success_criteria>
- `/locker/global` opens on `All content`, listing both vocabularies from one merged projection with empty categories hidden
- `?mode=looks`, `?mode=sounds` and every legacy `/locker/sounds*` path resolve to exactly what they resolve to today
- The header total is unchanged and is still the one Set-deduplicated denominator
- The runtime active-source concept has exactly one name across every surface that states it, and build-time collision wording is provably untouched
- The English catalog holds every key plans 02 to 05 consume, so no later plan reopens `src/locales/en/translation.json`
</success_criteria>

<output>
Create `.planning/phases/05-one-inventory-one-journey/05-01-SUMMARY.md` when done
</output>
