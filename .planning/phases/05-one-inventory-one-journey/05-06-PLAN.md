---
phase: 05-one-inventory-one-journey
plan: 06
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-05]
files_modified:
  - src/lib/provenance.ts
  - src/lib/provenance.test.ts
  - src/pages/Conflicts.tsx
  - src/pages/Profiles.tsx
  - src/components/locker/HeroCardPicker.tsx
  - src/components/locker/SoundEntryRow.tsx
  - src/components/locker/GlobalSoundShelf.tsx
  - src/components/locker/HeroSoundShelf.tsx
  - src/components/foundry/ChangePools.tsx
  - src/components/foundry/MyChanges.tsx
  - src/components/foundry/AssetSourcesPanel.tsx
  - src/components/foundry/FoundryBuildTray.tsx
autonomous: true
requirements: [REQ-ui-consequence-and-vocabulary]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Asking `which mod is responsible for this` is answered the same way on all four surfaces Lane 9 names: the Locker rows, the Conflicts cards, Installed and the sound shelves each offer the same phrase pointing at the same target (per D-17, REQ-ui-consequence-and-vocabulary)"
    - "There is exactly one target for provenance, `/?focusMod=<id>`, and it is produced by one helper rather than by seven hand-assembled strings, so a future call site cannot invent an eighth spelling (per D-17)"
    - "There is exactly one catalog key holding the provenance phrase, so the four surfaces cannot drift back to four wordings the way they drifted before (per D-17)"
    - "A Conflicts card can reach the mod it names in Installed, closing the one surface of Lane 9's four that had no provenance affordance at all"
    - "Every control disabled while a Profiles apply or update runs names its blocker in text rendered beside it, and points at that text through an accessible description; a tooltip supplements it and is never the only explanation (per D-16)"
    - "Every control disabled while a Conflicts resolve action runs names its blocker the same way, through rendered text rather than a tooltip (per D-16)"
    - "The Foundry forge and install button names which of its two blockers is in force: nothing staged and selected, or a forge already running (per D-16)"
    - "A blocker prefers a corrective action over a bare statement wherever one exists: the empty forge tray tells the user to select a staged edit rather than only reporting that it is empty (per D-16)"
    - "Exactly one blocker reason renders at a time per control, so two simultaneous explanations never stack (per D-15)"
    - "Re-running the conflict scan keeps the answer the user is currently reading on screen and states that a recheck is running, instead of replacing the page with a skeleton"
    - "The retained conflict answer is explicitly labelled as coming from the previous check while the recheck runs, so a stale row can never be mistaken for a current one"
    - "The very first conflict load, which has no prior answer to preserve, still shows the existing skeleton, because preserving nothing is not the same as preserving something"
    - "A failed recheck falls through to the existing error state with its existing retry, unchanged, because a failure is a different fact from a pending recheck"
    - "The three surfaces that already satisfy a sub-item are left alone and recorded as satisfied with file and line evidence, rather than rewritten to look like new work"
    - "No plan file outside this one changes behaviour: this plan adds affordances and explanations, and mutates no mod, no profile and no conflict record that was not already mutable from the same control"
    - statement: "A blocker line wraps within its existing container at a narrow width like every other inline explanation on these pages, and introduces no new spacing value or tone"
      verification: backstop
    - statement: "A provenance affordance on a Conflicts card for a mod whose record is missing from `modsMap` is not rendered at all, rather than rendered pointing at nothing"
      verification: backstop
  prohibitions:
    - requirement_id: REQ-ui-consequence-and-vocabulary
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT leave a control disabled by an in-flight Profiles apply, Conflicts resolve or Foundry forge with only a tooltip to explain it; each such control carries an accessible description pointing at text that is actually rendered on screen"
    - requirement_id: REQ-ui-consequence-and-vocabulary
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present a retained conflict answer as current while a recheck runs; the retained answer is labelled as being from the previous check for the whole time the recheck is in flight"
    - requirement_id: REQ-ui-consequence-and-vocabulary
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT add a second provenance target or a second provenance phrase; every producer routes through the one helper and every label reads the one key"
    - requirement_id: REQ-ui-consequence-and-vocabulary
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT add, remove or rename any key in `src/locales/en/translation.json` or regenerate `src/locales/manifest.json`; plan 05-01 owns the entire phase catalog change and this plan only consumes keys it created"
  artifacts:
    - path: "src/lib/provenance.ts"
      provides: "The one provenance route builder, so `/?focusMod=<id>` has a single producer"
      exports: ["focusModPath"]
    - path: "src/lib/provenance.test.ts"
      provides: "Coverage for encoding, shape and the refusal to emit an absolute or cross-origin destination"
    - path: "src/pages/Conflicts.tsx"
      provides: "The missing fourth provenance surface, the resolve-action blocker lines, and the recheck that preserves the prior answer"
      contains: "focusModPath"
    - path: "src/pages/Profiles.tsx"
      provides: "The apply and update blocker line and the accessible description every control it disables points at"
      contains: "aria-describedby"
    - path: "src/components/foundry/FoundryBuildTray.tsx"
      provides: "The forge and install button's two named blockers, one at a time"
      contains: "aria-describedby"
  key_links:
    - from: "src/pages/Conflicts.tsx"
      to: "src/lib/provenance.ts"
      via: "the conflict card's provenance button navigates to the path the helper builds"
      pattern: "focusModPath"
    - from: "src/lib/provenance.ts"
      to: "src/pages/Installed.tsx"
      via: "the helper emits the exact query parameter the existing focus effect reads, so a new producer lands on the existing consumer with no change to it"
      pattern: "focusMod"
    - from: "src/pages/Profiles.tsx"
      to: "src/locales/en/translation.json"
      via: "the blocker line reads the key plan 05-01 created; this plan adds no key"
      pattern: "profiles.actions.busyBlocker"
    - from: "src/components/foundry/FoundryBuildTray.tsx"
      to: "src/components/foundry/AssetSourcesPanel.tsx"
      via: "the forge blocker follows the inline aria-live explanation pattern the sources panel already ships, rather than inventing a second one"
      pattern: "aria-live"
---

<objective>
Finish REQ-ui-consequence-and-vocabulary's three sub-items that plan 05-03 did not reach: the disabled-blocker contract extended past Locker and Foundry to Profiles apply, Conflicts resolve and the Foundry forge and install path; pending work preserving the prior answer instead of blanking it; and provenance consolidated to one phrase and one target.

Purpose: plan 05-03 closed the bulk-undo half of this requirement and the blocker half only inside `src/pages/Installed.tsx`. The requirement names three further obligations by surface, and nothing in the ROADMAP amendment, 05-CONTEXT.md's decisions or its Deferred Ideas narrows them away. This plan closes them against the surfaces the requirement actually names.
Output: one provenance route helper with tests, one provenance phrase at one key across all four Lane 9 surfaces plus the Conflicts affordance that did not exist, an accessible blocker line on three further pages, and a conflict recheck that keeps the answer on screen while it runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-one-inventory-one-journey/05-CONTEXT.md
@.planning/phases/05-one-inventory-one-journey/05-PATTERNS.md
@.planning/phases/05-one-inventory-one-journey/05-VALIDATION.md
@CLAUDE.md
@docs/ui-conventions.md
@docs/locker-consistency-pass.md

<interfaces>
Contracts the executor needs. Do not go looking for these in the codebase.

From `src/pages/Installed.tsx` (existing, :975-983), the provenance consumer this plan does
NOT modify. It is the reason `/?focusMod=<id>` is the one correct target:

```typescript
const focusModId = focusParams.get('focusMod');
// effect: when focusModId is set and mods have loaded, scroll to and highlight
// the matching mod, then clear the query parameter through setFocusParams.
```

The seven existing producers of that target, verified this session. Six already emit the
identical string through the router; one assembles it by hand and bypasses the router:

```
src/components/foundry/AssetSourcesPanel.tsx:263   navigate(...)
src/components/foundry/AssetSourcesPanel.tsx:314   navigate(...)
src/components/foundry/MyChanges.tsx:145           navigate(...)  // openInInstalled callback
src/components/locker/GlobalSoundShelf.tsx:56      navigate(...)
src/components/locker/HeroSoundShelf.tsx:163       navigate(...)
src/components/locker/HeroCardPicker.tsx:412       direct hash assignment, not the router
```

The four existing catalog keys holding the provenance phrase, all four with the identical
value "Open in Installed", each read from exactly one call site:

```
foundry.sources.openInInstalled       -> AssetSourcesPanel.tsx:319 (title at :315)
foundry.myChanges.openInInstalled     -> MyChanges.tsx:636
poolView.openInInstalled              -> ChangePools.tsx:401
soundLocker.row.openInInstalled       -> SoundEntryRow.tsx:174
```

From `src/components/foundry/AssetSourcesPanel.tsx` (existing, :192 and :200-203), the
house pattern for a stated blocker beside a disabled control. This plan copies this shape
rather than inventing a second one:

```typescript
<button disabled={displayedLoading} title={t('foundry.sources.inspectHint')}>...</button>
{displayedLoading && (
  <p className="mt-1 flex items-center gap-1 text-[10px] text-text-secondary" aria-live="polite">
    {t('foundry.sources.inspecting')}
  </p>
)}
```

From `src/lib/useAssetClaims.ts` (existing, head comment lines 8-9), the house pattern for
pending work that preserves the prior answer. This plan copies this shape too:

```
 *  - A stale answer is kept on screen while a refresh runs, rather than being
 *    replaced by a spinner. The panel says it is refreshing; it does not blank.
```

From `src/pages/Conflicts.tsx` (existing), the scan state and the blanking branch:

```typescript
const [loading, setLoading] = useState(true);                 // :177
const loadConflicts = async () => { setLoading(true); ... };  // :215-216
if (loading) { return <ConflictsSkeleton />; }                // :588-590
```
`loadConflicts` is also awaited at :431, :468, :514 and :540 after every ignore, unignore,
disable and reorder, so the blanking fires on far more than the explicit Refresh buttons
at :601, :631 and :690.

From `src/pages/Conflicts.tsx` (existing), the in-flight flags that disable resolve controls:

```typescript
const [pendingPair, setPendingPair] = useState<string | null>(null);         // :200
const [pendingGlobalFile, setPendingGlobalFile] = useState<string | null>(null); // :172
const [pendingIgnoredMod, setPendingIgnoredMod] = useState<string | null>(null); // :176
```
Disabled sites: :814, :872, :1061, :1105, :1122 (pair), :1157 (global file), :1212 (ignored mod).

From `src/pages/Profiles.tsx` (existing), the in-flight flags and the seven controls they
disable with no stated blocker:

```typescript
const isApplying = applyingId === profile.id;   // :712
const isUpdating = updatingId === profile.id;   // :713
```
Disabled sites inside the profile card: :753, :824 (Apply), :849, :862, :873, :893, :970.

From `src/components/foundry/FoundryBuildTray.tsx` (existing, :186-196), the forge and
install button and the red check-failure alert that sits above it. The alert is for a
different fact and stays untouched:

```typescript
{blocked && (<p role="alert" ...>{blocked}</p>)}
<button type="button" onClick={() => void requestConfirmation()}
        disabled={!review.selected.length || busy} ...>
```

i18n keys this plan consumes. Every one is created by plan 05-01 task 2; this plan adds none:

```
common.provenance.openInInstalled       "Open in Installed"
common.provenance.openInInstalledHint   "Open this mod in the Installed list"
profiles.actions.busyBlocker            "Finish the profile change that is running before starting another."
conflicts.actions.busyBlocker           "Finish the change that is running before starting another."
conflicts.rescanning                    "Rechecking your installed mods. The results below are from the previous check."
foundry.buildTray.blockedEmpty          "Select at least one staged edit to forge."
foundry.buildTray.blockedBusy           "Forging is running. This will finish in a moment."
```
</interfaces>
</context>

<planner_decisions>
Recorded so the executor does not re-litigate them.

- **Three sub-items were verified against the working tree before any task was written, and three of the surfaces they name turned out to be already satisfied.** Those are recorded here as verdicts with evidence rather than planned as work, because planning work that is already done is how a phase acquires a false completion record. The verdicts:
  - **Catalog sync already preserves the prior answer.** `src/components/SyncIndicator.tsx` is a non-blocking badge that renders beside the content and returns null when there is no sync; `src/pages/Browse.tsx:3481` disables only the sync trigger button while `syncing` is true and never unmounts the results list. Nothing blanks. No task.
  - **Asset source inspection already preserves the prior answer, and is the pattern to copy.** `src/lib/useAssetClaims.ts:8-9` states the rule in its own head comment, `src/lib/inspectedAssetClaims.ts:157` repeats it, and `AssetSourcesPanel.tsx:111` renders `displayedLoading` beside a retained `displayedResult`. No task; this plan cites it as the analog for the conflict scan.
  - **The asset sources panel already states its blocker beside the disabled control.** `AssetSourcesPanel.tsx:192` disables the inspect button and `:200-203` renders `foundry.sources.inspecting` as an `aria-live` line naming the reason. This is the D-16-compliant precedent. No task; this plan copies its shape onto three pages that lack it.
  - **Portrait coverage has no re-run that blanks a prior answer.** `HeroCardPicker.tsx:96` initialises `loading` to true and `:163` is the only place it is set true again, inside the mount effect; the component's own comment at `:93-94` states it is remounted per hero, so there is no in-place refresh to preserve an answer across. The retry that plan 05-02 adds to `HeroPortraitFamilies` runs from a failed state, which has no prior answer by definition. No task.
- **What was genuinely missing, and is planned here.** The conflict scan blanks the entire page to a skeleton on every re-run, including the four post-mutation reloads. Profiles apply and Conflicts resolve disable controls with no stated blocker at all. The Foundry forge button has two disabled reasons and explains neither. The Conflicts page has no provenance affordance of any kind, which is one of the four surfaces Lane 9 names by name. One of the seven provenance producers bypasses the router.
- **Provenance: the phrase half was already converged, the enforcement was not.** All four existing keys hold the identical value "Open in Installed", so the user already sees one phrase on the three surfaces that have the affordance. That is not the same as being safe from drift: four keys with one value is exactly the state the four keys were in before they drifted the first time, which is what Lane 9 recorded. Consolidating to one key is what makes the value converge structurally rather than coincidentally, and it is why task 2 exists at all. Recorded as an explicit planner judgement, not a silent build.
- **One helper for the target, for the same reason.** Six of seven producers emit the identical string today, so a helper changes no behaviour for them. It changes what happens next: an eighth producer cannot invent an eighth spelling. The one producer that does differ, `HeroCardPicker.tsx:412`, bypasses React Router entirely by writing the location hash, which is a genuine behavioural divergence and not just a stylistic one.
- **Task 2 exceeds the five-file sizing heuristic, deliberately and with the reason stated.** It touches six files. Every edit in it is a single-line mechanical substitution at a line number this plan already cites, with no new logic in any of them, so the context cost is far below what six files normally implies. Splitting it would produce a fourth task in a plan the sizing rules cap at three, which is the worse trade.
- **The wave choice, and why it is wave 4 alone.** Two constraints forced it. `src/components/locker/HeroCardPicker.tsx` and `src/components/foundry/MyChanges.tsx` are both in plan 05-02's `files_modified`, so this plan cannot share wave 2. Plan 05-05 drives the working tree over CDP and its own must-haves require a tree nobody else is editing, so this plan cannot share wave 3 either. A wave of its own after 05-05 makes the zero-overlap rule hold by construction rather than by inspection.
- **This plan touches no catalog file.** The seven keys it reads are added by plan 05-01 task 2, which owns the entire phase's catalog change in wave 1. That single-owner rule is deliberate and is preserved here: the alternative, letting this plan add its own keys, would put a second writer on `src/locales/en/translation.json` and would make `pnpm i18n:manifest` a race between plans. `depends_on` lists 05-01 for exactly this reason.
- **One blocker reason at a time, matching plan 05-03.** Where two flags could both be true, the one describing the more disruptive operation wins the message. On the forge button that is the running forge over the empty selection; on a profile card it is whichever of apply or update is actually running, and they are mutually exclusive per profile id.
</planner_decisions>

<artifacts_this_phase_produces>
Symbols this plan creates. Every i18n key it reads was created by plan 05-01; this plan adds none.

- New files: `src/lib/provenance.ts`, `src/lib/provenance.test.ts`
- Functions: `focusModPath` (`src/lib/provenance.ts`)
- DOM contract: a stable blocker-line id per profile card in `src/pages/Profiles.tsx`, referenced by `aria-describedby` on every control that card's in-flight state disables
- DOM contract: a stable blocker-line id per conflict card and per ignored-panel row in `src/pages/Conflicts.tsx`, referenced the same way
- DOM contract: a stable blocker-line id above the forge button in `src/components/foundry/FoundryBuildTray.tsx`, referenced by `aria-describedby` on that button
- State in `src/pages/Conflicts.tsx`: a first-load flag distinguishing the initial scan from a recheck, so only the initial scan renders the skeleton

<!-- planner-discipline-allow: soundLocker.row.openInInstalled, foundry.myChanges.openInInstalled, poolView.openInInstalled, foundry.sources.openInInstalled, foundry.sources.openInInstalledHint, location.hash -->
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One provenance route, proved end to end from the surface that had none</name>
  <reversibility rating="reversible">Adding a helper and one new navigation affordance changes no stored data and no route contract; reverting is deleting two new files and one button.</reversibility>
  <read_first>
    - src/pages/Conflicts.tsx
    - src/pages/Installed.tsx
    - src/components/locker/HeroCardPicker.tsx
    - src/lib/useAssetClaims.ts
    - .planning/phases/05-one-inventory-one-journey/05-PATTERNS.md
    - .planning/phases/05-one-inventory-one-journey/05-VALIDATION.md
    - docs/locker-consistency-pass.md
  </read_first>
  <files>src/lib/provenance.ts, src/lib/provenance.test.ts, src/pages/Conflicts.tsx, src/components/locker/HeroCardPicker.tsx</files>
  <behavior>
    - `focusModPath` given an ordinary mod id returns a root-relative path carrying that id in the `focusMod` query parameter, and nothing else
    - An id containing a space, an ampersand or a question mark comes back percent-encoded, so it survives a round trip through `URLSearchParams`
    - An id that already looks like a query fragment cannot inject a second parameter: parsing the returned path yields exactly one parameter
    - The returned value is always root-relative and never carries a scheme or a host, so no input can turn a provenance click into an off-app navigation
    - An empty id returns a path with an empty parameter value rather than throwing, because a caller with no id is a caller bug and not a crash
  </behavior>
  <action>
Wire ONE provenance path end to end: a pure route builder, a brand new call site on the one Lane 9 surface that has no provenance affordance at all, the router, and the existing focus consumer in Installed that this task does not modify.

Create `src/lib/provenance.ts` as a pure module: no React, no Zustand, no IPC. Export `focusModPath(modId: string): string`, returning a root-relative path built from the literal `focusMod` query parameter with the id passed through `encodeURIComponent`. Write a head comment stating the rule this module exists to enforce, per D-17: asking which mod is responsible for something has one answer target across the whole app, and a producer that builds the string by hand is how four surfaces ended up with four spellings before. Name the consumer, the focus effect in `src/pages/Installed.tsx` near line 975, so a future reader knows what the string is for. Do not export anything else from this module in this task.

Create `src/lib/provenance.test.ts` covering every case in this task's behavior block, in the plain-node style of the sibling `src/lib/*.test.ts` files. Assert the shape by parsing the returned value with `URLSearchParams` rather than by string comparison, so the test proves the parameter survives rather than proving the exact characters.

Add the provenance affordance to the Conflicts card in `src/pages/Conflicts.tsx`. The per-side renderer near line 780, which already receives a `ModWithThumbnail` and renders its name, thumbnail and a disable button, gains one more control beside the existing disable button: a small icon button reading `common.provenance.openInInstalled` with `common.provenance.openInInstalledHint` as its `title` and `aria-label`, whose `onClick` navigates to `focusModPath(mod.id)`. Import `useNavigate` from `react-router-dom` in this file, which does not currently use it. Render the control only when the side actually resolved to a mod record from `modsMap`; a side that did not resolve keeps exactly the markup it has today, because a provenance link pointing at nothing is worse than no link. Match the existing disable button's size, spacing and hover treatment exactly and introduce no new spacing value or tone.

Repoint the one divergent producer. `src/components/locker/HeroCardPicker.tsx` line 412 assembles its destination by hand and writes it straight onto the window location, bypassing React Router. Replace that inline assignment with a `navigate` call built from `focusModPath`, importing `useNavigate` from `react-router-dom` and `focusModPath` from `../../lib/provenance`. Change nothing else about the `FoundryPoolList` embed: its props, its wrapper markup and its shuffle wiring stay exactly as they are, because plan 05-02 owns the captions on this element and this task must not collide with that intent.

Leave `src/pages/Installed.tsx` untouched. It is the consumer, it already reads the parameter this helper emits, and plan 05-03 owns that file.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/provenance.test.ts && pnpm typecheck && pnpm lint && pnpm i18n:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib/provenance.test.ts` exits 0
    - `pnpm typecheck` exits 0, `pnpm lint` exits 0, `pnpm i18n:check` exits 0
    - `pnpm exec vitest run` exits 0 with no previously passing test now failing
    - `src/lib/provenance.ts` exports `focusModPath` and is framework free: `grep -Eq "from 'react'|from 'zustand'|electronAPI|react-router" src/lib/provenance.ts` finds no match
    - `src/lib/provenance.test.ts` contains at least five `it(` cases, one per behavior line
    - `src/pages/Conflicts.tsx` imports `focusModPath` from `../lib/provenance` and calls it inside the per-side card renderer
    - `src/pages/Conflicts.tsx` contains `common.provenance.openInInstalled`
    - `grep -c 'location.hash' src/components/locker/HeroCardPicker.tsx` reports 0, proving the hand-assembled destination is gone
    - `src/components/locker/HeroCardPicker.tsx` imports `focusModPath` from `../../lib/provenance`
    - `git diff --name-only` for this task does not list `src/pages/Installed.tsx`, `src/locales/en/translation.json` or `src/locales/manifest.json`
    - Behaviour (per D-17): from a conflict card, clicking the new control lands on Installed with the named mod scrolled to and highlighted, which is the same landing the sound shelf control already produces
  </acceptance_criteria>
  <done>A conflict card can reach the mod it names in Installed, the destination is produced by one tested helper, the one producer that bypassed the router no longer does, and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Every remaining provenance producer and label reads the one helper and the one key</name>
  <read_first>
    - src/lib/provenance.ts
    - src/components/locker/SoundEntryRow.tsx
    - src/components/locker/GlobalSoundShelf.tsx
    - src/components/locker/HeroSoundShelf.tsx
    - src/components/foundry/ChangePools.tsx
    - src/components/foundry/MyChanges.tsx
    - src/components/foundry/AssetSourcesPanel.tsx
  </read_first>
  <files>src/components/locker/SoundEntryRow.tsx, src/components/locker/GlobalSoundShelf.tsx, src/components/locker/HeroSoundShelf.tsx, src/components/foundry/ChangePools.tsx, src/components/foundry/MyChanges.tsx, src/components/foundry/AssetSourcesPanel.tsx</files>
  <action>
Six single-line mechanical substitutions, no new logic anywhere. This is the task that makes "one phrase and one target" structural rather than coincidental: the four surfaces already show identical copy today, but they do it from four independent keys, which is the exact configuration they were in before they drifted the first time.

Label substitutions. Each of these files reads its own namespaced provenance key with the value "Open in Installed". Repoint each to `common.provenance.openInInstalled`, the single key plan 05-01 created, keeping the surrounding element, icon and class list exactly as it is:

- `src/components/locker/SoundEntryRow.tsx` line 174, the `title` on the open control
- `src/components/foundry/ChangePools.tsx` line 401, the `title` on the pool row's open control
- `src/components/foundry/MyChanges.tsx` line 636, the rendered label beside its `ExternalLink` icon
- `src/components/foundry/AssetSourcesPanel.tsx` lines 315 and 319, the `title` and the rendered label; the `title` moves to `common.provenance.openInInstalledHint`

Where a call site passes a `t()` fallback string as the second argument, keep a fallback and set it to the same "Open in Installed" text, matching how the rest of the tree calls `Tx` and `t` with a fallback.

Target substitutions. Each of these files builds the destination inline. Import `focusModPath` from the correct relative path for the file and replace the inline template literal with a call to it, leaving the surrounding `navigate` call, its `useCallback` wrapper and its dependency array otherwise unchanged:

- `src/components/locker/GlobalSoundShelf.tsx` line 56
- `src/components/locker/HeroSoundShelf.tsx` line 163
- `src/components/foundry/MyChanges.tsx` line 145
- `src/components/foundry/AssetSourcesPanel.tsx` lines 263 and 314

After this task, the only place in `src/` that knows how the provenance destination is spelled is `src/lib/provenance.ts`, and the only place that knows how the provenance phrase is worded is one catalog key.

Do not delete the four superseded keys from `src/locales/en/translation.json`. Plan 05-01 owns that file for this phase and a second writer on it would make the manifest regeneration a race between plans. Record the four now-unread key paths in this plan's summary as a follow-up so the removal happens as a deliberate catalog change rather than a side effect here.
  </action>
  <verify>
    <automated>pnpm exec vitest run && pnpm typecheck && pnpm lint && pnpm i18n:check && pnpm encoding:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0, `pnpm lint` exits 0, `pnpm i18n:check` exits 0, `pnpm encoding:check` exits 0
    - `pnpm exec vitest run` exits 0 with no previously passing test now failing
    - `grep -rc 'common.provenance.openInInstalled' src/ --include=*.tsx | grep -v ':0' | wc -l` reports 5 or more files, counting the four repointed here plus the Conflicts call site from task 1
    - `grep -rn "focusMod=" src/ --include=*.tsx --include=*.ts | grep -v 'src/lib/provenance' | grep -v 'src/pages/Installed.tsx'` returns no match, proving `src/lib/provenance.ts` is the only remaining producer of the destination
    - Each of the six files in this task imports `focusModPath` or reads `common.provenance.openInInstalled`, verified by a `grep -l` over the file list reporting six files
    - `git diff --name-only` for this task lists exactly the six files named in this task and no catalog file
    - Behaviour (per D-17): the open control on a sound row, a pool row, a My changes row and an asset sources row all read the same words and all land on the same Installed target they landed on before
  </acceptance_criteria>
  <done>Provenance has exactly one producer for its target and exactly one key for its phrase across every surface that offers it, and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 3: A stated blocker on Profiles apply, Conflicts resolve and the Foundry forge path, and a conflict recheck that keeps its answer</name>
  <read_first>
    - src/pages/Profiles.tsx
    - src/pages/Conflicts.tsx
    - src/components/foundry/FoundryBuildTray.tsx
    - src/components/foundry/AssetSourcesPanel.tsx
    - src/lib/useAssetClaims.ts
    - docs/ui-conventions.md
    - docs/locker-consistency-pass.md
  </read_first>
  <files>src/pages/Profiles.tsx, src/pages/Conflicts.tsx, src/components/foundry/FoundryBuildTray.tsx</files>
  <action>
Four changes across three pages, all following patterns this repository already ships rather than inventing new ones. The blocker shape is the one `AssetSourcesPanel.tsx` lines 192 and 200 to 203 already use: a disabled control paired with an inline `aria-live` line that names the reason, plus an `aria-describedby` tying them together so the reason reaches assistive technology and not only the eye. The preserved-answer shape is the one `src/lib/useAssetClaims.ts` states in its own head comment at lines 8 and 9.

Profiles apply, per D-16. Inside the profile card render near line 712, derive a stable id from the profile id for a blocker line, and render that line inside the card's action row whenever `isApplying` or `isUpdating` is true. Its text is `profiles.actions.busyBlocker`, drawn with the card's existing secondary text treatment and `aria-live="polite"`. Set `aria-describedby` to that id on each of the seven controls those two flags disable, at lines 753, 824, 849, 862, 873, 893 and 970. Keep every existing `title` attribute exactly as it is: D-16 permits a tooltip to supplement the explanation and forbids only a tooltip that is the sole explanation. The two flags are per profile id and mutually exclusive, so exactly one reason is ever in force per card.

Conflicts resolve, per D-16. Do the same for the three in-flight flags this page already tracks. For a conflict card, derive the blocker line id from the same pair key the card's disabled expressions already compare against, render the line inside the card whenever that pair is the pending one, and point the controls at lines 814, 872, 1061, 1105 and 1122 at it with `aria-describedby`. For the ignored-files panel row at line 1157 and the ignored-mods panel row at line 1212, derive the id from the file path and the mod identity respectively and do the same. The text for all of them is `conflicts.actions.busyBlocker`. Keep the existing `title` attributes.

The Foundry forge and install path, per D-16. The button at line 194 has two independent disabled reasons and explains neither. Add a blocker line directly above it with a stable id, rendered whenever the button is disabled, and set `aria-describedby` to that id on the button. Its text is `foundry.buildTray.blockedBusy` while a forge is running and `foundry.buildTray.blockedEmpty` when nothing is staged and selected, with the running case winning if both could apply, so exactly one reason shows at a time. The empty case names the corrective action, which is what D-16 asks for over a bare statement. Leave the red check-failure alert above it completely alone: that is a different fact, it already has `role="alert"`, and it is correct as it stands. Use the tray's existing secondary text treatment and introduce no new tone.

Pending work preserves the prior answer, on the conflict scan. Today `loadConflicts` sets the page's `loading` flag true at line 216 and the branch at line 588 replaces the whole page with the skeleton, and because `loadConflicts` is awaited again at lines 431, 468, 514 and 540, that blanking fires after every ignore, unignore, disable and reorder as well as on the explicit Refresh buttons. Split the two facts apart. Keep the existing flag as the first-load signal by tracking, in component state, whether a scan has ever completed. Guard the skeleton branch on the first-load case only, so an initial scan with no prior answer behaves exactly as it does now. For every later scan, keep the existing list rendered and add one line above it reading `conflicts.rescanning` with `aria-live="polite"`, which states both that a recheck is running and that what is on screen is from the previous check. That second half is the part that stops a retained row being mistaken for a current one. Leave the error branch at line 592 and the empty branch at line 611 exactly as they are: a failed recheck is a different fact from a pending one and already has its own retry. The state setters at lines 265 to 276 already run only after every await resolves, so the swap is already atomic and needs no change.

Add no i18n key in this task. All five strings it reads are created by plan 05-01.
  </action>
  <verify>
    <automated>pnpm exec vitest run && pnpm typecheck && pnpm lint && pnpm i18n:check && pnpm encoding:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0, `pnpm lint` exits 0, `pnpm i18n:check` exits 0, `pnpm encoding:check` exits 0
    - `pnpm exec vitest run` exits 0 with no previously passing test now failing
    - `grep -c 'aria-describedby' src/pages/Profiles.tsx` reports 7 or more
    - `grep -c 'aria-describedby' src/pages/Conflicts.tsx` reports 7 or more
    - `grep -c 'aria-describedby' src/components/foundry/FoundryBuildTray.tsx` reports 1 or more
    - `src/pages/Profiles.tsx` contains `profiles.actions.busyBlocker`, `src/pages/Conflicts.tsx` contains `conflicts.actions.busyBlocker` and `conflicts.rescanning`, and `src/components/foundry/FoundryBuildTray.tsx` contains both `foundry.buildTray.blockedBusy` and `foundry.buildTray.blockedEmpty`
    - `git diff src/components/foundry/FoundryBuildTray.tsx` shows no changed line inside the `role="alert"` paragraph, proving the check-failure alert was left alone
    - `git diff src/pages/Profiles.tsx src/pages/Conflicts.tsx` shows no line that removes an existing `title` attribute, proving tooltips were supplemented rather than replaced
    - `git diff --name-only` for this task lists exactly the three files named in this task
    - Behaviour (per D-16): with a profile applying, every other control on that card is disabled and one visible line in the card names the reason; with a forge running, the forge button is disabled and one visible line above it names the running forge rather than the empty tray
    - Behaviour (per D-15): pressing Refresh on a Conflicts page that already lists conflicts keeps every row on screen, adds one line saying a recheck is running against the previous results, and replaces the rows only when the recheck resolves; a first visit with no prior answer still shows the skeleton
  </acceptance_criteria>
  <done>No control disabled by a Profiles apply, a Conflicts resolve or a Foundry forge leaves its blocker to be guessed, a conflict recheck no longer blanks the answer the user is reading, and the change is committed.</done>
</task>

</tasks>

<threat_model>
ASVS level 1. Disposition policy: any threat rated high or critical blocks the plan until it carries
a concrete mitigation; medium and low may be accepted with a stated rationale. No threat in this
register is rated high or critical, so nothing here blocks.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mod metadata to route | A mod id, which originates from a GameBanana download or a user import, is interpolated into a client-side route by the new provenance helper |
| renderer to user | Blocker and recheck copy asserts a fact about an operation's state, and a wrong assertion misleads the user about what the app is doing |
| retained state to render | A conflict answer from a previous scan stays on screen while a new scan runs, so a stale fact and a current fact occupy the same region |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-22 | Tampering | `focusModPath` building a route from a mod id (`src/lib/provenance.ts`) | low | mitigate | The id passes through `encodeURIComponent` and the return value is root-relative with no scheme and no host, so a crafted id can neither inject a second query parameter nor become an off-app navigation. Both are covered by cases in `provenance.test.ts`, asserted by parsing with `URLSearchParams` rather than by string comparison. |
| T-05-23 | Spoofing | a retained conflict answer read as current during a recheck | medium | mitigate | The retained answer is never shown unlabelled: `conflicts.rescanning` states both that a recheck is running and that the visible results come from the previous check, rendered in an `aria-live` region so the change is announced and not only drawn. The swap stays atomic because the state setters already run after every await resolves. |
| T-05-24 | Repudiation | a blocker naming a reason that is not the reason the control is disabled | medium | mitigate | Each blocker expression ranges over exactly the terms of its control's own disabled expression, and the precedence rule is stated so only one renders. An acceptance criterion pins the forge case, where the two terms are independent and the mismatch would be most plausible. |
| T-05-25 | Elevation of Privilege | a new provenance affordance on the Conflicts page reaching content the page does not already show | low | mitigate | The control navigates only, performs no mutation, opens no IPC channel, and is rendered only for a side that already resolved to a record in `modsMap` and is already displayed on that card. It can do nothing the user could not already do by finding the mod in Installed by hand. |
| T-05-26 | Information Disclosure | blocker copy leaking operation detail | low | accept | All five blocker strings are fixed catalog values with no interpolation of a path, an id or a file name. They name a state the user themselves just caused. |
| T-05-27 | Denial of Service | the first-load flag stranding the Conflicts page on a permanent recheck line | low | mitigate | The recheck line is bound to the same flag the existing `finally` block already clears at the end of `loadConflicts`, so it cannot outlive the scan; the error branch is left untouched and still offers its retry. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no external package (05-RESEARCH.md Package Legitimacy Audit: not applicable). No package-manager install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm typecheck` green and `pnpm lint` green, which `docs/ui-conventions.md` requires after any UI change
- `pnpm exec vitest run` green, including the new `src/lib/provenance.test.ts`
- `pnpm i18n:check` green and `pnpm encoding:check` green, with no key added to `src/locales/en/translation.json` and no regeneration of `src/locales/manifest.json` by this plan
- `git diff --name-only` across the whole plan lists no catalog file and does not list `src/pages/Installed.tsx`
- `grep -rn "focusMod=" src/ --include=*.tsx --include=*.ts` outside `src/lib/provenance.ts` and `src/pages/Installed.tsx` returns no match
</verification>

<success_criteria>
- Provenance is one phrase from one key and one target from one helper on all four surfaces Lane 9 names, including the Conflicts cards, which had no provenance affordance at all before this plan
- No control disabled by a Profiles apply, a Conflicts resolve or a Foundry forge and install leaves its blocker to be guessed, and no blocker is explained by a tooltip alone
- Exactly one blocker reason renders at a time per control
- Re-running the conflict scan preserves the answer the user is reading and says plainly that what is on screen is from the previous check
- The surfaces that already satisfied a sub-item are recorded as satisfied with file and line evidence and are not rewritten
</success_criteria>

<output>
Create `.planning/phases/05-one-inventory-one-journey/05-06-SUMMARY.md` when done
</output>
