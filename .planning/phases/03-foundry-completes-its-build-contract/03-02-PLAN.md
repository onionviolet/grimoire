---
phase: 03-foundry-completes-its-build-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/foundry/ShuffleToggleButton.tsx
  - src/components/locker/HeroSkinsPanel.tsx
  - src/components/foundry/SoundBrowse.tsx
  - src/lib/foundrySoundShuffle.ts
  - src/lib/foundrySoundShuffle.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-foundry-sound-shuffle-surfacing, REQ-foundry-pool-audition-fidelity]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user can add or remove one of a hero's installed sound-swap mods from the launch shuffle pool without leaving Foundry, and the same membership is reflected in the Locker for that hero (per D-09)"
    - "Foundry's shuffle toggle writes through appStore's existing toggleSoundShuffleIncluded action keyed by shuffleSoundKey(mod), with no second Set and no second localStorage key (per D-10, D-11)"
    - "A Foundry sound surface offers a link that lands on the Locker's view of that hero's sounds, reusing the already documented /locker/sounds?hero= contract rather than a new route"
    - "The shuffle toggle button rendered in Foundry and the two rendered inside HeroSkinsPanel are one component, so a change to the control cannot drift between the Locker and Foundry"
    - "Repeated presses of the same pool row advance the audition cursor through every clip and wrap at the end; a cursor at or past the pool length normalizes back into range and a single-clip pool stays at index 0"
    - "An event with zero clips does not throw and does not advance a cursor: poolCursor of a zero-length pool is 0 and advancePoolCursor of a zero-length pool is 0"
    - "Pool audition order is the pool's own index order and is stable across presses: the cursor advances by exactly one and wraps, never skipping or repeating within a cycle"
    - "Pool cursor arithmetic is integer modulo normalized for negative inputs, so no rounding, truncation or overflow contract is involved"
    - "REQUIREMENTS.md's traceability row for REQ-foundry-pool-audition-fidelity records the requirement as delivered before this phase, citing the commit and date, rather than leaving it Phase 3 Pending (per D-08)"
    - "A hero with zero installed sound-swap mods renders no shuffle row at all, rather than an empty-state banner"
    - "Pool membership is read from already-loaded local state, so the toggle has no loading treatment and no async gap"
    - "Each sound row's toggle carries its own aria-pressed, so a hero with some sounds in the pool and others out is representable with no aggregate indicator"
    - "One toggle renders per sound-swap mod, matching how HeroSkinsPanel already renders one per skin group"
    - "The toggle is a single inline button inside the existing row layout at the sm spacing step, introducing no new wrapping or scrolling container"
    - "The hero name interpolates only into the toggle's title and aria-label, never into rendered layout, so name length cannot widen the row"
    - "Navigation to the Locker sound view is a synchronous route change with no pending or interstitial state to render"
    - "The Locker link's visible label is the fixed Open in Sound Locker string, so a long hero name cannot widen it; the hero name travels in the query string"
    - "The Locker link is absent along with the row it sits in when a hero has no sound-swap mods"
    - "The Locker link renders at the existing compact-link treatment reused from MyChanges: 11px normal-weight secondary text with the ExternalLink icon at size 11"
    - "The Locker link wraps with its container rather than forcing horizontal scroll"
    - statement: "A failed pool write reverts the toggle's aria-pressed state rather than leaving a pressed button whose write did not land"
      verification: backstop
    - statement: "A hero display name that does not match a Locker roster entry lands on the unfiltered Sound Locker rather than on a silently empty filtered view"
      verification: backstop
  prohibitions:
    - requirement_id: REQ-foundry-sound-shuffle-surfacing
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT add or remove a hero's sound from the launch shuffle pool as a side effect of any other Foundry action; pool membership changes only on the user's explicit toggle press"
    - requirement_id: REQ-foundry-sound-shuffle-surfacing
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT enable, disable, reorder, rename or delete an installed mod from the Foundry shuffle surface; Installed and the Locker stay the only authority for enabled state"
    - requirement_id: REQ-foundry-pool-audition-fidelity
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT record REQ-foundry-pool-audition-fidelity as delivered on the strength of a document alone; the traceability correction must cite the source file whose behavior was read and the commit that shipped it"
  artifacts:
    - path: "src/components/foundry/ShuffleToggleButton.tsx"
      provides: "The single shuffle-inclusion toggle control shared by the Locker and Foundry"
      exports: ["default"]
    - path: "src/lib/foundrySoundShuffle.ts"
      provides: "Pure selection of a hero's installed sound-swap mods and their shuffle keys"
      exports: ["heroSoundShuffleRows", "soundLockerHref"]
    - path: "src/lib/foundrySoundShuffle.test.ts"
      provides: "Coverage for the hero filter, the key derivation and the Locker link string"
    - path: "src/components/foundry/SoundBrowse.tsx"
      provides: "The Foundry-side shuffle pool row and Locker link"
      contains: "ShuffleToggleButton"
  key_links:
    - from: "src/components/foundry/SoundBrowse.tsx"
      to: "src/stores/appStore.ts"
      via: "useAppStore reads soundShuffleIncluded and calls toggleSoundShuffleIncluded directly, with no prop threading through Locker.tsx"
      pattern: "toggleSoundShuffleIncluded"
    - from: "src/lib/foundrySoundShuffle.ts"
      to: "src/lib/lockerRandomizer.ts"
      via: "shuffleSoundKey is reused unchanged as the pool identity key"
      pattern: "shuffleSoundKey"
    - from: "src/components/locker/HeroSkinsPanel.tsx"
      to: "src/components/foundry/ShuffleToggleButton.tsx"
      via: "both inline shuffle buttons are replaced by the extracted component"
      pattern: "ShuffleToggleButton"
---

<objective>
Close the two pool-honesty gaps: make a hero's launch shuffle pool editable from inside Foundry with a link back to the Locker's fuller view of the same pool, and correct the traceability record for the pool-audition requirement that was already delivered before this phase started.

Purpose: REQ-foundry-sound-shuffle-surfacing asks that a user managing a pool they are looking at is not sent to another page to do it. REQ-foundry-pool-audition-fidelity is doc drift, not open work (D-06, D-07, D-08), and leaving it marked Pending is the kind of dishonesty PROJECT.md warns about.
Output: one extracted shuffle toggle component used by both surfaces, a pure hero-to-pool-rows helper with tests, a Foundry shuffle row plus Locker link, and a corrected REQUIREMENTS.md traceability row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-foundry-completes-its-build-contract/03-CONTEXT.md
@.planning/phases/03-foundry-completes-its-build-contract/03-RESEARCH.md
@.planning/phases/03-foundry-completes-its-build-contract/03-PATTERNS.md
@.planning/phases/03-foundry-completes-its-build-contract/03-UI-SPEC.md
@CLAUDE.md
@docs/ui-conventions.md

<interfaces>
Contracts the executor needs. Do not go looking for these in the codebase.

From `src/lib/lockerRandomizer.ts` (existing, line 55), reused unchanged per D-11:

```typescript
/** Separate namespace keeps a sound pack from colliding with a skin pack. */
export function shuffleSoundKey(mod: Mod): string {
  return `sound:${shuffleSkinKey(mod)}`;
}
export const SOUND_SHUFFLE_INCLUDED_KEY = 'lockerSoundShuffleIncluded';
```

From `src/stores/appStore.ts` (existing, lines 217, 306, 754-758), already globally reachable:

```typescript
soundShuffleIncluded: Set<string>;
toggleSoundShuffleIncluded: (soundKey: string) => void;
```

From `src/components/locker/HeroSkinsPanel.tsx` (existing), the exact markup to extract. Two near-identical copies live at roughly lines 646-672 and 846-872:

```tsx
{onToggleIncluded && (
  <button
    type="button"
    onClick={(e) => { e.stopPropagation(); onToggleIncluded(); }}
    aria-pressed={isIncluded}
    aria-label={isIncluded
      ? t('locker.randomize.removeFromShuffle', { name: primary.name })
      : t('locker.randomize.addToShuffle', { name: primary.name })}
    title={/* same two strings */}
    className={`absolute right-1.5 top-1.5 z-30 flex h-7 w-7 items-center justify-center rounded-full backdrop-blur-sm transition-[opacity,background-color,color] duration-150 focus-visible:opacity-100 group-hover/card:opacity-100 ${
      isIncluded
        ? 'opacity-100 bg-accent text-accent-foreground hover:bg-accent/80'
        : `${shuffleArmed ? 'opacity-100' : 'opacity-0'} bg-black/65 text-white/90 hover:bg-accent/70 hover:text-accent-foreground`
    }`}
  >
    <Shuffle className="h-3.5 w-3.5" />
  </button>
)}
```

From `src/components/foundry/MyChanges.tsx` (existing, lines 305-317), the exact Locker link pattern and the only route string that resolves correctly:

```tsx
<button
  type="button"
  onClick={() => navigate(scope ? `/locker/sounds?hero=${encodeURIComponent(scope)}` : '/locker/sounds/global')}
  className="flex items-center gap-1 text-[11px] font-normal text-text-secondary hover:text-text-primary"
>
  <ExternalLink size={11} /> {t('foundry.myChanges.openInSoundLocker', 'Open in Sound Locker')}
</button>
```

From `src/lib/lockerMode.ts` (existing, lines 61-66), the doc comment that names Foundry as this string's intended caller:

```typescript
// `/locker/sounds?hero=<display name>` is how Foundry's My changes panel
// links a hero's sounds. The path alone reads as the old landing page, so
// without the query it would rewrite to the grid and silently drop the
// hero the user asked for.
```

From `src/components/foundry/SoundBrowse.tsx` (existing, line 124), the mount surface and what it already has in scope:

```typescript
export default function SoundBrowse({ heroes, heroNames, hero: scopedHero, only, onStage }: SoundBrowseProps)
// heroNames: Map<string, string>  (codename -> display name)
// scopedHero: string | null       (codename)
// useAppStore is already imported at line 53
```
</interfaces>
</context>

<planner_decisions>
- **Where the Foundry shuffle surface mounts.** Inside `SoundBrowse.tsx`, rendered whenever the view is hero scoped. `HeroWorkshop.tsx` renders `SoundBrowse` in both its Abilities section (`only="gameplay"`) and its Voice section (`only="voice"`), and those sections are mutually exclusive nav states, so the row appears exactly once on screen in either. The top-level Foundry Sounds tab renders it once a hero is chosen. This keeps `HeroWorkshop.tsx` out of this plan entirely, which is what lets it run in the same wave as plan 03-01.
- **No new i18n keys.** Per the UI-SPEC copy contract, the toggle reuses `locker.randomize.addToShuffle` and `locker.randomize.removeFromShuffle`, the link reuses `foundry.myChanges.openInSoundLocker`, and the row heading reuses `locker.randomize.onLaunch` ("Shuffle on launch"). This plan therefore does not touch `src/locales/en/translation.json` or `src/locales/manifest.json`.
- **No code ships for pool audition.** D-07. `src/components/foundry/useClipPlayer.ts` and `src/components/settings/ForkBuildCard.tsx` are read only in this plan, never edited. `src/components/foundry/useClipPlayer.test.ts` already covers the cursor contract; task 3 asserts that coverage rather than adding to it.
</planner_decisions>

<artifacts_this_phase_produces>
Symbols this plan creates. Newly created, so not subject to source-grounding drift verification.

- React components: `ShuffleToggleButton` (default export of `src/components/foundry/ShuffleToggleButton.tsx`)
- Functions: `heroSoundShuffleRows`, `soundLockerHref` (`src/lib/foundrySoundShuffle.ts`)
- Types: `HeroSoundShuffleRow` (`src/lib/foundrySoundShuffle.ts`)
- New files: `src/components/foundry/ShuffleToggleButton.tsx`, `src/lib/foundrySoundShuffle.ts`, `src/lib/foundrySoundShuffle.test.ts`
- i18n keys: none. Every string reuses an existing key.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shuffle toggle into one component and pure hero-pool selection</name>
  <read_first>
    - src/components/locker/HeroSkinsPanel.tsx
    - src/lib/lockerRandomizer.ts
    - src/lib/lockerUtils.ts
    - src/types/mod.ts
    - src/components/foundry/MyChanges.tsx
    - src/lib/lockerMode.ts
    - docs/ui-conventions.md
  </read_first>
  <files>src/components/foundry/ShuffleToggleButton.tsx, src/components/locker/HeroSkinsPanel.tsx, src/lib/foundrySoundShuffle.ts</files>
  <action>
Create `src/components/foundry/ShuffleToggleButton.tsx` as the single definition of the shuffle-inclusion control. Props: `isIncluded: boolean`, `onToggle: () => void`, `name: string` (the display name interpolated into the accessible label), `armed?: boolean` (the Locker's hover-reveal behaviour), and `className?: string` for the caller's positioning. The component owns the icon, the `type="button"`, the `aria-pressed={isIncluded}`, the `aria-label` and `title` both reading `locker.randomize.removeFromShuffle` when included and `locker.randomize.addToShuffle` when not, both interpolating `{{name}}`, and the pressed-versus-unpressed colour states. It calls `event.stopPropagation()` before `onToggle()` so a card-level click handler does not also fire. Positioning classes such as the Locker's `absolute right-1.5 top-1.5 z-30` are NOT baked in: they arrive through `className`, because the Foundry usage is an inline row control, not an overlay on a card. The accent fill appears only in the pressed state, which is the entire accent budget this control is allowed under the UI-SPEC Color contract.

Replace both inline shuffle buttons in `src/components/locker/HeroSkinsPanel.tsx` with `ShuffleToggleButton`, passing the positioning classes each call site currently hardcodes (the two differ: roughly `right-1.5 top-1.5 h-7 w-7` at the first site and its larger sibling at the second) through `className`. This is a refactor with zero behaviour change: the rendered DOM, the two i18n keys, the `aria-pressed` value, the hover-reveal driven by `shuffleArmed`, and the `stopPropagation` must all stay exactly as they are today. Do not change the props `HeroSkinsPanel` receives from `Locker.tsx`, and keep the diff against `HeroSkinsPanel.tsx` minimal per the fork-divergence policy.

Create `src/lib/foundrySoundShuffle.ts` holding the pure half, so the Foundry surface has something testable without a DOM. Export `interface HeroSoundShuffleRow { mod: Mod; key: string; name: string }` and `heroSoundShuffleRows(mods: readonly Mod[], heroDisplayName: string): HeroSoundShuffleRow[]`, which selects only mods carrying a `soundSwap` record scoped to that hero, compares hero names through `canonicalHeroName` from `src/lib/lockerUtils.ts` so an alias recorded by one surface still matches the roster name another used, derives `key` from `shuffleSoundKey(mod)` reused unchanged, uses the mod name as `name`, and returns a stable order (mod name, then mod id as tie-break) so equal-comparing rows never reorder between renders. Resolve each mod's hero the same way `changeList.ts` does for a sound row: `mod.lockerHero` first, then `mod.soundSwap.reforge?.heroName`. Also export `soundLockerHref(heroDisplayName: string | null): string` returning the hero-scoped Locker sounds path carrying the display name as a percent-encoded `hero` query parameter, and the global sounds path when the name is null or blank. Copy both strings byte for byte from the `navigate` call in `MyChanges.tsx` shown in the interfaces block; do not retype them from memory. This function is the only place either route string is written in this plan.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm exec vitest run src/lib/lockerRandomizer.test.ts src/lib/lockerMode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0 and `pnpm lint` exits 0
    - `src/components/foundry/ShuffleToggleButton.tsx` exists and contains `aria-pressed`
    - `! grep -Eq 'aria-pressed=\{isIncluded\}' src/components/locker/HeroSkinsPanel.tsx` succeeds, proving no inline copy of the control remains in the panel
    - `grep -c 'ShuffleToggleButton' src/components/locker/HeroSkinsPanel.tsx` reports at least 3 (the import plus both call sites)
    - `src/lib/foundrySoundShuffle.ts` exports `heroSoundShuffleRows` and `soundLockerHref` and imports `shuffleSoundKey` from `../lib/lockerRandomizer` or the equivalent relative path
    - `! grep -Eq 'lockerSoundShuffleIncluded|new Set<string>\(\)' src/lib/foundrySoundShuffle.ts` succeeds, proving no second pool store was introduced
    - Behaviour: the Locker's skin cards still show the shuffle button on hover, still toggle pool membership, and still read the same accessible label as before
  </acceptance_criteria>
  <done>One shuffle toggle component is used by both HeroSkinsPanel call sites with no behaviour change, and the pure hero-pool selection helper exists, and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the shuffle pool row and the Locker link inside Foundry's sound surface</name>
  <read_first>
    - src/components/foundry/SoundBrowse.tsx
    - src/components/foundry/ShuffleToggleButton.tsx
    - src/lib/foundrySoundShuffle.ts
    - src/stores/appStore.ts
    - src/components/foundry/MyChanges.tsx
    - .planning/phases/03-foundry-completes-its-build-contract/03-UI-SPEC.md
  </read_first>
  <files>src/components/foundry/SoundBrowse.tsx</files>
  <action>
In `src/components/foundry/SoundBrowse.tsx` add a shuffle pool block rendered only when the view is hero scoped, that is when `scopedHero` resolves to a display name through `heroNames`. Read `mods`, `soundShuffleIncluded` and `toggleSoundShuffleIncluded` directly from `useAppStore`, which the file already imports; do not thread any of this through props and do not route it via `Locker.tsx`. Compute the rows with `heroSoundShuffleRows(mods, heroDisplayName)` inside a `useMemo` keyed on `mods` and the display name.

When the row list is empty, render nothing at all. Per D-11 and the UI-SPEC empty-state contract this is a deliberate absence, so do not add a "no sounds to shuffle yet" banner or any other empty-state copy.

When it is non-empty, render a compact section headed by the existing `locker.randomize.onLaunch` string, with one row per entry. Each row shows the mod name and a `ShuffleToggleButton` whose `isIncluded` is `soundShuffleIncluded.has(row.key)`, whose `onToggle` calls `toggleSoundShuffleIncluded(row.key)`, whose `name` is `row.name`, whose `armed` is true so the inline control is always visible rather than hover revealed, and whose `className` positions it inline at the sm spacing step (`gap-2`) instead of absolutely. Each row's toggle carries its own pressed state; there is no aggregate or tri-state indicator. Wrap the section in the md padding step (`p-4`) inside the surface's existing card treatment, and use existing primitives from `src/components/common` rather than new markup, per docs/ui-conventions.md.

Beside the heading render the Locker link using the compact-link treatment copied from `MyChanges.tsx`: a `type="button"` with `className="flex items-center gap-1 text-[11px] font-normal text-text-secondary hover:text-text-primary"`, the lucide `ExternalLink` icon at `size={11}`, and the label from the existing key `foundry.myChanges.openInSoundLocker`. Its click handler calls `navigate(soundLockerHref(heroDisplayName))` using React Router's `useNavigate`. The visible label is fixed, so the hero name reaches the Locker only through the query string.

Nothing in this block may enable, disable, reorder, rename or delete a mod. The only store action it is permitted to call is `toggleSoundShuffleIncluded`. Pool membership must change only on an explicit toggle press, never as a side effect of rendering, of a hero change, or of any other Foundry action.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0, `pnpm lint` exits 0, `pnpm test` exits 0
    - `src/components/foundry/SoundBrowse.tsx` contains `ShuffleToggleButton`, `toggleSoundShuffleIncluded`, `heroSoundShuffleRows` and `soundLockerHref`
    - `! grep -Eq 'toggleMod|deleteMod|reorderMods|setModPriorityFolder' src/components/foundry/SoundBrowse.tsx` succeeds inside the new block's region, proving the shuffle surface cannot change enabled state or order (region-scope the check to the added section if the file already calls one of these elsewhere)
    - `! grep -Eq "/locker/sounds\\?hero=" src/components/foundry/SoundBrowse.tsx` succeeds, proving the route string is written only in `src/lib/foundrySoundShuffle.ts`
    - `pnpm i18n:check` exits 0 with no new key added to `src/locales/en/translation.json`
    - Behaviour: with a hero that has at least one installed sound-swap mod selected in Foundry, the shuffle section renders one toggle per mod, pressing a toggle flips its pressed state, and navigating to the Locker for that hero shows the same membership
    - Behaviour: with a hero that has no installed sound-swap mods, no shuffle section and no link render
  </acceptance_criteria>
  <done>A user can add or remove a hero's sound from the launch shuffle pool without leaving Foundry and can reach the Locker's view of that pool from the same block, and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Cover the pool helpers with tests and correct the pool-audition traceability record</name>
  <read_first>
    - src/lib/foundrySoundShuffle.ts
    - src/lib/lockerRandomizer.test.ts
    - src/lib/lockerMode.test.ts
    - src/components/foundry/useClipPlayer.test.ts
    - src/components/settings/ForkBuildCard.tsx
    - .planning/REQUIREMENTS.md
    - .planning/phases/03-foundry-completes-its-build-contract/03-CONTEXT.md
  </read_first>
  <files>src/lib/foundrySoundShuffle.test.ts, .planning/REQUIREMENTS.md</files>
  <action>
Create `src/lib/foundrySoundShuffle.test.ts`. Cover `heroSoundShuffleRows`: a mod with no `soundSwap` is excluded; a sound-swap mod scoped to another hero is excluded; a mod whose hero was recorded as an alias still matches the roster name through `canonicalHeroName`; the returned `key` equals `shuffleSoundKey(mod)` exactly, proving no second key scheme; an empty mod list returns an empty array; and rows that compare equal on name keep a stable order decided by mod id. Cover `soundLockerHref`: a hero name produces the percent-encoded hero-scoped path, a name containing a space or a plus sign encodes correctly, and a null or blank name produces the global sounds path.

Run and read `pnpm exec vitest run src/components/foundry/useClipPlayer.test.ts`. Per D-06 the requirement is already delivered, and this run is how you confirm that against the tree rather than trusting the document. It already covers the pool cursor contract that REQ-foundry-pool-audition-fidelity asks for: walking the pool and wrapping at the end, staying at index 0 for a single-clip event, normalizing a stale out-of-range cursor, safety on an empty pool, and normalizing negatives. Per D-07 do not add to it, do not edit `src/components/foundry/useClipPlayer.ts`, and do not edit `src/components/settings/ForkBuildCard.tsx`.

Correct the traceability record in `.planning/REQUIREMENTS.md`. Its traceability row for this requirement currently carries Phase 3 and an open status. Rewrite it to record delivery as predating this phase, naming commit `9b01c63` (2026-07-26) and the two source files that were read to confirm it (`src/components/foundry/useClipPlayer.ts` for the per-press pool cycling, `src/components/settings/ForkBuildCard.tsx` for the default-on `forkPoolCycling` setting), and pointing at 03-CONTEXT.md D-06 through D-08 for the reasoning. Also correct the requirement's own bullet in the body of REQUIREMENTS.md, which still describes the superseded behaviour where the player only reaches the first clip; replace that description with what the code now does, keeping the requirement id and its position unchanged. Do not silently mark it complete: the correction must carry the evidence, or it is the same doc drift it is fixing.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/foundrySoundShuffle.test.ts src/components/foundry/useClipPlayer.test.ts && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib/foundrySoundShuffle.test.ts src/components/foundry/useClipPlayer.test.ts` exits 0
    - `src/lib/foundrySoundShuffle.test.ts` contains at least eight `it(` cases
    - `git diff --name-only` for this task does NOT list `src/components/foundry/useClipPlayer.ts` or `src/components/settings/ForkBuildCard.tsx`
    - `! grep -Eq 'REQ-foundry-pool-audition-fidelity.*Pending' .planning/REQUIREMENTS.md` succeeds
    - `.planning/REQUIREMENTS.md` contains `9b01c63` and `useClipPlayer` on or beside the corrected row
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>The Foundry pool helpers are covered by passing tests, the existing pool-audition coverage is confirmed rather than duplicated, REQUIREMENTS.md records the requirement as delivered before this phase with its evidence, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to browser localStorage | Shuffle pool membership is persisted client side under `SOUND_SHUFFLE_INCLUDED_KEY` |
| Foundry surface to appStore | A new caller gains write access to shuffle membership state that until now only the Locker page wrote |
| renderer to renderer (HashRouter) | A hero display name is placed in a route query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | Foundry shuffle block in `SoundBrowse.tsx` | medium | mitigate | The block is permitted exactly one store action, `toggleSoundShuffleIncluded`. It must not call `toggleMod`, `deleteMod`, `reorderMods` or `setModPriorityFolder`, so a Foundry surface cannot become a second authority over enabled state. Asserted by a region-scoped negative grep in Task 2's acceptance criteria. |
| T-03-06 | Tampering | `SOUND_SHUFFLE_INCLUDED_KEY` in `lockerRandomizer.ts` | low | mitigate | Reuse the single existing key and the single existing store action rather than a Foundry-local Set or key. Two stores for one concept would desync the moment either surface toggled independently. Asserted by the negative grep in Task 1's acceptance criteria and by the key-equality test in Task 3. |
| T-03-07 | Information Disclosure | `/locker/sounds?hero=` query string | low | accept | The hero display name is already user-visible on the surface producing the link, the route is resolved entirely client side by `resolveLockerRoute`, and no network request carries it. Accepting rather than adding an opaque id scheme for a name the user is looking at. |
| T-03-08 | Tampering | `.planning/REQUIREMENTS.md` traceability edit | low | mitigate | The correction must cite the commit and the source files read, so a later reader can re-verify the claim against the tree instead of trusting the row. Asserted by Task 3's acceptance criteria. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no external package (03-RESEARCH.md Package Legitimacy Audit: not applicable). No package-manager install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm typecheck` green
- `pnpm lint` green
- `pnpm test` green
- `pnpm i18n:check` green with no new key added
- `pnpm encoding:check` green
</verification>

<success_criteria>
- A hero's launch shuffle pool membership can be changed from inside Foundry, and the Locker reflects the same membership for the same hero
- Foundry offers a link that lands on the Locker's view of that hero's sounds, using the already documented route string
- One shuffle toggle component serves both the Locker and Foundry
- REQUIREMENTS.md records REQ-foundry-pool-audition-fidelity as delivered before this phase, with the commit and the source files as evidence
</success_criteria>

<output>
Create `.planning/phases/03-foundry-completes-its-build-contract/03-02-SUMMARY.md` when done
</output>
